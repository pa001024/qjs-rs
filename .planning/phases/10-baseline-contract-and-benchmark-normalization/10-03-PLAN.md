---
phase: 10-baseline-contract-and-benchmark-normalization
plan: 03
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
files_modified:
  - .github/scripts/check_engine_benchmark_contract.py
  - .github/scripts/benchmark_contract/fixtures/benchmark-report-valid.json
  - .github/scripts/benchmark_contract/fixtures/benchmark-report-missing-case.json
  - scripts/render_engine_benchmark_report.py
  - docs/engine-benchmarks.md
  - .github/workflows/ci.yml
  - .planning/phases/10-baseline-contract-and-benchmark-normalization/10-BASELINE-CONTRACT-EVIDENCE.md
autonomous: true
requirements:
  - PERF-01
  - PERF-02
must_haves:
  truths:
    - JSON artifacts are automatically contract-validated for required engines, metadata, and case coverage before they are treated as baseline evidence.
    - Human-readable report output surfaces contract metadata (profile, timing mode, comparator versions/status) alongside latency tables.
    - Local and CI runbooks use deterministic commands/output paths so benchmark evidence can be reproduced and audited.
  artifacts:
    - .github/scripts/check_engine_benchmark_contract.py
    - .github/scripts/benchmark_contract/fixtures/benchmark-report-valid.json
    - .github/scripts/benchmark_contract/fixtures/benchmark-report-missing-case.json
    - scripts/render_engine_benchmark_report.py
    - docs/engine-benchmarks.md
    - .github/workflows/ci.yml
    - .planning/phases/10-baseline-contract-and-benchmark-normalization/10-BASELINE-CONTRACT-EVIDENCE.md
  key_links:
    - `check_engine_benchmark_contract.py` validates benchmark JSON before report generation/evidence publication.
    - `render_engine_benchmark_report.py` reads contract metadata fields introduced in Plans 10-01/10-02 and renders them in markdown output.
    - CI invokes the contract checker with deterministic artifact paths documented in `docs/engine-benchmarks.md`.
---

<objective>
Close Phase 10 with automated benchmark-contract validation, normalized report rendering, and reproducible local/CI execution guidance.

Purpose: make PERF-01/PERF-02 evidence generation repeatable and governance-friendly.
Output: contract checker + report updates + CI/runbook wiring + phase evidence note.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-baseline-contract-and-benchmark-normalization/10-RESEARCH.md
@docs/benchmark-contract.md
@crates/benchmarks/src/main.rs
@scripts/render_engine_benchmark_report.py
@docs/engine-benchmarks.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement benchmark contract checker with deterministic fixtures/self-test</name>
  <files>.github/scripts/check_engine_benchmark_contract.py, .github/scripts/benchmark_contract/fixtures/benchmark-report-valid.json, .github/scripts/benchmark_contract/fixtures/benchmark-report-missing-case.json</files>
  <action>Create a repo-local checker script that validates schema version, run metadata completeness, required engine set, and required case IDs. Add fixture-backed `--self-test` mode covering pass + intentional failure scenarios. Keep checker deterministic and independent of live benchmark execution so CI can run it quickly.</action>
  <verify>
    <automated>python .github/scripts/check_engine_benchmark_contract.py --self-test</automated>
  </verify>
  <done>Contract checker reliably detects missing metadata/cases/engines and can be executed in CI without external runtime dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Update human-readable report renderer to surface contract metadata</name>
  <files>scripts/render_engine_benchmark_report.py</files>
  <action>Extend the markdown report renderer to display contract metadata (schema version, run profile, timing mode, comparator versions/status/paths, run controls including warmup). Preserve existing latency chart/table sections while clearly flagging unsupported or unavailable comparator states in output text.</action>
  <verify>
    <automated>python scripts/render_engine_benchmark_report.py --input .github/scripts/benchmark_contract/fixtures/benchmark-report-valid.json --chart target/benchmarks/contract-smoke.svg --report target/benchmarks/contract-smoke.md && rg --line-number "schema|profile|timing|quickjs|node" target/benchmarks/contract-smoke.md</automated>
  </verify>
  <done>Rendered reports include both performance numbers and reproducibility contract context required by PERF-01.</done>
</task>

<task type="auto">
  <name>Task 3: Wire CI/runbook contract checks and record Phase 10 evidence procedure</name>
  <files>docs/engine-benchmarks.md, .github/workflows/ci.yml, .planning/phases/10-baseline-contract-and-benchmark-normalization/10-BASELINE-CONTRACT-EVIDENCE.md</files>
  <action>Update benchmark docs with profile-based local/CI commands and deterministic artifact paths, including checker invocation order (run benchmark -> validate contract -> render report). Add a CI step that executes the benchmark contract checker against deterministic artifact location (or fixture in fast mode). Write `10-BASELINE-CONTRACT-EVIDENCE.md` capturing the exact command sequence and evidence files expected for Phase 10 closure.</action>
  <verify>
    <automated>rg --line-number "local-dev|ci-linux|check_engine_benchmark_contract.py|engine-comparison" docs/engine-benchmarks.md .github/workflows/ci.yml .planning/phases/10-baseline-contract-and-benchmark-normalization/10-BASELINE-CONTRACT-EVIDENCE.md</automated>
  </verify>
  <done>Phase documentation and CI both enforce the same reproducible contract workflow for benchmark evidence generation.</done>
</task>

</tasks>

<verification>
Acceptance requires passing checker self-test, successful report render smoke run from fixture JSON, and documented/wired deterministic command flow in both docs and CI.
</verification>

<success_criteria>
Phase 10 benchmark evidence is contract-validated, human-readable, and reproducible across local and CI execution contexts, satisfying PERF-01 and PERF-02 closure conditions.
</success_criteria>

<output>
After completion, create `.planning/phases/10-baseline-contract-and-benchmark-normalization/10-03-SUMMARY.md`.
</output>
