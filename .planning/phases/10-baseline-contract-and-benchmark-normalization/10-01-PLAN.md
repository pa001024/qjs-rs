---
phase: 10-baseline-contract-and-benchmark-normalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/benchmark-contract.md
  - crates/benchmarks/src/contract.rs
  - crates/benchmarks/src/main.rs
  - crates/benchmarks/tests/benchmark_contract.rs
autonomous: true
requirements:
  - PERF-01
  - PERF-02
must_haves:
  truths:
    - Benchmark JSON artifacts expose an explicit contract envelope (`schema_version`, `run_profile`, `timing_mode`, reproducibility metadata) instead of implicit shape.
    - The required hot-path case set is locked as stable IDs (`arith-loop`, `fib-iterative`, `array-sum`, `json-roundtrip`) with contract-level ownership.
    - Local and CI run controls are contract-defined (iterations/samples/warmup) and flow into artifact metadata for reproducibility.
  artifacts:
    - docs/benchmark-contract.md
    - crates/benchmarks/src/contract.rs
    - crates/benchmarks/src/main.rs
    - crates/benchmarks/tests/benchmark_contract.rs
  key_links:
    - `docs/benchmark-contract.md` defines the benchmark contract that `crates/benchmarks/src/contract.rs` enforces in code.
    - `crates/benchmarks/src/main.rs` serializes report artifacts according to the contract module, not ad-hoc inline structs.
    - `crates/benchmarks/tests/benchmark_contract.rs` fails if required case IDs or envelope fields drift from the contract.
---

<objective>
Define and lock the Phase 10 benchmark contract so subsequent normalization work has a deterministic schema and case baseline.

Purpose: eliminate implicit benchmark assumptions before adapter/reporting changes.
Output: benchmark contract spec + code-level contract module + automated contract tests.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-baseline-contract-and-benchmark-normalization/10-RESEARCH.md
@crates/benchmarks/src/main.rs
@docs/engine-benchmarks.md

<interfaces>
From `crates/benchmarks/src/main.rs` (current baseline):
```rust
fn benchmark_cases() -> Vec<BenchCase>;

#[derive(Debug, Serialize)]
struct BenchmarkReport {
    generated_at_utc: String,
    config: BenchmarkConfig,
    environment: EnvironmentInfo,
    cases: Vec<CaseReport>,
    aggregate: AggregateReport,
}
```

Contract implementation in this plan should move these implicit contracts into a dedicated module with explicit versioned fields.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author benchmark contract specification and artifact policy</name>
  <files>docs/benchmark-contract.md</files>
  <action>Create a benchmark contract document covering: schema version (`bench.v1`), required engines (`qjs-rs`, `boa-engine`, `quickjs-c`, `nodejs`), required case IDs (the 4 PERF-02 hot paths), run profiles (`local-dev`, `ci-linux`), timing mode enum, and deterministic artifact naming (`target/benchmarks/engine-comparison.&lt;profile&gt;.json`). Include mandatory reproducibility metadata fields and strict rules for missing/unsupported engines.</action>
  <verify>
    <automated>rg --line-number "bench.v1|local-dev|ci-linux|arith-loop|fib-iterative|array-sum|json-roundtrip|engine-comparison" docs/benchmark-contract.md</automated>
  </verify>
  <done>Contract doc is explicit enough that benchmark output shape and required case set can be validated mechanically.</done>
</task>

<task type="auto">
  <name>Task 2: Implement versioned contract module and wire runner/report envelope</name>
  <files>crates/benchmarks/src/contract.rs, crates/benchmarks/src/main.rs</files>
  <action>Add a dedicated contract module with strongly typed constants/structs for schema version, run profile, timing mode, required case catalog, and output naming policy. Refactor `main.rs` to consume this module for CLI parsing and report serialization (no duplicated inline schema definitions). Keep benchmark execution behavior unchanged in this plan; only codify and enforce the contract envelope and controls.</action>
  <verify>
    <automated>cargo run -p benchmarks -- --help | rg --line-number "profile|iterations|samples|output"</automated>
  </verify>
  <done>Runner compiles and emits help/argument surface that reflects the new contract controls.</done>
</task>

<task type="auto">
  <name>Task 3: Add contract regression tests for schema envelope and required case catalog</name>
  <files>crates/benchmarks/tests/benchmark_contract.rs, crates/benchmarks/src/main.rs</files>
  <action>Create focused tests that fail on contract drift: one test for exact required case ID set and one test for required benchmark report envelope fields (`schema_version`, `run_profile`, `timing_mode`, control metadata). Tests must avoid external engine process execution (use serialization/unit-level checks).</action>
  <verify>
    <automated>cargo test -p benchmarks benchmark_contract_required_case_ids -- --exact && cargo test -p benchmarks benchmark_report_contract_envelope_fields -- --exact</automated>
  </verify>
  <done>Contract drift is caught by deterministic tests before any long benchmark run.</done>
</task>

</tasks>

<verification>
Phase acceptance for this plan requires benchmark contract documentation, code-level contract ownership, and tests proving required PERF-02 case IDs plus PERF-01 schema envelope cannot silently drift.
</verification>

<success_criteria>
A versioned benchmark contract exists and is enforced in code/tests, enabling adapter normalization work to proceed against fixed artifacts and case coverage expectations.
</success_criteria>

<output>
After completion, create `.planning/phases/10-baseline-contract-and-benchmark-normalization/10-01-SUMMARY.md`.
</output>
