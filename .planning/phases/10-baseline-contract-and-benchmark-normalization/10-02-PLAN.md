---
phase: 10-baseline-contract-and-benchmark-normalization
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - crates/benchmarks/src/main.rs
  - crates/benchmarks/src/contract.rs
  - crates/benchmarks/tests/adapter_normalization.rs
  - docs/benchmark-contract.md
autonomous: true
requirements:
  - PERF-01
  - PERF-02
must_haves:
  truths:
    - Cross-engine timing semantics are explicitly normalized for one contract timing mode per run (no apples-to-oranges adapter behavior).
    - Comparator invocation is configurable and preflight-validated so missing binaries/paths fail fast with actionable diagnostics.
    - Artifacts include engine execution metadata (command/path/version/status + checksum/guard parity fields) needed for reproducible comparison evidence.
  artifacts:
    - crates/benchmarks/src/main.rs
    - crates/benchmarks/src/contract.rs
    - crates/benchmarks/tests/adapter_normalization.rs
    - docs/benchmark-contract.md
  key_links:
    - `main.rs` adapter runners implement one shared timing contract defined in `contract.rs`.
    - Comparator preflight metadata captured at run start is serialized into report environment/engine sections.
    - `adapter_normalization.rs` guards against reverting to inconsistent checksum/timing behavior across engines.
---

<objective>
Normalize adapter behavior and reproducibility metadata so benchmark numbers are truly comparable across `qjs-rs`, `boa-engine`, `quickjs-c`, and `nodejs`.

Purpose: remove comparability drift risk before optimization phases consume benchmark evidence.
Output: parity-aligned adapter execution flow + configurable comparator preflight + regression tests.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-baseline-contract-and-benchmark-normalization/10-RESEARCH.md
@docs/benchmark-contract.md
@crates/benchmarks/src/main.rs
@crates/benchmarks/src/contract.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce adapter timing/checksum parity under one contract timing mode</name>
  <files>crates/benchmarks/src/main.rs, crates/benchmarks/src/contract.rs</files>
  <action>Refactor engine adapters so all engines execute under the same contract timing mode for a run (selected in 10-01 contract). Remove current qjs-rs vs external-engine timing mismatch by routing all adapters through a shared policy branch. Normalize checksum/guard extraction so each engine reports value-based parity data instead of placeholder counters.</action>
  <verify>
    <automated>cargo test -p benchmarks adapter_timing_mode_is_uniform -- --exact && cargo test -p benchmarks adapter_checksum_parity_is_value_based -- --exact</automated>
  </verify>
  <done>Benchmark adapters cannot silently run mixed timing semantics and all engines contribute comparable guard/checksum evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Add comparator preflight and configurable command/path controls</name>
  <files>crates/benchmarks/src/main.rs, crates/benchmarks/src/contract.rs, docs/benchmark-contract.md</files>
  <action>Add CLI/env-backed comparator controls (at minimum node and quickjs command/path/workdir) with clear precedence rules. Implement preflight checks before benchmark execution to capture version/availability/path metadata and fail fast for required comparators in strict profiles. Persist preflight results in artifact metadata so runs are reproducible and diagnosable.</action>
  <verify>
    <automated>cargo run -p benchmarks -- --help | rg --line-number "node|quickjs|profile" && cargo test -p benchmarks comparator_preflight_metadata_is_complete -- --exact</automated>
  </verify>
  <done>Comparator dependencies are explicit, validated up-front, and recorded in output artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Add adapter-normalization regression tests without external-process dependency</name>
  <files>crates/benchmarks/tests/adapter_normalization.rs, crates/benchmarks/src/main.rs</files>
  <action>Create deterministic tests that exercise contract/policy helpers (timing mode selection, command resolution, metadata completeness, checksum normalization) using fixture/stub data rather than invoking live Node/QuickJS binaries. Ensure tests explicitly guard against prior drift patterns identified in 10-RESEARCH.</action>
  <verify>
    <automated>cargo test -p benchmarks adapter_normalization -- --nocapture</automated>
  </verify>
  <done>Adapter parity and reproducibility policy regressions are caught in fast automated tests.</done>
</task>

</tasks>

<verification>
Run the targeted adapter-normalization tests and CLI/help checks; acceptance requires uniform timing contract enforcement, preflight metadata serialization, and deterministic regression coverage.
</verification>

<success_criteria>
Benchmark outputs are generated from parity-aligned adapter semantics with explicit comparator configuration and reproducibility metadata sufficient for Phase 11 decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/10-baseline-contract-and-benchmark-normalization/10-02-SUMMARY.md`.
</output>
