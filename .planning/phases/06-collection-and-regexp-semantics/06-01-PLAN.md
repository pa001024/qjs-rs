---
phase: 06-collection-and-regexp-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/runtime/src/lib.rs
  - crates/builtins/src/lib.rs
  - crates/vm/src/lib.rs
  - crates/test-harness/tests/collection_semantics.rs
autonomous: true
requirements:
  - BUI-04
must_haves:
  truths:
    - `WeakMap` and `WeakSet` are dedicated constructors/prototypes, not aliases of `Map` and `Set`.
    - `Map/Set` preserve SameValueZero key semantics and live iteration behavior under in-loop mutations.
    - `WeakMap/WeakSet` reject non-object keys with deterministic `TypeError` behavior in constructor and method paths.
  artifacts:
    - crates/runtime/src/lib.rs
    - crates/builtins/src/lib.rs
    - crates/vm/src/lib.rs
    - crates/test-harness/tests/collection_semantics.rs
  key_links:
    - `builtins::install_baseline` registers dedicated weak constructors that dispatch through new runtime `NativeFunction` variants.
    - VM collection host-function dispatch enforces weak-key object constraints and constructor iterable fail-fast behavior.
    - Harness integration tests execute script-level Map/Set/WeakMap/WeakSet scenarios against runtime semantics, not fixture stubs.
---

<objective>
Close BUI-04 by removing weak-collection constructor alias shortcuts and locking dedicated collection semantics.

Purpose: Ensure collection behavior is runtime-semantic and deterministic rather than shape-compatible by alias.
Output: Dedicated weak constructors/brands in runtime + builtins + VM, plus regression coverage for Map/Set/WeakMap/WeakSet baseline behavior.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-collection-and-regexp-semantics/06-CONTEXT.md
@.planning/phases/06-collection-and-regexp-semantics/06-RESEARCH.md
@.planning/phases/05-core-builtins-baseline/05-03-SUMMARY.md
@crates/runtime/src/lib.rs
@crates/builtins/src/lib.rs
@crates/vm/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split WeakMap/WeakSet constructor identity from Map/Set alias paths</name>
  <files>crates/runtime/src/lib.rs, crates/builtins/src/lib.rs, crates/vm/src/lib.rs</files>
  <action>Add dedicated `NativeFunction` variants and constructor dispatch paths for `WeakMap` and `WeakSet`, then update builtin installation so globals no longer point to `MapConstructor`/`SetConstructor`. Preserve existing constructor/prototype link invariants while creating weak-specific constructor/prototype identities.</action>
  <verify>
    <automated>cargo test -p vm weak_collection_constructor_identity -- --exact</automated>
  </verify>
  <done>`WeakMap`/`WeakSet` globals, constructor names, and prototype links are dedicated and no longer aliases of `Map`/`Set`.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce weak-key constraints and lock Map/Set baseline semantics</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Implement strict object-key validation for weak collections across constructor iterable ingestion and method calls (`get/set/has/delete` and `add/has/delete`). Keep Map/Set behavior aligned with locked decisions: SameValueZero key equality, insertion-order stability (`set` existing key keeps position, delete then re-set appends), and live iteration under mutation. Ensure invalid iterable entries fail fast without partial continuation beyond first invalid element.</action>
  <verify>
    <automated>cargo test -p vm collection_semantics_same_value_zero_and_live_iteration -- --exact</automated>
  </verify>
  <done>Weak collections deterministically reject invalid keys/entries, and Map/Set baseline ordering/equality/iteration semantics are regression-locked.</done>
</task>

<task type="auto">
  <name>Task 3: Add harness integration coverage for collection baseline and error edges</name>
  <files>crates/test-harness/tests/collection_semantics.rs</files>
  <action>Create a focused integration suite covering constructor identity, SameValueZero behavior (`NaN`, signed zero), live mutation iteration, weak non-object-key `TypeError` cases, and weak iterable invalid-entry fail-fast behavior. Include at least one positive and one boundary/error assertion for each collection family.</action>
  <verify>
    <automated>cargo test -p test-harness --test collection_semantics</automated>
  </verify>
  <done>Collection semantics are validated at script-execution level with deterministic positive and boundary/error coverage.</done>
</task>

</tasks>

<verification>
Run `cargo test -p vm weak_collection_constructor_identity -- --exact`, `cargo test -p vm collection_semantics_same_value_zero_and_live_iteration -- --exact`, and `cargo test -p test-harness --test collection_semantics`.
</verification>

<success_criteria>
BUI-04 is observably closed: weak collections are dedicated (non-aliased) and all four collection families have deterministic baseline semantics and regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/06-collection-and-regexp-semantics/06-01-SUMMARY.md`.
</output>

