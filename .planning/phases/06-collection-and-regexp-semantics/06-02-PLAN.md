---
phase: 06-collection-and-regexp-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - crates/vm/src/lib.rs
  - crates/test-harness/tests/regexp_semantics.rs
autonomous: true
requirements:
  - BUI-05
must_haves:
  truths:
    - `RegExp` constructor accepts supported flags (`g/i/m/s/u/y`) and rejects unsupported patterns/flags with deterministic `SyntaxError`.
    - `RegExp.prototype.exec` and `RegExp.prototype.test` share one match core and apply consistent `lastIndex` transitions.
    - `RegExp.prototype.toString` emits stable normalized `/source/flags` output while preserving observable constructor state.
  artifacts:
    - crates/vm/src/lib.rs
    - crates/test-harness/tests/regexp_semantics.rs
  key_links:
    - `RegExpTestThis` and `RegExpExecThis` dispatch both call a single match core plus shared lastIndex transition helper.
    - Exec-result array construction links match-core output (full match + captures) to `index`/`input` properties.
    - Harness integration assertions validate constructor + exec/test + toString behavior through script execution paths.
---

<objective>
Close BUI-05 by hardening RegExp constructor and prototype semantics around match behavior, flags, and lastIndex.

Purpose: Eliminate `exec/test` drift and deterministic-error gaps in current RegExp baseline.
Output: Shared RegExp matching state machine in VM plus focused runtime integration coverage.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-collection-and-regexp-semantics/06-CONTEXT.md
@.planning/phases/06-collection-and-regexp-semantics/06-RESEARCH.md
@.planning/phases/06-collection-and-regexp-semantics/06-01-SUMMARY.md
@crates/vm/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize RegExp match/lastIndex transitions for exec and test</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Refactor RegExp dispatch so `exec` and `test` share one internal match state helper with explicit pre/post `lastIndex` handling for default/global/sticky cases. Preserve writable checks and deterministic typed errors when `lastIndex` cannot be updated.</action>
  <verify>
    <automated>cargo test -p vm regexp_last_index_transition_matrix -- --exact</automated>
  </verify>
  <done>`exec` and `test` follow one consistent `lastIndex` transition contract across success/failure and flag combinations.</done>
</task>

<task type="auto">
  <name>Task 2: Complete RegExp constructor, exec result shape, and deterministic SyntaxError boundaries</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Ensure supported flags remain `g/i/m/s/u/y` only, and constructor rejects unsupported flags/patterns with deterministic `SyntaxError` category behavior. Extend `exec` return shape to include capture groups plus `index` and `input` properties, and keep `toString` normalization stable as `/source/flags`.</action>
  <verify>
    <automated>cargo test -p vm regexp_exec_capture_and_constructor_errors -- --exact</automated>
  </verify>
  <done>Constructor and prototype methods expose stable, supported-pattern behavior with deterministic success/error outcomes.</done>
</task>

<task type="auto">
  <name>Task 3: Add harness integration matrix for RegExp baseline semantics</name>
  <files>crates/test-harness/tests/regexp_semantics.rs</files>
  <action>Create integration tests covering constructor cloning, flag preservation, `exec/test` consistency, `lastIndex` transitions (including sticky/global failure reset paths), capture-slot materialization, and unsupported-flag/pattern `SyntaxError` cases.</action>
  <verify>
    <automated>cargo test -p test-harness --test regexp_semantics</automated>
  </verify>
  <done>RegExp behavior is locked with script-level positive and boundary/error assertions tied directly to phase acceptance.</done>
</task>

</tasks>

<verification>
Run `cargo test -p vm regexp_last_index_transition_matrix -- --exact`, `cargo test -p vm regexp_exec_capture_and_constructor_errors -- --exact`, and `cargo test -p test-harness --test regexp_semantics`.
</verification>

<success_criteria>
BUI-05 is observably closed: supported RegExp constructor/prototype behavior is deterministic, shared-core, and regression-protected.
</success_criteria>

<output>
After completion, create `.planning/phases/06-collection-and-regexp-semantics/06-02-SUMMARY.md`.
</output>

