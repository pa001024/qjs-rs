---
phase: 03-promise-job-queue-semantics
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - crates/vm/src/lib.rs
  - crates/test-harness/tests/promise_job_queue.rs
autonomous: true
requirements:
  - ASY-01
  - ASY-02
must_haves:
  truths:
    - Promise handler throws/rejections propagate through queued chains with deterministic rejection behavior.
    - Enqueue + drain via host callbacks produce reproducible processed/remaining outcomes, deterministic callback invocation order/count, and stable failure categories.
    - Queued Promise captures remain rooted through GC until execution and are releasable after drain completes.
  artifacts:
    - crates/vm/src/lib.rs
    - crates/test-harness/tests/promise_job_queue.rs
  key_links:
    - Host callback enqueue + drain contract from `03-01` is exercised by harness-level Promise chain scenarios implemented in this plan.
    - Promise propagation semantics from `03-02` are verified under enqueue + drain via host callbacks control and nested queue growth.
    - Pending-job root candidate retention/release is asserted in queue execution and GC stress tests.
---

<objective>
Close Phase 3 by proving queue contract, exception propagation, and GC safety together under enqueue + drain via host callbacks control.

Purpose: Turn ASY-01 and ASY-02 into end-to-end observable guarantees, not only unit-level behavior.
Output: VM propagation/GC hardening tests and test-harness integration coverage for enqueue + drain via host callbacks control.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-promise-job-queue-semantics/03-CONTEXT.md
@.planning/phases/03-promise-job-queue-semantics/03-RESEARCH.md
@.planning/phases/03-promise-job-queue-semantics/03-01-SUMMARY.md
@.planning/phases/03-promise-job-queue-semantics/03-02-SUMMARY.md
@crates/vm/src/lib.rs
@crates/test-harness/tests/test262_lite.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add callback-driven Promise job queue integration suite in test-harness</name>
  <files>crates/test-harness/tests/promise_job_queue.rs</files>
  <action>Create a dedicated integration test file that drives VM Promise execution with explicit host callback enqueue + drain flows. Add callback-focused cases that assert `on_enqueue -> on_drain_start -> on_drain_end` invocation order and counts, plus enqueue-trigger path coverage where chained handlers enqueue nested jobs during drain. Keep assertions user-observable (result values/order tokens and callback trace), not internal-only.</action>
  <verify>
    <automated>cargo test -p test-harness --test promise_job_queue</automated>
  </verify>
  <done>Harness tests reproduce deterministic callback-driven enqueue/drain outcomes, including callback order/count and nested enqueue-trigger paths.</done>
</task>

<task type="auto">
  <name>Task 2: Harden queued exception propagation and drain-loop failure policy</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Add/adjust VM queue execution logic so handler-throw and rejection propagation remain deterministic in chained jobs, while drain loop continues through normal Promise rejections and only aborts on VM-infrastructure failures. Add callback-contract failure cases for reentrant drain and invalid callback interactions, and lock deterministic error category/message-shape expectations in unit tests.</action>
  <verify>
    <automated>cargo test -p vm promise_queue_exception_propagation -- --exact</automated>
  </verify>
  <done>Promise queue propagation and callback failure behavior are stable, regression-tested, and aligned with defined stop/continue and deterministic-failure policy.</done>
</task>

<task type="auto">
  <name>Task 3: Verify GC root integrity for queued captures under stress drain</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Add GC-focused VM tests that enqueue jobs capturing object values, run collections before and during drain, and assert capture objects survive while queued and can be reclaimed after job completion/release. Reuse existing pending-job root candidate machinery and keep checks deterministic across repeated loops.</action>
  <verify>
    <automated>cargo test -p vm promise_queue_gc_root_integrity -- --exact</automated>
  </verify>
  <done>Queued Promise captures are proven GC-safe during pending state and reclaimable after deterministic release points.</done>
</task>

</tasks>

<verification>
Run `cargo test -p vm` and `cargo test -p test-harness --test promise_job_queue`. Confirm deterministic ordering/propagation, callback invocation order and counts, enqueue-trigger callback path during nested scheduling, deterministic failure on reentrant or invalid callback interactions, and queued-capture GC safety.
</verification>

<success_criteria>
Phase 3 closes with end-to-end guarantees for both ASY-01 and ASY-02: deterministic Promise queue semantics, safe enqueue + drain via host callbacks control, and reproducible exception/GC behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03-promise-job-queue-semantics/03-03-SUMMARY.md`.
</output>
