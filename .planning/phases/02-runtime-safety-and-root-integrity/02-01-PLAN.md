---
phase: 02-runtime-safety-and-root-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/vm/src/lib.rs
autonomous: true
requirements:
  - MEM-01
must_haves:
  truths:
    - GC root scanning always includes stack/state roots, realm globals, module-cache candidate roots, and pending job-queue candidate roots.
    - Values reachable only through module/job candidate root buckets survive collection until those buckets release them.
    - Root collection remains deterministic under both boundary GC and runtime GC paths.
  artifacts:
    - crates/vm/src/lib.rs
  key_links:
    - Vm-owned module and job candidate containers feed directly into collect_roots.
    - execute_in_realm resets candidate root containers so root state cannot leak across runs.
    - collect_garbage and maybe_collect_runtime always mark from the same root source.
---

<objective>
Close the MEM-01 root-coverage gap by making module-cache and pending job references first-class GC roots in VM root collection.

Purpose: Eliminate root omission risk before module/job features expand in later phases.
Output: VM root-collection wiring and focused root-coverage regressions in `crates/vm/src/lib.rs`.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-runtime-safety-and-root-integrity/02-CONTEXT.md
@.planning/phases/02-runtime-safety-and-root-integrity/02-RESEARCH.md
@docs/root-strategy.md
@docs/gc-design.md
@crates/vm/src/lib.rs

<interfaces>
From crates/vm/src/lib.rs:
```rust
fn collect_roots(&self, realm: &Realm) -> Vec<JsValue>;
fn collect_garbage(&mut self, realm: &Realm) -> GcStats;
fn maybe_collect_runtime(&mut self, realm: &Realm);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit module/job root candidate buckets to VM state</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Add VM-owned containers for module-cache root candidates and pending job-queue root candidates, add small internal registration/clear helpers, and reset these containers in `execute_in_realm`. Keep the API internal to VM for this phase; do not add host-facing async/module APIs yet.</action>
  <verify>
    <automated>cargo test -p vm</automated>
  </verify>
  <done>VM state can store module/job candidate roots and run lifecycle reset without compile or test regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire candidate buckets into root traversal and lock with regression tests</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Extend `collect_roots` so module-cache and pending-job candidate buckets are always appended to the root snapshot, then add VM unit tests that prove objects reachable only from these buckets survive GC and are reclaimed once candidates are removed. Include repeated allocation/collection loops to ensure no stale-handle panics are introduced by the new root paths.</action>
  <verify>
    <automated>cargo test -p vm</automated>
  </verify>
  <done>GC tests demonstrate deterministic survival/reclamation behavior for module/job candidate roots and preserve existing VM GC coverage.</done>
</task>

</tasks>

<verification>
Run `cargo test -p vm`. Confirm no root-category omissions remain in `collect_roots` for the Phase 2 required categories (stack frames, globals, module candidates, pending job candidates).
</verification>

<success_criteria>
MEM-01 root coverage is observable in code and tests: module/job candidate references are treated as first-class roots and collections remain deterministic.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-safety-and-root-integrity/02-01-SUMMARY.md`.
</output>
