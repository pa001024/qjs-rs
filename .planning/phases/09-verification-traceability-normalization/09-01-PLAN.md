---
phase: 09-verification-traceability-normalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/verification-schema.md
  - .planning/phases/01-semantic-core-closure/01-VERIFICATION.md
  - .planning/phases/02-runtime-safety-and-root-integrity/02-VERIFICATION.md
  - .planning/phases/03-promise-job-queue-semantics/03-VERIFICATION.md
  - .planning/phases/04-es-module-lifecycle/04-VERIFICATION.md
  - .planning/phases/05-core-builtins-baseline/05-VERIFICATION.md
  - .planning/phases/06-collection-and-regexp-semantics/06-VERIFICATION.md
  - .planning/phases/07-compatibility-and-governance-gates/07-VERIFICATION.md
  - .planning/phases/08-async-and-module-builtins-integration-closure/08-VERIFICATION.md
  - .planning/phases/09-verification-traceability-normalization/09-VERIFICATION-SCHEMA-MIGRATION.md
autonomous: true
requirements:
  - None (audit integration debt closure)
must_haves:
  truths:
    - Every phase verification artifact (01-08) exposes one canonical frontmatter contract with stable machine fields.
    - Frontmatter schema drift (`verified_at`, missing frontmatter, missing `requirements_checked`) is eliminated without rewriting historical evidence bodies.
    - Requirement mapping policy is explicit and deterministic: `requirements_checked` is sourced from `.planning/REQUIREMENTS.md` traceability mapping.
  artifacts:
    - .planning/verification-schema.md
    - .planning/phases/01-semantic-core-closure/01-VERIFICATION.md
    - .planning/phases/02-runtime-safety-and-root-integrity/02-VERIFICATION.md
    - .planning/phases/03-promise-job-queue-semantics/03-VERIFICATION.md
    - .planning/phases/04-es-module-lifecycle/04-VERIFICATION.md
    - .planning/phases/05-core-builtins-baseline/05-VERIFICATION.md
    - .planning/phases/06-collection-and-regexp-semantics/06-VERIFICATION.md
    - .planning/phases/07-compatibility-and-governance-gates/07-VERIFICATION.md
    - .planning/phases/08-async-and-module-builtins-integration-closure/08-VERIFICATION.md
    - .planning/phases/09-verification-traceability-normalization/09-VERIFICATION-SCHEMA-MIGRATION.md
  key_links:
    - `.planning/verification-schema.md` defines required keys consumed by all `*-VERIFICATION.md` frontmatter blocks.
    - Migrated verification frontmatter aligns each phase file to canonical keys (`phase`, `phase_number`, `verified`, `status`, `score`, `requirements_checked`).
    - Migration report records per-file field normalization so tooling changes in 09-02 can validate against a known artifact contract.
---

<objective>
Standardize verification artifact schema across historical phases so requirement traceability can be machine-evaluated without manual fallback.

Purpose: close the schema drift debt called out by the milestone audit before wiring stricter automation gates.
Output: canonical verification schema contract + normalized Phase 01-08 verification frontmatter + migration evidence report.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/09-verification-traceability-normalization/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write canonical verification frontmatter contract and mapping policy</name>
  <files>.planning/verification-schema.md</files>
  <action>Create a single source-of-truth schema document for phase verification artifacts. Define required frontmatter keys (`phase`, `phase_number`, `verified`, `status`, `score`, `requirements_checked`), value formats (phase number two-digit string, ISO-8601 timestamp), and a strict mapping rule that `requirements_checked` must be derived from `.planning/REQUIREMENTS.md` traceability table (not free-form body text or ROADMAP duplicates). Include explicit handling for phases with no mapped requirement IDs (`requirements_checked: []`).</action>
  <verify>
    <automated>rg --line-number "phase_number|requirements_checked|REQUIREMENTS.md|requirements_checked: \\[]" .planning/verification-schema.md</automated>
  </verify>
  <done>Schema contract exists, is machine-oriented, and unambiguously defines both field shape and requirement ownership policy.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize structural frontmatter fields in Phase 01-08 verification artifacts</name>
  <files>.planning/phases/01-semantic-core-closure/01-VERIFICATION.md, .planning/phases/02-runtime-safety-and-root-integrity/02-VERIFICATION.md, .planning/phases/03-promise-job-queue-semantics/03-VERIFICATION.md, .planning/phases/04-es-module-lifecycle/04-VERIFICATION.md, .planning/phases/05-core-builtins-baseline/05-VERIFICATION.md, .planning/phases/06-collection-and-regexp-semantics/06-VERIFICATION.md, .planning/phases/07-compatibility-and-governance-gates/07-VERIFICATION.md, .planning/phases/08-async-and-module-builtins-integration-closure/08-VERIFICATION.md</files>
  <action>For every existing verification file, ensure a top-level YAML frontmatter block exists and normalize key names to canonical schema (`verified_at` -> `verified`, remove legacy schema-only keys that are no longer canonical machine contract). Preserve historical narrative/evidence bodies and only touch body text where needed to keep consistency with renamed frontmatter fields. Keep `status`/`score` semantics unchanged while making all files parseable by one schema.</action>
  <verify>
    <automated>node C:/Users/Administrator/.codex/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/phases/01-semantic-core-closure/01-VERIFICATION.md --schema verification && node C:/Users/Administrator/.codex/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/phases/02-runtime-safety-and-root-integrity/02-VERIFICATION.md --schema verification && node C:/Users/Administrator/.codex/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/phases/03-promise-job-queue-semantics/03-VERIFICATION.md --schema verification && node C:/Users/Administrator/.codex/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/phases/04-es-module-lifecycle/04-VERIFICATION.md --schema verification && node C:/Users/Administrator/.codex/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/phases/05-core-builtins-baseline/05-VERIFICATION.md --schema verification && node C:/Users/Administrator/.codex/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/phases/06-collection-and-regexp-semantics/06-VERIFICATION.md --schema verification && node C:/Users/Administrator/.codex/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/phases/07-compatibility-and-governance-gates/07-VERIFICATION.md --schema verification && node C:/Users/Administrator/.codex/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/phases/08-async-and-module-builtins-integration-closure/08-VERIFICATION.md --schema verification</automated>
  </verify>
  <done>All eight verification files pass one verification frontmatter schema validator invocation path with no file-specific manual fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Backfill deterministic requirement mappings and publish migration report</name>
  <files>.planning/phases/01-semantic-core-closure/01-VERIFICATION.md, .planning/phases/02-runtime-safety-and-root-integrity/02-VERIFICATION.md, .planning/phases/03-promise-job-queue-semantics/03-VERIFICATION.md, .planning/phases/04-es-module-lifecycle/04-VERIFICATION.md, .planning/phases/05-core-builtins-baseline/05-VERIFICATION.md, .planning/phases/06-collection-and-regexp-semantics/06-VERIFICATION.md, .planning/phases/07-compatibility-and-governance-gates/07-VERIFICATION.md, .planning/phases/08-async-and-module-builtins-integration-closure/08-VERIFICATION.md, .planning/phases/09-verification-traceability-normalization/09-VERIFICATION-SCHEMA-MIGRATION.md</files>
  <action>Populate `requirements_checked` in each phase verification frontmatter using `.planning/REQUIREMENTS.md` traceability ownership as canonical mapping source. Ensure ASY ownership reflects the current canonical mapping policy documented in Task 1. Add `09-VERIFICATION-SCHEMA-MIGRATION.md` capturing per-file before/after key changes and the final requirement mapping matrix used for automation.</action>
  <verify>
    <automated>python -c "from pathlib import Path; import re, sys; req_ids={m.group(1) for m in re.finditer(r'^\\|\\s*([A-Z]{3}-\\d{2})\\s*\\|', Path('.planning/REQUIREMENTS.md').read_text(encoding='utf-8'), re.MULTILINE)}; verifs=sorted(Path('.planning/phases').glob('*/*-VERIFICATION.md')); mapped=set(); missing_field=[]; legacy=[]; missing_ids=set(req_ids); pattern=re.compile(r'^\\s*-\\s*([A-Z]{3}-\\d{2})\\s*$', re.MULTILINE); frontmatter_re=re.compile(r'^---\\n(.*?)\\n---', re.DOTALL); \nfor vf in verifs:\n t=vf.read_text(encoding='utf-8'); m=frontmatter_re.match(t);\n if not m: missing_field.append(str(vf)); continue\n fm=m.group(1)\n if 'requirements_checked:' not in fm: missing_field.append(str(vf))\n if 'verified_at:' in fm: legacy.append(str(vf))\n mapped.update(pattern.findall(fm))\nmissing_ids-=mapped\nif missing_field or legacy or missing_ids:\n print('missing_field:', missing_field); print('legacy_verified_at:', legacy); print('missing_requirement_ids:', sorted(missing_ids)); sys.exit(1)\nprint('verification mapping backfill ok')"</automated>
  </verify>
  <done>Every requirement ID from `.planning/REQUIREMENTS.md` appears in normalized verification frontmatter, no legacy timestamp key remains, and migration evidence is documented.</done>
</task>

</tasks>

<verification>
Acceptance requires: canonical schema doc present, all Phase 01-08 verification files frontmatter-validated by the same validator command family, and deterministic requirement coverage extraction from frontmatter-only fields without body parsing.
</verification>

<success_criteria>
Phase verification artifacts are structurally normalized and ready for strict automated traceability checks in the next plan without special-case handling.
</success_criteria>

<output>
After completion, create `.planning/phases/09-verification-traceability-normalization/09-01-SUMMARY.md`.
</output>

