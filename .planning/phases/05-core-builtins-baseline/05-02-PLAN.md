---
phase: 05-core-builtins-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - crates/vm/src/lib.rs
  - crates/test-harness/tests/json_interop.rs
  - crates/test-harness/tests/test262_lite.rs
autonomous: true
requirements:
  - BUI-03
must_haves:
  truths:
    - `JSON.parse` accepts baseline nested JSON and applies `reviver` walk semantics deterministically.
    - Malformed JSON input throws deterministic `SyntaxError`-category failures rather than placeholder values.
    - `JSON.stringify` supports replacer/space behavior and throws deterministic `TypeError` on cycles.
  artifacts:
    - crates/vm/src/lib.rs
    - crates/test-harness/tests/json_interop.rs
    - crates/test-harness/tests/test262_lite.rs
  key_links:
    - Parse output conversion feeds a reviver post-walk path inside VM JSON call dispatch.
    - Stringify traversal path shares deterministic object/array property-order guarantees already enforced by runtime object iteration rules.
    - Harness JSON suites execute both direct runtime checks and test262-lite subset gates for malformed/cycle paths.
---

<objective>
Close BUI-03 by replacing placeholder JSON behavior with deterministic parse/stringify semantics and targeted conformance gates.

Purpose: Provide reliable JSON interop needed by harness and integration behaviors.
Output: VM JSON parse/stringify algorithms with reviver/replacer/space/cycle handling and dedicated regressions.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-core-builtins-baseline/05-CONTEXT.md
@.planning/phases/05-core-builtins-baseline/05-RESEARCH.md
@.planning/phases/05-core-builtins-baseline/05-01-SUMMARY.md
@crates/vm/src/lib.rs
@crates/test-harness/tests/test262_lite.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement baseline JSON.parse with deterministic SyntaxError and reviver walk</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Replace placeholder parse logic with full baseline JSON value support (object/array/string/number/boolean/null). Map malformed input to deterministic `SyntaxError`-category exceptions and implement post-parse holder/key recursive reviver traversal that can transform/delete properties according to baseline semantics.</action>
  <verify>
    <automated>cargo test -p vm json_parse_reviver_semantics -- --exact</automated>
  </verify>
  <done>Parse supports baseline JSON grammar with deterministic malformed-input failure category and reviver recursion behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement baseline JSON.stringify with replacer/space and cycle TypeError</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Implement stringify traversal for nested objects/arrays with stable output ordering under current engine guarantees. Support function/array `replacer`, numeric/string `space` clamp behavior, filtering of unsupported values in object/array positions, and deterministic cycle detection returning `TypeError`-category exceptions.</action>
  <verify>
    <automated>cargo test -p vm json_stringify_replacer_space_cycle -- --exact</automated>
  </verify>
  <done>Stringify produces deterministic baseline output and rejects cyclic structures with deterministic typed errors.</done>
</task>

<task type="auto">
  <name>Task 3: Add JSON interoperability regression suite and lite conformance subset gate</name>
  <files>crates/test-harness/tests/json_interop.rs, crates/test-harness/tests/test262_lite.rs</files>
  <action>Create deterministic harness tests covering positive nested parse/stringify, reviver/replacer/space behavior, malformed parse inputs, and cycle errors. Extend test262-lite phase coverage with a stable `built-ins/JSON` subset command profile that can serve as CI contract for this phase.</action>
  <verify>
    <automated>cargo test -p test-harness --test json_interop</automated>
  </verify>
  <done>JSON behavior is validated by both direct harness regressions and a repeatable test262-lite subset profile.</done>
</task>

</tasks>

<verification>
Run `cargo test -p vm json_parse_reviver_semantics -- --exact`, `cargo test -p vm json_stringify_replacer_space_cycle -- --exact`, and `cargo test -p test-harness --test json_interop` to verify BUI-03 closure.
</verification>

<success_criteria>
BUI-03 is closed for Phase 5 baseline: parse/stringify/reviver/replacer/space/cycle behavior is deterministic and enforced in runtime and harness tests.
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-builtins-baseline/05-02-SUMMARY.md`.
</output>
