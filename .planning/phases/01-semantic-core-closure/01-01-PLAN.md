---
phase: 01-semantic-core-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/vm/src/lib.rs
  - crates/test-harness/tests/semantics_eval_scope.rs
autonomous: true
requirements:
  - SEM-01
  - SEM-02
must_haves:
  truths:
    - Direct eval preserves caller lexical visibility and strict-mode behavior defined by target semantics.
    - Indirect eval resolves through global scope only and never captures caller lexical bindings.
    - Eval-related failures preserve deterministic semantic error categories and strict/non-strict this binding behavior.
    - Nested closures and block/function boundaries preserve lexical capture, shadowing, and TDZ behavior under control flow.
  artifacts:
    - crates/vm/src/lib.rs
    - crates/test-harness/tests/semantics_eval_scope.rs
  key_links:
    - Bytecode eval call forms dispatch to VM direct/indirect eval paths without heuristic fallback.
    - Eval execution restores scopes, var scope stack, and with-object stack after completion/error.
    - Identifier reference load/store paths remain the single lexical resolution flow for eval + closure interactions.
---

<objective>
Close SEM-01 and SEM-02 by hardening eval dispatch/scope behavior and lexical environment correctness against a focused regression matrix.

Purpose: Ensure eval and lexical binding semantics are spec-aligned and deterministic before downstream runtime phases.
Output: One targeted semantic regression suite plus VM-level fixes confined to eval/scope reference choke points.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-semantic-core-closure/01-CONTEXT.md
@.planning/phases/01-semantic-core-closure/01-RESEARCH.md
@crates/vm/src/lib.rs
@crates/test-harness/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add eval and lexical regression matrix</name>
  <files>crates/test-harness/tests/semantics_eval_scope.rs</files>
  <action>Create a new integration test suite covering direct vs indirect eval scope visibility, strict vs non-strict eval behavior, eval error-category preservation, and nested closure/block shadowing interactions. Keep tests script-driven through the existing harness API and isolate each assertion to a single observable semantic truth.</action>
  <verify>
    <automated>cargo test -p test-harness --test semantics_eval_scope</automated>
  </verify>
  <done>Regression suite exists and exercises all SEM-01/SEM-02 edge categories listed in this task with deterministic assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Harden VM eval and lexical reference paths</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Patch only the centralized eval/identifier-resolution paths so direct eval uses caller lexical semantics, indirect eval stays global-only, strict-mode injection remains intact, and scope/with-stack restoration is deterministic on both normal and exceptional exits. Preserve existing semantic error categories (SyntaxError/ReferenceError/TypeError precedence) and do not introduce non-spec convenience behavior.</action>
  <verify>
    <automated>cargo test -p vm</automated>
    <automated>cargo test -p test-harness --test semantics_eval_scope</automated>
  </verify>
  <done>All new eval/scope tests pass and no VM test regressions appear in the touched semantic paths.</done>
</task>

</tasks>

<verification>
Run `cargo test -p vm` and `cargo test -p test-harness --test semantics_eval_scope`. If failures expose strict/eval interaction gaps, keep fixes constrained to eval dispatch and lexical reference flow in `crates/vm/src/lib.rs`.
</verification>

<success_criteria>
Direct and indirect eval behavior, strict-mode effects, and lexical capture/shadowing outcomes are deterministic and match the SEM-01/SEM-02 requirement truths in automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-semantic-core-closure/01-01-SUMMARY.md`.
</output>
