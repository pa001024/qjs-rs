---
phase: 07-compatibility-and-governance-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/parser/src/lib.rs
  - crates/vm/src/lib.rs
  - crates/test-harness/tests/test262_lite.rs
  - .github/workflows/ci.yml
  - .github/PULL_REQUEST_TEMPLATE.md
  - .github/governance/exceptions.json
  - .github/governance/README.md
  - .github/governance/fixtures/pr_event_runtime_change.json
  - .github/governance/fixtures/pr_event_refactor_only.json
  - .github/scripts/validate_governance.py
autonomous: true
requirements:
  - TST-01
  - TST-03
must_haves:
  truths:
    - Known failing default-branch gates are restored to green first (fmt drift, parser clippy failure, and stress-suite regression), then locked as hard-blocking merge gates.
    - Default-branch CI enforces `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo test --workspace` as hard blockers with no silent bypass path.
    - Governance exceptions are explicit records with required metadata and expiration dates, and expired records are rejected automatically.
    - Runtime-observable PRs cannot pass governance checks without one positive and one boundary/error test reference whose referenced test files exist (or a refactor-only exception record with no-semantic-change evidence).
  artifacts:
    - crates/parser/src/lib.rs
    - crates/vm/src/lib.rs
    - crates/test-harness/tests/test262_lite.rs
    - .github/workflows/ci.yml
    - .github/PULL_REQUEST_TEMPLATE.md
    - .github/governance/exceptions.json
    - .github/governance/README.md
    - .github/governance/fixtures/pr_event_runtime_change.json
    - .github/governance/fixtures/pr_event_refactor_only.json
    - .github/scripts/validate_governance.py
  key_links:
    - TST-01 remediation task updates the exact files implicated by current gate failures before governance wiring is considered complete.
    - CI `pull_request` jobs pass `GITHUB_EVENT_PATH` into governance validation so enforcement reads the actual PR payload/body, not template-only samples.
    - Validator checks PR checklist fields and resolves declared `1 + 1` test references against repository files, then CI runs fmt/clippy/test chain as acceptance.
---

<objective>
Establish hard-blocking CI governance gates by first restoring known red gates to green, then enforcing PR payload-level `1 + 1` test compliance.

Purpose: Close TST-01 and TST-03 with deterministic policy-as-code and live PR-event enforcement instead of reviewer convention.
Output: Remediation for known red gates, CI gate wiring, governance exception contract, PR checklist contract, PR-event validator, and fixture-backed validator tests.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-compatibility-and-governance-gates/07-CONTEXT.md
@.planning/phases/07-compatibility-and-governance-gates/07-RESEARCH.md
@.github/workflows/ci.yml
@docs/current-status.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore current red quality gates to green in known failing files</name>
  <files>crates/parser/src/lib.rs, crates/vm/src/lib.rs, crates/test-harness/tests/test262_lite.rs</files>
  <action>Remediate the currently known TST-01 failures captured in Phase 7 research: format drift in `crates/test-harness/tests/test262_lite.rs` and `crates/vm/src/lib.rs`, parser clippy failure (`type_complexity`) in `crates/parser/src/lib.rs` without adding blanket `allow` attributes, and stress-profile regression(s) surfaced by `runs_test262_lite_suite_in_stress_profile`. Keep fixes semantic and reproducible, not by weakening assertions or skipping failing coverage.</action>
  <verify>
    <automated>cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test -p test-harness --test test262_lite runs_test262_lite_suite_in_stress_profile -- --exact</automated>
  </verify>
  <done>Known red fmt/clippy/stress-test signals are repaired and reproducibly green before governance wiring is finalized.</done>
</task>

<task type="auto">
  <name>Task 2: Implement exception lifecycle + PR payload validator modes with reference-existence checks</name>
  <files>.github/PULL_REQUEST_TEMPLATE.md, .github/governance/exceptions.json, .github/governance/README.md, .github/governance/fixtures/pr_event_runtime_change.json, .github/governance/fixtures/pr_event_refactor_only.json, .github/scripts/validate_governance.py</files>
  <action>Define/validate exception schema (`reason`, `impact_scope`, `owner`, `expires_at`, `rollback_condition`) with expiry rejection. Extend governance validator to support PR-level enforcement modes: `--validate-pr-event &lt;path&gt;` for real GitHub pull_request payloads, payload/body checklist parsing for runtime-observable declaration + `1 + 1` references, and `--require-test-reference-exists --repo-root &lt;repo&gt;` to fail when referenced tests are missing. Add fixture PR events for runtime-change and refactor-only cases so validator self-tests prove both valid and invalid paths deterministically.</action>
  <verify>
    <automated>python .github/scripts/validate_governance.py --exceptions .github/governance/exceptions.json --check-template .github/PULL_REQUEST_TEMPLATE.md --validate-pr-event .github/governance/fixtures/pr_event_runtime_change.json --repo-root . --require-test-reference-exists --self-test</automated>
  </verify>
  <done>Validator enforces exception lifecycle, real PR payload/body compliance fields, and repository-backed test-reference existence checks.</done>
</task>

<task type="auto">
  <name>Task 3: Wire pull_request payload validation and full hard-blocking gate chain in CI</name>
  <files>.github/workflows/ci.yml</files>
  <action>Update CI so governance validation runs before quality gates and, on `pull_request`, executes validator PR-event mode against `${GITHUB_EVENT_PATH}` with `--require-test-reference-exists --repo-root .`. Keep `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo test --workspace` as mandatory blockers after governance checks. Do not add bypass branches for invalid payloads, expired exceptions, or missing test references.</action>
  <verify>
    <automated>python .github/scripts/validate_governance.py --exceptions .github/governance/exceptions.json --check-template .github/PULL_REQUEST_TEMPLATE.md --validate-pr-event .github/governance/fixtures/pr_event_runtime_change.json --repo-root . --require-test-reference-exists && cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace</automated>
  </verify>
  <done>CI enforces PR payload-level governance + full quality gate chain as deterministic hard blockers for merge flow.</done>
</task>

</tasks>

<verification>
Run Task 1 remediation checks first, then run the full Task 3 governance + fmt/clippy/test chain as final acceptance; confirm invalid PR payloads, missing test references, and expired exceptions all exit non-zero.
</verification>

<success_criteria>
TST-01 and TST-03 are enforceable in automation: known red gates are repaired, quality gates remain mandatory, exception lifecycle is explicit/expiring, and runtime-change PRs are validated from actual pull_request payloads with `1 + 1` test-reference existence checks.
</success_criteria>

<output>
After completion, create `.planning/phases/07-compatibility-and-governance-gates/07-01-SUMMARY.md`.
</output>
