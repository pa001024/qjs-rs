---
phase: 07-compatibility-and-governance-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/test-harness/src/test262.rs
  - crates/test-harness/src/bin/test262-run.rs
  - crates/test-harness/tests/test262_lite.rs
  - docs/test262-baseline.md
autonomous: true
requirements:
  - TST-02
must_haves:
  truths:
    - test262 summaries expose `discovered`, `executed`, `failed`, and category-level skipped counters for every tracked run.
    - Runner outputs both JSON and Markdown reports from a single execution with matching totals and deterministic schema fields.
    - Skip categories are explicit and reproducible across fixture files, unsupported flags, includes, feature gates, and unsupported harness globals.
  artifacts:
    - crates/test-harness/src/test262.rs
    - crates/test-harness/src/bin/test262-run.rs
    - crates/test-harness/tests/test262_lite.rs
    - docs/test262-baseline.md
  key_links:
    - `run_suite` skip classification feeds one summary model used by both JSON and Markdown writers.
    - CLI report writers serialize the same counters, preventing drift between machine and human outputs.
    - Baseline docs record the exact command contract that generates report artifacts.
---

<objective>
Upgrade test262 reporting to Phase 7 schema requirements with explicit skip categories and dual report formats.

Purpose: Close TST-02 with a measurable, auditable compatibility reporting contract.
Output: Categorized skip accounting in harness summaries, JSON+Markdown report generation, and locked regression tests.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-compatibility-and-governance-gates/07-CONTEXT.md
@.planning/phases/07-compatibility-and-governance-gates/07-RESEARCH.md
@crates/test-harness/src/test262.rs
@crates/test-harness/src/bin/test262-run.rs
@crates/test-harness/tests/test262_lite.rs
@docs/test262-baseline.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit skip taxonomy and category counters in suite summaries</name>
  <files>crates/test-harness/src/test262.rs</files>
  <action>Replace boolean skip handling with explicit skip-reason classification and deterministic category counters. Cover at minimum: `fixture_file`, `flag_module`, `flag_only_strict`, `flag_async`, `requires_includes`, `requires_feature`, and `requires_harness_global_262`. Keep aggregate `skipped` total, and guarantee it equals the sum of category counters.</action>
  <verify>
    <automated>cargo test -p test-harness --lib</automated>
  </verify>
  <done>Harness summary data model includes required skip categories and deterministic aggregation invariants.</done>
</task>

<task type="auto">
  <name>Task 2: Emit fixed-schema JSON and Markdown reports from `test262-run`</name>
  <files>crates/test-harness/src/bin/test262-run.rs, docs/test262-baseline.md</files>
  <action>Add a `--markdown &lt;path&gt;` output option and update JSON serialization to include required summary fields plus `skipped_categories`. Ensure Markdown output contains totals and per-category skip rows derived from the same summary model used for JSON. Update baseline docs with the exact command contract and expected report files.</action>
  <verify>
    <automated>cargo run -p test-harness --bin test262-run -- --root crates/test-harness/fixtures/test262-lite --max-cases 50 --allow-failures --json target/test262-phase7-report.json --markdown target/test262-phase7-report.md && rg --line-number "\"discovered\"|\"executed\"|\"failed\"|\"skipped_categories\"" target/test262-phase7-report.json && rg --line-number "Skipped Categories|discovered|executed|failed" target/test262-phase7-report.md</automated>
  </verify>
  <done>Runner produces machine-readable JSON and human-readable Markdown reports with aligned metrics and skip-category breakdown.</done>
</task>

<task type="auto">
  <name>Task 3: Lock report-schema and skip-aggregation regressions with tests</name>
  <files>crates/test-harness/src/bin/test262-run.rs, crates/test-harness/tests/test262_lite.rs</files>
  <action>Add regression tests that assert required report keys exist, category sums match `skipped`, and Markdown headings/sections stay deterministic. Keep tests exact-name friendly where possible so plan verification commands always execute concrete targets.</action>
  <verify>
    <automated>cargo test -p test-harness --bin test262-run && cargo test -p test-harness --test test262_lite</automated>
  </verify>
  <done>Schema and aggregation regressions are guarded by automated tests and fail deterministically on contract drift.</done>
</task>

</tasks>

<verification>
Run Task 2 command to generate both report formats, then run Task 3 tests to prove schema and aggregation contracts are regression-locked.
</verification>

<success_criteria>
TST-02 is satisfied: every tracked test262 run yields deterministic discovered/executed/failed metrics plus explicit skip categories in both JSON and Markdown outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/07-compatibility-and-governance-gates/07-02-SUMMARY.md`.
</output>
