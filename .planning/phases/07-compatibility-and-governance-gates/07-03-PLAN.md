---
phase: 07-compatibility-and-governance-gates
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - crates/test-harness/src/bin/test262-run.rs
  - .github/scripts/run_compat_snapshot.py
  - .github/scripts/sync_current_status.py
  - .github/workflows/ci.yml
  - docs/compatibility/phase7-snapshots.json
  - docs/gc-snapshot-report.md
  - docs/current-status.md
  - docs/test262-baseline.md
autonomous: true
requirements:
  - MEM-03
  - TST-04
must_haves:
  truths:
    - Compatibility telemetry runs produce independent `baseline` and `stress` profile outputs with fixed threshold checks.
    - GC drift policy is two-tier (`warning` and `blocking`) and triggers mandatory investigation on consecutive anomalies.
    - Compatibility snapshots are archived with deterministic metadata and diffable manifest entries by phase/milestone run.
    - `docs/current-status.md` is generated/checked deterministically from snapshot manifest state, and CI fails when checked-in status drifts from manifest-derived output.
  artifacts:
    - crates/test-harness/src/bin/test262-run.rs
    - .github/scripts/run_compat_snapshot.py
    - .github/scripts/sync_current_status.py
    - .github/workflows/ci.yml
    - docs/compatibility/phase7-snapshots.json
    - docs/gc-snapshot-report.md
    - docs/current-status.md
    - docs/test262-baseline.md
  key_links:
    - Snapshot script executes baseline/stress `test262-run` commands and persists JSON+Markdown outputs per run id.
    - Manifest records include threshold status and anomaly streak derived from current and previous runs.
    - Status sync script consumes manifest entries to update/check `docs/current-status.md`, and CI check mode blocks drift.
    - CI runs snapshot + status check chain and fails on blocking breaches, consecutive anomalies, or status-document drift.
---

<objective>
Deliver reproducible compatibility snapshot governance with MEM-03 telemetry enforcement and TST-04 machine-checked status-sync discipline.

Purpose: Turn telemetry/reporting into a repeatable release-governance control plane.
Output: Profile-aware GC drift policy, snapshot manifest pipeline, deterministic `current-status` updater/checker, CI drift gate wiring, and manifest-backed documentation updates.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-compatibility-and-governance-gates/07-CONTEXT.md
@.planning/phases/07-compatibility-and-governance-gates/07-RESEARCH.md
@.planning/phases/07-compatibility-and-governance-gates/07-01-SUMMARY.md
@.planning/phases/07-compatibility-and-governance-gates/07-02-SUMMARY.md
@crates/test-harness/src/bin/test262-run.rs
@.github/workflows/ci.yml
@docs/gc-snapshot-report.md
@docs/current-status.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add profile-aware GC drift policy and anomaly classification in `test262-run`</name>
  <files>crates/test-harness/src/bin/test262-run.rs</files>
  <action>Extend runner output metadata so each tracked run is labeled as `baseline` or `stress`, and classify GC threshold outcomes into `ok`, `warning`, or `blocking`. Add support for comparing against prior run summaries to compute anomaly streaks and emit deterministic investigation-required status when anomalies are consecutive.</action>
  <verify>
    <automated>cargo test -p test-harness --bin test262-run</automated>
  </verify>
  <done>Runner exposes deterministic profile/drift status required for dual-track MEM-03 governance checks.</done>
</task>

<task type="auto">
  <name>Task 2: Implement reproducible snapshot pipeline and CI gate wiring</name>
  <files>.github/scripts/run_compat_snapshot.py, .github/workflows/ci.yml, docs/compatibility/phase7-snapshots.json</files>
  <action>Create a snapshot orchestrator that runs fixed baseline and stress commands, writes JSON+Markdown outputs under a deterministic run-id path, and appends a manifest entry containing phase, milestone, commit, command lines, summary totals, skip categories, GC metrics, drift status, anomaly streak, and stable timestamp metadata. Wire CI to execute this script and fail on blocking drift or consecutive anomalies.</action>
  <verify>
    <automated>python .github/scripts/run_compat_snapshot.py --phase 07 --milestone v1.0 --manifest docs/compatibility/phase7-snapshots.json --output-dir target/compatibility --allow-dirty && rg --line-number "\"baseline\"|\"stress\"|\"anomaly_streak\"|\"status\"" docs/compatibility/phase7-snapshots.json</automated>
  </verify>
  <done>Snapshot generation is reproducible, manifest-backed, and CI-enforced for MEM-03 regression governance.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic `current-status` updater/checker and enforce drift blocking in CI</name>
  <files>.github/scripts/sync_current_status.py, .github/workflows/ci.yml, docs/gc-snapshot-report.md, docs/current-status.md, docs/test262-baseline.md</files>
  <action>Implement a deterministic status sync tool with explicit modes: `write` regenerates `docs/current-status.md` from `docs/compatibility/phase7-snapshots.json`, and `check` recomputes expected content and exits non-zero when the checked-in status file is out of sync (printing a deterministic diff/hint). Update CI to run `check` mode after snapshot generation on pull requests and default-branch runs. Update docs to mandate this command contract for every major convergence update.</action>
  <verify>
    <automated>python .github/scripts/run_compat_snapshot.py --phase 07 --milestone v1.0 --manifest docs/compatibility/phase7-snapshots.json --output-dir target/compatibility --allow-dirty && python .github/scripts/sync_current_status.py --manifest docs/compatibility/phase7-snapshots.json --status-doc docs/current-status.md --mode check && rg --line-number "sync_current_status.py|mode check|phase7-snapshots.json|warning|blocking|consecutive anomal" docs/gc-snapshot-report.md docs/current-status.md docs/test262-baseline.md</automated>
  </verify>
  <done>`docs/current-status.md` synchronization becomes machine-checkable and CI-blocking, eliminating manual drift from manifest-backed status artifacts.</done>
</task>

</tasks>

<verification>
Run Task 2 snapshot generation, then run Task 3 `sync_current_status.py --mode check`; acceptance requires CI-equivalent commands to fail on status drift and on blocking/consecutive-anomaly telemetry states.
</verification>

<success_criteria>
MEM-03 and TST-04 are enforceable: baseline/stress telemetry governance is policy-driven with warning/blocking + anomaly escalation, compatibility snapshots are reproducible, and `docs/current-status.md` drift versus snapshot manifest is machine-checked and CI-blocking.
</success_criteria>

<output>
After completion, create `.planning/phases/07-compatibility-and-governance-gates/07-03-SUMMARY.md`.
</output>
