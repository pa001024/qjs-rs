---
phase: 11-hot-path-optimization-and-target-closure
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - crates/vm/src/fast_path.rs
  - crates/vm/src/lib.rs
  - crates/vm/tests/perf_packet_b.rs
  - crates/benchmarks/src/main.rs
  - docs/engine-benchmarks.md
  - .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
autonomous: true
requirements:
  - PERF-03
  - PERF-04
  - PERF-05
must_haves:
  truths:
    - A second guarded optimization packet accelerates array/property-heavy paths with preserved descriptor/prototype semantics.
    - Final Phase 11 candidate artifact proves `qjs-rs` aggregate mean is no worse than `boa-engine` under the locked closure profile.
    - Phase 11 outputs include maintainability-boundary evidence (pure Rust runtime core, explicit fallback paths, governance gates passing).
  artifacts:
    - crates/vm/src/fast_path.rs
    - crates/vm/src/lib.rs
    - crates/vm/tests/perf_packet_b.rs
    - crates/benchmarks/src/main.rs
    - docs/engine-benchmarks.md
    - .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
  key_links:
    - `crates/vm/src/lib.rs` array/property dispatch routes through guarded dense-index helpers with canonical fallback on non-dense/prototype/accessor cases.
    - `crates/vm/tests/perf_packet_b.rs` validates optimization-on/off parity for array-heavy semantics and edge cases.
    - `11-TARGET-CLOSURE-EVIDENCE.md` captures baseline -> packet-A -> packet-B deltas plus checker outputs proving PERF-03 closure and PERF-05 constraints.
---

<objective>
Implement Optimization Packet B (array/property path), then close Phase 11 with target-proof evidence.

Purpose: deliver the second required hot-path optimization and validate final aggregate target closure against boa-engine.
Output: guarded array/property optimizations + semantic parity coverage + final closure evidence bundle.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-RESEARCH.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-01-SUMMARY.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-02-SUMMARY.md
@crates/vm/src/lib.rs
@crates/vm/src/fast_path.rs
@crates/benchmarks/src/main.rs
@docs/performance-closure-policy.md

<interfaces>
From `crates/vm/src/lib.rs` array/property hotspots:
```rust
fn get_property_from_receiver(&mut self, receiver: JsValue, property: &str, realm: &Realm) -> Result<JsValue, VmError>;
fn set_property_on_receiver(&mut self, receiver: JsValue, property: String, value: JsValue, realm: &Realm) -> Result<JsValue, VmError>;
fn array_length(&self, object_id: ObjectId) -> Result<usize, VmError>;
```

From packet evidence flow:
- Baseline: `engine-comparison.local-dev.phase11-baseline.json`
- Packet-A: `engine-comparison.local-dev.packet-a.json`
- Final candidate in this plan must use `engine-comparison.local-dev.packet-b.json`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add guarded dense-array index fast path with descriptor/prototype-safe fallback</name>
  <files>crates/vm/src/fast_path.rs, crates/vm/src/lib.rs</files>
  <action>Implement Packet-B array/property acceleration for canonical dense numeric indices used in `array-sum`-style loops. Fast path preconditions must explicitly reject non-dense arrays, accessor-backed properties, prototype-chain hits, and exotic receivers. Route all rejected cases to existing property/array logic unchanged and instrument guard-hit/miss counters.</action>
  <verify>
    <automated>cargo test -p vm packet_b_array_dense_index_fast_path_guarding -- --exact</automated>
  </verify>
  <done>Array-heavy workloads get a safe fast path while all complex property semantics continue through canonical fallback behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Prove array/property semantic parity under optimization toggles</name>
  <files>crates/vm/tests/perf_packet_b.rs, crates/vm/src/lib.rs</files>
  <action>Add targeted parity tests for holes, inherited indices, accessor properties, sparse arrays, and prototype mutations. Each test must run with packet-B optimization enabled and disabled and assert identical externally observable results (including error behavior/order where relevant).</action>
  <verify>
    <automated>cargo test -p vm perf_packet_b -- --nocapture</automated>
  </verify>
  <done>Packet-B optimization does not alter observable semantics across dense and edge-case array/property scenarios.</done>
</task>

<task type="auto">
  <name>Task 3: Run final closure suite and publish PERF-03/PERF-04/PERF-05 evidence bundle</name>
  <files>crates/benchmarks/src/main.rs, docs/engine-benchmarks.md, .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md</files>
  <action>Produce final local-dev candidate artifact (`packet-b`) and corroborating ci-linux artifact, validate both against contract, then run perf-target checker requiring `qjs-rs` aggregate mean <= `boa-engine` on the locked closure profile. Document per-case deltas for both optimization packets, maintainability boundary checklist (no runtime-core C FFI, layer-local changes, guarded fallback paths), and quality-gate command outcomes in `11-TARGET-CLOSURE-EVIDENCE.md`.</action>
  <verify>
    <automated>cargo run -p benchmarks --release -- --profile local-dev --output target/benchmarks/engine-comparison.local-dev.packet-b.json --allow-missing-comparators && cargo run -p benchmarks --release -- --profile ci-linux --output target/benchmarks/engine-comparison.ci-linux.packet-b.json --allow-missing-comparators && python .github/scripts/check_engine_benchmark_contract.py --input target/benchmarks/engine-comparison.local-dev.packet-b.json && python .github/scripts/check_engine_benchmark_contract.py --input target/benchmarks/engine-comparison.ci-linux.packet-b.json && python .github/scripts/check_perf_target.py --baseline target/benchmarks/engine-comparison.local-dev.phase11-baseline.json --candidate target/benchmarks/engine-comparison.local-dev.packet-b.json --require-qjs-lte-boa && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test</automated>
  </verify>
  <done>Phase 11 has a final artifact and evidence document proving target closure, two optimized hotspots, and maintainability boundary compliance.</done>
</task>

</tasks>

<verification>
Plan acceptance requires passing packet-B parity tests, successful contract+perf-target checks on final artifacts, and green governance commands (`fmt`, `clippy`, `test`) after optimization work.
</verification>

<success_criteria>
Phase 11 exits with two evidence-backed hot-path optimization packets and a validated closure artifact demonstrating `qjs-rs <= boa-engine` on the authoritative profile while preserving semantic and architectural constraints.
</success_criteria>

<output>
After completion, create `.planning/phases/11-hot-path-optimization-and-target-closure/11-03-SUMMARY.md`.
</output>

