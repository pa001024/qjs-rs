---
phase: 11-hot-path-optimization-and-target-closure
plan: 12
type: execute
wave: 9
depends_on:
  - 11-11
files_modified:
  - crates/vm/src/lib.rs
  - crates/vm/src/fast_path.rs
  - crates/vm/src/perf.rs
  - crates/vm/tests/perf_packet_d.rs
  - crates/vm/tests/perf_hotspot_attribution.rs
  - crates/benchmarks/src/main.rs
  - docs/engine-benchmarks.md
  - .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
  - .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
gap_closure: true
requirements:
  - PERF-03
  - PERF-04
  - PERF-05
must_haves:
  truths:
    - Packet-G introduces a new guarded identifier-resolution optimization that preserves fallback semantics under with/prototype/accessor/scope-mutation paths.
    - Authoritative packet-g benchmark artifacts are contract-valid and evaluated with the active quickjs-ratio gate (`qjs-rs <= 1.25x quickjs-c`).
    - Verification and evidence artifacts report an explicit closure verdict with blocker values when PERF-03 remains open.
  artifacts:
    - crates/vm/src/lib.rs
    - crates/vm/src/fast_path.rs
    - crates/vm/src/perf.rs
    - crates/vm/tests/perf_packet_d.rs
    - crates/vm/tests/perf_hotspot_attribution.rs
    - crates/benchmarks/src/main.rs
    - .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
    - .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
  key_links:
    - Identifier resolution fast path must remain guarded by scope-generation and with-scope safety checks.
    - Benchmark candidate metadata must align with performance-closure policy (`local-dev`, `eval-per-iteration`, same-host + strict comparator evidence).
    - Closure wording in verification docs must derive from the same packet-g candidate artifact and checker transcript.
---

<objective>
Execute the next gap-closure optimization loop for Phase 11 with an identifier-resolution-focused packet-g candidate.

Purpose: The latest authoritative packet-final run is still far above the PERF-03 threshold (`5.755257x > 1.25x`), so Phase 11 needs another guarded hotspot attempt plus an auditable verdict update.
Output: Packet-g VM changes, parity/attribution evidence, authoritative benchmark artifacts, and synchronized verification status.
</objective>

<execution_context>
@C:/Users/Administrator/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-RESEARCH.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-11-SUMMARY.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
@docs/performance-closure-policy.md
@crates/vm/src/lib.rs
@crates/vm/src/fast_path.rs
@crates/vm/src/perf.rs
@crates/vm/tests/perf_packet_d.rs
@crates/vm/tests/perf_hotspot_attribution.rs
@crates/benchmarks/src/main.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add packet-g hotspot miss taxonomy before code-path changes</name>
  <files>crates/vm/src/perf.rs, crates/vm/src/lib.rs, crates/vm/tests/perf_hotspot_attribution.rs</files>
  <behavior>
    - Test 1: hotspot attribution exposes packet-D/packet-G identifier guard hit/miss/revalidate counters in a deterministic snapshot.
    - Test 2: with-scope and scope-mutation scripts increase miss/revalidate buckets while stable lexical loops increase hit buckets.
  </behavior>
  <action>Extend VM hotspot/perf tracking so packet-g can prove where identifier-resolution time is still spent (hit path vs miss/revalidate/fallback). Keep metrics default-off, and preserve existing packet-a/b/c/d counters and API compatibility.</action>
  <verify>
    <automated>cargo test -p vm perf_hotspot_attribution -- --nocapture</automated>
  </verify>
  <done>Attribution output distinguishes identifier fast-hit vs fallback/miss families and is test-covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement packet-g guarded identifier fallback-reduction path</name>
  <files>crates/vm/src/lib.rs, crates/vm/src/fast_path.rs, crates/vm/tests/perf_packet_d.rs</files>
  <behavior>
    - Test 1: packet-g enabled/disabled runs produce identical JS results on existing packet-d parity scripts.
    - Test 2: stable lexical identifiers drive packet-g fast-hit counters; with/prototype/accessor/unknown-identifier paths force fallback counters.
    - Test 3: scope-generation changes invalidate stale entries and recover via canonical slow path (no stale binding reuse).
  </behavior>
  <action>Implement a new packet-g guard that reduces repeated `resolve_binding_id_slow` scans for common lexical lookup patterns while preserving packet-d safety invariants. Reuse existing scope-generation invalidation rules, and explicitly avoid any benchmark-specific shortcut logic or semantic bypass.</action>
  <verify>
    <automated>cargo test -p vm perf_packet_d -- --nocapture && cargo test -p vm perf_hotspot_attribution -- --nocapture</automated>
  </verify>
  <done>Packet-g guarded fast path lands with parity-safe fallback behavior and deterministic guard counter coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Run authoritative packet-g closure workflow and publish verdict</name>
  <files>crates/benchmarks/src/main.rs, docs/engine-benchmarks.md, .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md, .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>Preflight the locked baseline artifact path and regenerate baseline with the authoritative policy command if missing. Generate `packet-g` local-dev artifact using strict comparator mode, validate contract, and run `check_perf_target.py --require-qjs-lte-quickjs-ratio 1.25` against the locked phase11 baseline. Append command transcript, candidate hash, aggregate means, ratio, and explicit PASS/FAIL verdict to evidence and verification docs. Record PERF-04 before/after hotspot deltas and PERF-05 boundary evidence (no runtime-core C FFI introduction). If PERF-03 and governance are jointly green in this same run, promote `.planning/REQUIREMENTS.md` + `.planning/ROADMAP.md` closure wording; otherwise keep explicit open-gap wording.</action>
  <verify>
    <automated>cargo fmt --check && cargo clippy -p vm -p benchmarks -- -D warnings && cargo run -p benchmarks --bin benchmarks --release -- --profile local-dev --output target/benchmarks/engine-comparison.local-dev.packet-g.json --quickjs-path scripts/quickjs-wsl.cmd --strict-comparators && python .github/scripts/check_engine_benchmark_contract.py --input target/benchmarks/engine-comparison.local-dev.packet-g.json && python .github/scripts/check_perf_target.py --baseline target/benchmarks/engine-comparison.local-dev.phase11-baseline.json --candidate target/benchmarks/engine-comparison.local-dev.packet-g.json --require-qjs-lte-quickjs-ratio 1.25 && rg --line-number "status:|PERF-03|PERF-04|PERF-05|quickjs-c|ratio|gaps_found|passed|packet-g" .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md .planning/REQUIREMENTS.md .planning/ROADMAP.md && rg --line-number "extern\s+\"C\"|\bunsafe\b" crates/vm crates/runtime crates/bytecode crates/builtins</automated>
  </verify>
  <done>Packet-g evidence is contract-valid with an authoritative quickjs-ratio verdict, and verification artifacts are synchronized to the exact run outcome.</done>
</task>

</tasks>

<verification>
Execution is accepted only if packet-g evidence includes contract validation + quickjs-ratio checker output and verification docs mirror the same verdict.
</verification>

<success_criteria>
Phase 11 has a new guarded optimization candidate and a machine-checkable PERF-03 verdict; blockers remain explicit if ratio gate still fails.
</success_criteria>

<output>
After completion, create `.planning/phases/11-hot-path-optimization-and-target-closure/11-12-SUMMARY.md`
</output>
