---
phase: 11-hot-path-optimization-and-target-closure
plan: 14
type: execute
wave: 11
depends_on:
  - 11-13
files_modified:
  - docs/engine-benchmarks.md
  - .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
  - .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
gap_closure: true
requirements:
  - PERF-03
  - PERF-04
  - PERF-05
must_haves:
  truths:
    - Authoritative packet-h candidate evidence is produced under locked profile/config and evaluated with active PERF-03 quickjs-ratio gate (`1.25x`).
    - Closure docs report exact measured values and checker verdict from the same packet-h candidate path without ambiguity.
    - PERF-04/PERF-05 status remains traceable with explicit boundary evidence (no runtime-core C FFI) even if PERF-03 is still red.
  artifacts:
    - target/benchmarks/engine-comparison.local-dev.packet-h.json
    - target/benchmarks/phase11-closure-bundle.packet-h.json
    - .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
    - .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  key_links:
    - PERF-03 checker invocation must compare the locked baseline (`phase11-baseline`) to packet-h artifact using `--require-qjs-lte-quickjs-ratio 1.25`.
    - `phase11-closure-bundle.packet-h.json` must include artifact hash, checker exit code, aggregate means, and ratio for machine-checkable verdict ingestion.
    - Verification, roadmap, requirements, and state wording must derive from the same packet-h bundle values (no mixed-run data).
---

<objective>
Execute the authoritative packet-h closure workflow and synchronize Phase 11 closure verdict artifacts.

Purpose: Phase 11 cannot close until PERF-03 has an authoritative pass, and latest blocker is still `6.236987x > 1.25x`; this plan produces the next authoritative machine-checkable verdict path.
Output: Packet-h authoritative benchmark artifact, closure-bundle JSON manifest, and synchronized verification/traceability documents.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-RESEARCH.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-13-SUMMARY.md
@docs/performance-closure-policy.md
@docs/engine-benchmarks.md
@crates/benchmarks/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run authoritative packet-h governance and benchmark gate bundle</name>
  <files>docs/engine-benchmarks.md</files>
  <action>Execute authoritative sequence on locked profile: governance gates (`fmt`, `clippy`, targeted packet tests), strict-comparator benchmark generation for `target/benchmarks/engine-comparison.local-dev.packet-h.json`, benchmark-contract validation, and PERF-03 ratio checker against `target/benchmarks/engine-comparison.local-dev.phase11-baseline.json`. Capture checker stdout/stderr and exit code even when ratio gate fails, and record packet-h artifact SHA256 for provenance.</action>
  <verify>
    <automated>cargo fmt --check && cargo clippy -p vm -p benchmarks -- -D warnings && cargo test -p vm perf_packet_d -- --nocapture && cargo test -p vm perf_hotspot_attribution -- --nocapture && cargo run -p benchmarks --bin benchmarks --release -- --profile local-dev --output target/benchmarks/engine-comparison.local-dev.packet-h.json --quickjs-path scripts/quickjs-wsl.cmd --strict-comparators && python .github/scripts/check_engine_benchmark_contract.py --input target/benchmarks/engine-comparison.local-dev.packet-h.json && python -c "import json,pathlib,subprocess,sys; out_path=pathlib.Path('target/benchmarks/perf-target.packet-h.stdout.log'); err_path=pathlib.Path('target/benchmarks/perf-target.packet-h.stderr.log'); verdict_path=pathlib.Path('target/benchmarks/perf-target.packet-h.verdict.json'); out_path.parent.mkdir(parents=True, exist_ok=True); cmd=['python','.github/scripts/check_perf_target.py','--baseline','target/benchmarks/engine-comparison.local-dev.phase11-baseline.json','--candidate','target/benchmarks/engine-comparison.local-dev.packet-h.json','--require-qjs-lte-quickjs-ratio','1.25']; p=subprocess.run(cmd,capture_output=True,text=True); out_path.write_text(p.stdout,encoding='utf-8'); err_path.write_text(p.stderr,encoding='utf-8'); errors=[line[2:] for line in p.stderr.splitlines() if line.startswith('- ')]; threshold_only=bool(errors) and all('require-qjs-lte-quickjs-ratio failed' in line for line in errors); status='passed' if p.returncode==0 else ('threshold_fail_expected' if p.returncode==1 and threshold_only else 'checker_or_runtime_error'); verdict={'status':status,'exit_code':p.returncode,'stdout_log':out_path.as_posix(),'stderr_log':err_path.as_posix()}; verdict_path.write_text(json.dumps(verdict,indent=2),encoding='utf-8'); print(json.dumps(verdict)); sys.exit(0 if status in ('passed','threshold_fail_expected') else (p.returncode or 1))"</automated>
  </verify>
  <done>Authoritative packet-h candidate exists with governance transcript, contract-check result, ratio-check output, and artifact hash provenance.</done>
</task>

<task type="auto">
  <name>Task 2: Emit machine-checkable packet-h closure bundle and sync traceability docs</name>
  <files>.planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md, .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Build `target/benchmarks/phase11-closure-bundle.packet-h.json` from the authoritative run with fields: baseline path, candidate path, candidate hash, governance command statuses, perf-target checker exit code, aggregate mean values, and computed `qjs-rs/quickjs-c` ratio. Update evidence and verification docs to cite this exact bundle path and numbers. If ratio checker passes, promote Phase 11 to closure-ready wording; otherwise keep explicit blocker wording with concrete value and command transcript.</action>
  <verify>
    <automated>python .github/scripts/check_engine_benchmark_contract.py --input target/benchmarks/engine-comparison.local-dev.packet-h.json && python -c "import json,sys; p='target/benchmarks/phase11-closure-bundle.packet-h.json'; d=json.load(open(p,'r',encoding='utf-8')); assert d['candidate'].endswith('engine-comparison.local-dev.packet-h.json'); assert 'qjs_rs_to_quickjs_ratio' in d['aggregate']; print('bundle-ok', d['aggregate']['qjs_rs_to_quickjs_ratio'])" && rg --line-number "packet-h|phase11-closure-bundle.packet-h.json|qjs-rs/quickjs-c|PERF-03|PERF-04|PERF-05|status:|gaps_found|passed" .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md</automated>
  </verify>
  <done>Phase 11 authoritative closure verdict is machine-checkable via packet-h bundle JSON and fully synchronized across all traceability docs.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run runtime-core boundary check and encode PERF-05 evidence in closure docs</name>
  <files>.planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md, .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md</files>
  <action>Run runtime-core boundary scan for `extern \"C\"` and `unsafe` across `crates/vm`, `crates/runtime`, `crates/bytecode`, and `crates/builtins`. Record the exact command and result in packet-h evidence/verification sections so PERF-05 closure signal remains explicit and tied to the same authoritative verdict cycle.</action>
  <verify>
    <automated>python -c "import pathlib,subprocess,sys; out_path=pathlib.Path('target/benchmarks/perf05-boundary-scan.packet-h.log'); out_path.parent.mkdir(parents=True, exist_ok=True); cmd=['rg','--line-number','extern\\\\s+\"C\"|\\\\bunsafe\\\\b','crates/vm','crates/runtime','crates/bytecode','crates/builtins']; p=subprocess.run(cmd,capture_output=True,text=True); text=(p.stdout or '') + (p.stderr or ''); out_path.write_text(text,encoding='utf-8'); print(text,end=''); sys.exit(0 if p.returncode==1 else (1 if p.returncode==0 else p.returncode))" && rg --line-number "extern\\\\s+\\\"C\\\"|unsafe|runtime-core|PERF-05|boundary" .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md</automated>
  </verify>
  <done>PERF-05 boundary evidence is refreshed in the same packet-h closure packet and remains auditable.</done>
</task>

</tasks>

<verification>
Execution passes only if packet-h candidate, packet-h bundle JSON, and synchronized verification documents all reference the same authoritative run values.
</verification>

<success_criteria>
Phase 11 has an authoritative packet-h closure verdict path that can be machine-checked directly, with explicit PASS/FAIL outcome and blocker values for PERF-03.
</success_criteria>

<output>
After completion, create `.planning/phases/11-hot-path-optimization-and-target-closure/11-14-SUMMARY.md`
</output>
