---
phase: 11-hot-path-optimization-and-target-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/scripts/check_perf_target.py
  - crates/benchmarks/src/contract.rs
  - crates/benchmarks/src/main.rs
  - crates/benchmarks/tests/hot_path_contract.rs
  - crates/vm/src/lib.rs
  - crates/vm/src/perf.rs
  - crates/vm/tests/perf_hotspot_attribution.rs
  - docs/engine-benchmarks.md
  - docs/performance-closure-policy.md
autonomous: true
requirements:
  - PERF-03
  - PERF-04
  - PERF-05
must_haves:
  truths:
    - Phase 11 has one explicit closure gate profile/timing policy, and target claims are forbidden outside that policy.
    - Benchmark artifacts include machine-checkable perf-target metadata and optional qjs-rs hotspot attribution required for packet-level optimization evidence.
    - VM hotspot attribution can run with optimizations disabled so later packet deltas are auditable instead of guesswork.
  artifacts:
    - .github/scripts/check_perf_target.py
    - crates/benchmarks/src/contract.rs
    - crates/benchmarks/src/main.rs
    - crates/benchmarks/tests/hot_path_contract.rs
    - crates/vm/src/perf.rs
    - crates/vm/src/lib.rs
    - crates/vm/tests/perf_hotspot_attribution.rs
    - docs/performance-closure-policy.md
  key_links:
    - `docs/performance-closure-policy.md` defines the PERF-03 gate profile and comparator-availability policy that `.github/scripts/check_perf_target.py` enforces.
    - `crates/vm/src/perf.rs` exposes hotspot attribution data that `crates/benchmarks/src/main.rs` serializes into benchmark artifacts.
    - `crates/benchmarks/tests/hot_path_contract.rs` prevents metadata/attribution drift that would invalidate Phase 11 before/after evidence.
---

<objective>
Create the Phase 11 safety and evidence foundation before changing runtime hot paths.

Purpose: lock PERF-03 closure semantics and establish attribution/parity tooling so optimization packets can be proven safe and effective.
Output: perf-target checker + closure policy doc + benchmark contract extensions + VM hotspot attribution baseline.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-RESEARCH.md
@crates/benchmarks/src/contract.rs
@crates/benchmarks/src/main.rs
@crates/vm/src/lib.rs
@docs/engine-benchmarks.md

<interfaces>
From `crates/benchmarks/src/contract.rs`:
```rust
pub const SCHEMA_VERSION: &str = "bench.v1";
pub enum RunProfile { LocalDev, CiLinux }
pub enum TimingMode { EvalPerIteration }
pub struct BenchmarkReport { ... }
```

From `crates/benchmarks/src/main.rs`:
```rust
fn run_qjs_rs_eval_per_iteration(script: &str, iterations: usize) -> Result<SampleMeasurement>;
fn aggregate(cases: &[CaseReport]) -> AggregateReport;
```

From `crates/vm/src/lib.rs`:
```rust
pub struct Vm { ... }
pub fn execute_in_realm(&mut self, chunk: &Chunk, realm: &Realm) -> Result<JsValue, VmError>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock PERF-03 closure policy and machine-check perf-target gate</name>
  <files>.github/scripts/check_perf_target.py, docs/performance-closure-policy.md, docs/engine-benchmarks.md</files>
  <action>Create a dedicated perf-target checker script that compares baseline/candidate artifacts and supports self-test fixtures. The policy must lock one authoritative closure profile (`local-dev`, `eval-per-iteration`, same-host rerun) and define comparator availability rules (qjs-rs vs boa-engine required; quickjs-c/node may be missing if metadata records status/reason). Document required commands and acceptance thresholds in `docs/performance-closure-policy.md` and reference them from `docs/engine-benchmarks.md`.</action>
  <verify>
    <automated>python .github/scripts/check_perf_target.py --self-test && rg --line-number "authoritative|local-dev|qjs-rs|boa-engine|same-host|missing comparator" docs/performance-closure-policy.md docs/engine-benchmarks.md</automated>
  </verify>
  <done>Phase 11 has an explicit, script-enforced closure policy and cannot claim PERF-03 without passing the perf-target checker.</done>
</task>

<task type="auto">
  <name>Task 2: Add VM hotspot attribution primitives with zero-semantic-impact toggles</name>
  <files>crates/vm/src/perf.rs, crates/vm/src/lib.rs, crates/vm/tests/perf_hotspot_attribution.rs</files>
  <action>Add an internal `perf` module for opcode-family/hot-path counters (at minimum numeric ops, identifier resolution, and array indexed property operations). Wire it behind explicit opt-in toggles (e.g., env/VM config) so default behavior is unchanged. Add tests proving counters increment deterministically while script results remain identical with attribution enabled/disabled.</action>
  <verify>
    <automated>cargo test -p vm perf_hotspot_attribution_records_opcode_families -- --exact && cargo test -p vm perf_hotspot_toggle_preserves_semantics -- --exact</automated>
  </verify>
  <done>Attribution data is available for Phase 11 hotspot ranking without changing observable JS behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Extend benchmark contract/report with attribution + produce Phase 11 baseline artifact</name>
  <files>crates/benchmarks/src/contract.rs, crates/benchmarks/src/main.rs, crates/benchmarks/tests/hot_path_contract.rs, docs/engine-benchmarks.md</files>
  <action>Extend benchmark report metadata to carry: optimization mode/tag, packet identifier, and optional qjs-rs hotspot attribution snapshot. Ensure contract serialization keeps backward compatibility for Phase 10 fields while making new Phase 11 fields explicit and test-covered. Run and validate a fresh baseline artifact at `target/benchmarks/engine-comparison.local-dev.phase11-baseline.json` using the locked closure profile and publish the exact command sequence in docs.</action>
  <verify>
    <automated>cargo test -p benchmarks hot_path_contract -- --nocapture && cargo run -p benchmarks --release -- --profile local-dev --output target/benchmarks/engine-comparison.local-dev.phase11-baseline.json --allow-missing-comparators && python .github/scripts/check_engine_benchmark_contract.py --input target/benchmarks/engine-comparison.local-dev.phase11-baseline.json</automated>
  </verify>
  <done>Phase 11 has a validated baseline artifact that includes the metadata required to compare optimization packets and final closure runs.</done>
</task>

</tasks>

<verification>
Acceptance requires (1) perf-target checker self-test pass, (2) VM attribution tests pass, and (3) a freshly validated `phase11-baseline` artifact generated under the locked closure policy.
</verification>

<success_criteria>
Phase 11 optimization work can proceed with objective hotspot attribution, explicit closure rules, and machine-checkable baseline evidence that supports PERF-03/PERF-04/PERF-05 auditing.
</success_criteria>

<output>
After completion, create `.planning/phases/11-hot-path-optimization-and-target-closure/11-01-SUMMARY.md`.
</output>

