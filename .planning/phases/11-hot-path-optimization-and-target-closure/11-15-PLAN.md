---
phase: 11-hot-path-optimization-and-target-closure
plan: 15
type: execute
wave: 12
depends_on:
  - 11-14
files_modified:
  - crates/vm/src/lib.rs
  - crates/vm/tests/perf_packet_d.rs
  - crates/vm/tests/perf_hotspot_attribution.rs
  - crates/benchmarks/src/main.rs
  - docs/engine-benchmarks.md
autonomous: true
gap_closure: true
requirements:
  - PERF-03
  - PERF-04
  - PERF-05
must_haves:
  truths:
    - Packet-i reduces identifier-resolution fallback churn for loop-stable outer lexical bindings without changing observable script results.
    - Packet-i keeps guarded fallback behavior for with/prototype/accessor/unknown-identifier paths and preserves packet parity semantics.
    - Benchmark harness can emit packet-i artifacts with deterministic toggle inference and contract-valid hotspot attribution payloads.
  artifacts:
    - crates/vm/src/lib.rs
    - crates/vm/tests/perf_packet_d.rs
    - crates/vm/tests/perf_hotspot_attribution.rs
    - crates/benchmarks/src/main.rs
    - docs/engine-benchmarks.md
  key_links:
    - Packet-i toggle wiring in `crates/benchmarks/src/main.rs` must activate qjs-rs packet-i revalidation behavior through VM runtime flags.
    - Packet-d/packet-g revalidate counters and `identifier_resolution_fallback_scans` must remain consistent between VM tests and benchmark hotspot snapshots.
    - Packet-i smoke artifact path naming must drive deterministic packet toggle inference used by authoritative rerun commands.
---

<objective>
Land packet-i revalidation optimization to cut fallback scans in loop-heavy lexical workloads before the next authoritative closure rerun.

Purpose: The latest authoritative packet-h ratio is still `6.260034x`, with `fib-iterative` (`8.977744x`) and `array-sum` (`7.193323x`) still dominated by identifier-resolution churn despite prior packets.
Output: Packet-i VM optimization, parity/hotspot tests, and packet-i smoke benchmark artifact flow ready for authoritative closure evaluation.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-RESEARCH.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-14-SUMMARY.md
@crates/vm/src/lib.rs
@crates/vm/tests/perf_packet_d.rs
@crates/vm/tests/perf_hotspot_attribution.rs
@crates/benchmarks/src/main.rs
@docs/engine-benchmarks.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add packet-i parity and hotspot regression tests for scope-churn revalidation</name>
  <files>crates/vm/tests/perf_packet_d.rs, crates/vm/tests/perf_hotspot_attribution.rs</files>
  <behavior>
    - Test 1: packet-i enabled/disabled executions produce identical JS values on loop/block/with stress scripts.
    - Test 2: stable outer lexical bindings with inner block churn increase packet-d/packet-g revalidate-hit coverage and reduce fallback scans versus packet-h baseline.
    - Test 3: with/object-shadow/unknown-identifier paths still produce packet-i misses and keep fallback-scan attribution non-zero.
  </behavior>
  <action>Introduce packet-i toggle-aware tests first, reusing existing packet-d/packet-g/packet-h perf suites rather than adding one-off harnesses. Lock deterministic expectations around `identifier_resolution_fallback_scans` and revalidate counters so packet-i can prove measurable hotspot improvement without semantic drift.</action>
  <verify>
    <automated>cargo test -p vm perf_packet_d -- --nocapture && cargo test -p vm perf_hotspot_attribution -- --nocapture</automated>
  </verify>
  <done>Packet-i behavioral and hotspot expectations are codified and failing prior to packet-i implementation changes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement packet-i shadow-aware slot/name revalidation in VM fast-path resolution</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Implement packet-i as a guarded extension of packet-d/packet-g revalidation: when scope-generation changes, allow cache reuse when the cached binding remains visible without shadowing across newer inner scopes, instead of requiring innermost-scope-only revalidation. Keep `with` behavior, unknown-identifier behavior, and canonical slow-path fallback unchanged; packet-i must be runtime-toggleable for benchmark comparison and safety rollback.</action>
  <verify>
    <automated>cargo test -p vm perf_packet_d -- --nocapture && cargo test -p vm perf_hotspot_attribution -- --nocapture && cargo test -p vm -- --nocapture</automated>
  </verify>
  <done>Packet-i fast path lands with guarded semantics, deterministic counter behavior, and full VM test parity.</done>
</task>

<task type="auto">
  <name>Task 3: Wire packet-i benchmark toggle inference and produce smoke artifact</name>
  <files>crates/benchmarks/src/main.rs, docs/engine-benchmarks.md</files>
  <action>Extend benchmark packet inference so packet-i output paths enable the intended VM packet stack (c+d+g+h plus packet-i revalidation behavior), update packet toggle tests in `main.rs`, and document packet-i smoke command sequence. Generate `target/benchmarks/engine-comparison.local-dev.packet-i.smoke.json` with strict comparators and validate contract compatibility.</action>
  <verify>
    <automated>cargo test -p benchmarks -- --nocapture && cargo run -p benchmarks --bin benchmarks --release -- --profile local-dev --output target/benchmarks/engine-comparison.local-dev.packet-i.smoke.json --quickjs-path scripts/quickjs-wsl.cmd --strict-comparators && python .github/scripts/check_engine_benchmark_contract.py --input target/benchmarks/engine-comparison.local-dev.packet-i.smoke.json</automated>
  </verify>
  <done>Packet-i smoke artifact is contract-valid and benchmark pipeline is ready for authoritative quickjs-ratio closure rerun.</done>
</task>

</tasks>

<verification>
Execution is accepted only if packet-i tests prove semantics parity and hotspot counter deltas, and packet-i smoke artifact passes benchmark contract validation.
</verification>

<success_criteria>
Phase 11 has a new packet-i optimization candidate that is test-backed, benchmark-wired, and ready for authoritative PERF-03 rerun.
</success_criteria>

<output>
After completion, create `.planning/phases/11-hot-path-optimization-and-target-closure/11-15-SUMMARY.md`
</output>

