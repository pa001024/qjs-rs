---
phase: 11-hot-path-optimization-and-target-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - crates/vm/src/fast_path.rs
  - crates/vm/src/lib.rs
  - crates/vm/tests/perf_packet_a.rs
  - crates/benchmarks/src/main.rs
  - crates/benchmarks/tests/perf_packet_a_report.rs
  - .planning/phases/11-hot-path-optimization-and-target-closure/11-PACKET-A-EVIDENCE.md
autonomous: true
requirements:
  - PERF-03
  - PERF-04
  - PERF-05
must_haves:
  truths:
    - The first optimization packet improves arithmetic/call-heavy hotspots with guarded fast paths and measurable before/after evidence.
    - Numeric and binding acceleration paths always fall back to canonical slow paths when preconditions do not hold.
    - Packet-A benchmark artifacts are contract-valid and directly comparable against the Phase 11 baseline.
  artifacts:
    - crates/vm/src/fast_path.rs
    - crates/vm/src/lib.rs
    - crates/vm/tests/perf_packet_a.rs
    - crates/benchmarks/src/main.rs
    - crates/benchmarks/tests/perf_packet_a_report.rs
    - .planning/phases/11-hot-path-optimization-and-target-closure/11-PACKET-A-EVIDENCE.md
  key_links:
    - `crates/vm/src/lib.rs` dispatch for arithmetic + identifier resolution routes through guarded helpers in `crates/vm/src/fast_path.rs`.
    - `crates/vm/tests/perf_packet_a.rs` proves optimization-on/off semantic parity for arithmetic/fib-focused scripts.
    - `11-PACKET-A-EVIDENCE.md` ties packet-A benchmark output to baseline via `.github/scripts/check_perf_target.py`.
---

<objective>
Implement Optimization Packet A (numeric and binding hot paths) with hard safety/fallback guarantees.

Purpose: close the largest arithmetic/call-heavy gap while preserving semantics and maintainable VM boundaries.
Output: guarded numeric + binding fast paths, packet-focused tests, and packet-A benchmark evidence.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-RESEARCH.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-01-SUMMARY.md
@crates/vm/src/lib.rs
@crates/vm/src/perf.rs
@crates/benchmarks/src/main.rs
@docs/performance-closure-policy.md

<interfaces>
From `crates/vm/src/lib.rs` hotspot anchors:
```rust
fn execute_code(&mut self, code: &[Opcode], functions: &[CompiledFunction], realm: &Realm, allow_return: bool, strict: bool) -> Result<ExecutionSignal, VmError>;
fn evaluate_add(&mut self, realm: &Realm, caller_strict: bool) -> Result<JsValue, VmError>;
fn resolve_identifier_reference(&mut self, name: &str, realm: &Realm, _strict: bool) -> Result<IdentifierReference, VmError>;
```

From `docs/performance-closure-policy.md` (from 11-01):
- Baseline artifact: `target/benchmarks/engine-comparison.local-dev.phase11-baseline.json`
- Candidate artifact naming must include packet identifier.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add guarded numeric fast paths for arithmetic-heavy opcode families</name>
  <files>crates/vm/src/fast_path.rs, crates/vm/src/lib.rs</files>
  <action>Introduce packet-A numeric helpers that short-circuit common `Number`/`Bool` arithmetic paths (`Add`, `Sub`, `Mul`, `Div`, and loop-critical numeric coercion sites) when operands satisfy strict preconditions. Keep string-concat/object-coercion behavior untouched by routing all non-safe cases to existing primitive/coercion logic. Ensure guard misses are explicit and recorded in perf attribution counters.</action>
  <verify>
    <automated>cargo test -p vm packet_a_numeric_fast_path_parity -- --exact</automated>
  </verify>
  <done>Arithmetic-heavy benchmarks use fast numeric paths where safe, and all non-safe operand combinations still follow canonical semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add scoped binding-resolution acceleration with deterministic invalidation</name>
  <files>crates/vm/src/fast_path.rs, crates/vm/src/lib.rs, crates/vm/tests/perf_packet_a.rs</files>
  <action>Implement a guarded binding lookup accelerator for loop-heavy/local-scope identifier reads and writes. The cache path must be invalidated on scope stack mutations, `with` interactions, and global/property fallthrough cases. Add tests that specifically cover shadowing, closure captures, and `with`/global fallback behavior to prove no semantic drift.</action>
  <verify>
    <automated>cargo test -p vm packet_a_binding_cache_scope_invalidation -- --exact && cargo test -p vm packet_a_binding_cache_with_scope_fallback -- --exact</automated>
  </verify>
  <done>Identifier-heavy paths are accelerated only when safe, and invalidation guarantees preserve correct lookup semantics in dynamic scope scenarios.</done>
</task>

<task type="auto">
  <name>Task 3: Publish packet-A before/after evidence against locked baseline</name>
  <files>crates/benchmarks/src/main.rs, crates/benchmarks/tests/perf_packet_a_report.rs, .planning/phases/11-hot-path-optimization-and-target-closure/11-PACKET-A-EVIDENCE.md</files>
  <action>Tag packet-A benchmark output in report metadata, generate `target/benchmarks/engine-comparison.local-dev.packet-a.json`, validate it against bench contract, and compare it to the phase11 baseline using `check_perf_target.py`. Require explicit per-case evidence for `arith-loop` and `fib-iterative`, plus a non-regression check for `json-roundtrip`. Record command lines and deltas in `11-PACKET-A-EVIDENCE.md`.</action>
  <verify>
    <automated>cargo test -p benchmarks perf_packet_a_report -- --nocapture && cargo run -p benchmarks --release -- --profile local-dev --output target/benchmarks/engine-comparison.local-dev.packet-a.json --allow-missing-comparators && python .github/scripts/check_engine_benchmark_contract.py --input target/benchmarks/engine-comparison.local-dev.packet-a.json && python .github/scripts/check_perf_target.py --baseline target/benchmarks/engine-comparison.local-dev.phase11-baseline.json --candidate target/benchmarks/engine-comparison.local-dev.packet-a.json --expect-case-improvement arith-loop --expect-case-improvement fib-iterative --max-case-regression json-roundtrip=1.10</automated>
  </verify>
  <done>Packet-A produces validated, auditable improvements on targeted hotspots with explicit non-regression evidence.</done>
</task>

</tasks>

<verification>
Plan acceptance requires parity tests for guarded numeric/binding paths and a successful packet-A delta check versus the locked Phase 11 baseline artifact.
</verification>

<success_criteria>
At least one full optimization packet is landed with measurable hotspot gains, guarded fallback behavior, and evidence that remains compliant with PERF-05 boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/11-hot-path-optimization-and-target-closure/11-02-SUMMARY.md`.
</output>

