---
phase: 11-hot-path-optimization-and-target-closure
plan: 06
type: execute
wave: 3
depends_on:
  - 11-05
files_modified:
  - crates/bytecode/src/lib.rs
  - crates/vm/src/fast_path.rs
  - crates/vm/src/lib.rs
  - crates/vm/tests/perf_packet_d.rs
  - crates/benchmarks/src/main.rs
  - docs/engine-benchmarks.md
  - .planning/phases/11-hot-path-optimization-and-target-closure/11-PACKET-D-EVIDENCE.md
autonomous: true
gap_closure: true
requirements:
  - PERF-03
  - PERF-04
  - PERF-05
must_haves:
  truths:
    - The next PERF-03 closure candidate must target the dominant remaining hotspot family (`identifier_resolution`) without regressing packet-B array hot paths.
    - Identifier resolution acceleration must avoid repeated string-hash lookups by using stable compiler-provided identifier slots, with guarded fallback for dynamic scope/`with`/mutation paths.
    - Packet-D evidence must include baseline/packet-b/packet-c/packet-d deltas plus authoritative `check_perf_target.py --require-qjs-lte-boa` output.
  artifacts:
    - crates/bytecode/src/lib.rs
    - crates/vm/src/fast_path.rs
    - crates/vm/src/lib.rs
    - crates/vm/tests/perf_packet_d.rs
    - crates/benchmarks/src/main.rs
    - .planning/phases/11-hot-path-optimization-and-target-closure/11-PACKET-D-EVIDENCE.md
  key_links:
    - Bytecode identifier opcodes must expose stable identifier-slot metadata consumed directly by VM packet-D guards.
    - `crates/vm/src/lib.rs` must route packet-D slot-cache hits through canonical fallback logic whenever guard predicates fail.
    - `crates/vm/tests/perf_packet_d.rs` must verify parity/invalidation coverage with packet-D enabled and disabled.
    - `11-PACKET-D-EVIDENCE.md` must include machine-checkable command transcript and closure verdict tied to packet-d artifacts.
---

<objective>
Build and evaluate a packet-D closure candidate focused on identifier-resolution overhead so Phase 11 can make another authoritative PERF-03 attempt.

Purpose: Packet-C regressed aggregate performance and the closure gate remains open; packet-D must target the highest-cost remaining execution path with stronger cache locality.
Output: packet-D identifier-slot fast path + parity/guard tests + contract-valid packet-d benchmark evidence.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-05-SUMMARY.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-PACKET-C-EVIDENCE.md
@crates/bytecode/src/lib.rs
@crates/vm/src/fast_path.rs
@crates/vm/src/lib.rs
@crates/benchmarks/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add packet-D identifier-slot cache path in compiler + VM</name>
  <files>crates/bytecode/src/lib.rs, crates/vm/src/fast_path.rs, crates/vm/src/lib.rs</files>
  <action>Introduce stable identifier-slot metadata in bytecode for identifier-heavy opcodes, then implement a packet-D VM cache keyed by slot/scope generation instead of identifier strings. Ensure every packet-D hit is guarded by dynamic-scope safety checks (`with` frames, scope/binding mutation invalidation, accessor/prototype-sensitive global lookups) and immediately falls back to canonical resolution when guards fail. Keep packet-D counters metrics-gated like prior packets.</action>
  <verify>
    <automated>cargo test -p vm packet_d_identifier_slot_fast_path_guarding -- --exact</automated>
  </verify>
  <done>Packet-D slot-cache fast path exists, is guard-protected, and can be toggled without semantic drift.</done>
</task>

<task type="auto">
  <name>Task 2: Add packet-D parity, invalidation, and hotspot attribution coverage</name>
  <files>crates/vm/tests/perf_packet_d.rs, crates/vm/src/lib.rs</files>
  <action>Create packet-D tests that execute with optimization enabled/disabled and assert identical observable behavior for lexical shadowing, `typeof` unknown identifier, `with` lookups, global own-data shortcuts, accessor/prototype transitions, and post-mutation cache invalidation. Assert packet-D guard hit/miss counters and confirm hotspot attribution still reports identifier-resolution activity when enabled.</action>
  <verify>
    <automated>cargo test -p vm perf_packet_d -- --nocapture</automated>
  </verify>
  <done>Packet-D behavior is parity-safe under dynamic lookup edge cases and exposes auditable guard telemetry.</done>
</task>

<task type="auto">
  <name>Task 3: Generate packet-D closure artifacts and PERF-03 verdict evidence</name>
  <files>crates/benchmarks/src/main.rs, docs/engine-benchmarks.md, .planning/phases/11-hot-path-optimization-and-target-closure/11-PACKET-D-EVIDENCE.md</files>
  <action>Add packet-D optimization tagging/toggles to benchmark harness output, generate `local-dev` and `ci-linux` packet-d artifacts, validate both with contract checker, and run authoritative closure gate against `phase11-baseline` using `--require-qjs-lte-boa`. In `11-PACKET-D-EVIDENCE.md`, record aggregate/per-case deltas versus baseline/packet-b/packet-c, hotspot snapshots, exact command transcript, and explicit pass/fail closure verdict.</action>
  <verify>
    <automated>cargo run -p benchmarks --release -- --profile local-dev --output target/benchmarks/engine-comparison.local-dev.packet-d.json --allow-missing-comparators && cargo run -p benchmarks --release -- --profile ci-linux --output target/benchmarks/engine-comparison.ci-linux.packet-d.json --allow-missing-comparators && python .github/scripts/check_engine_benchmark_contract.py --input target/benchmarks/engine-comparison.local-dev.packet-d.json && python .github/scripts/check_engine_benchmark_contract.py --input target/benchmarks/engine-comparison.ci-linux.packet-d.json && python .github/scripts/check_perf_target.py --baseline target/benchmarks/engine-comparison.local-dev.phase11-baseline.json --candidate target/benchmarks/engine-comparison.local-dev.packet-d.json --require-qjs-lte-boa</automated>
  </verify>
  <done>Packet-D has contract-valid evidence and an authoritative PERF-03 gate verdict suitable for final closure rerun.</done>
</task>

</tasks>

<verification>
Planning-time expectation (gap-closure mode): PERF-03 may still appear open before execution; this plan is accepted only if it explicitly targets the measured identifier-resolution bottleneck and produces authoritative packet-d evidence.

Post-execution acceptance requires packet-D parity tests to pass and packet-d artifacts/checker output to be reproducible under locked benchmark policy.
</verification>

<success_criteria>
Planning output is accepted when this plan introduces a concrete, evidence-driven PERF-03 closure attempt rather than repeating packet-c behavior.

Execution output is successful when packet-D delivers a verified closure candidate with full evidence and checker transcript for downstream governance + traceability synchronization.
</success_criteria>

<output>
After completion, create `.planning/phases/11-hot-path-optimization-and-target-closure/11-06-SUMMARY.md`.
</output>
