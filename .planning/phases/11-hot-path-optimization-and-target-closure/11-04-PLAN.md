---
phase: 11-hot-path-optimization-and-target-closure
plan: 04
type: execute
wave: 1
depends_on:
  - 11-03
files_modified:
  - crates/vm/src/fast_path.rs
  - crates/vm/src/lib.rs
  - crates/vm/tests/perf_packet_c.rs
  - crates/benchmarks/src/main.rs
  - docs/engine-benchmarks.md
  - .planning/phases/11-hot-path-optimization-and-target-closure/11-PACKET-C-EVIDENCE.md
autonomous: true
gap_closure: true
requirements:
  - PERF-03
  - PERF-04
  - PERF-05
must_haves:
  truths:
    - The next closure candidate must pass `check_perf_target.py --require-qjs-lte-boa` on the locked `local-dev` / `eval-per-iteration` profile.
    - Packet-C optimization targets the dominant remaining hotspot family (`identifier_resolution`) and keeps fallback parity for `with`/dynamic/global lookup edge cases.
    - Packet-C evidence reports before/after deltas versus phase11-baseline and packet-b using contract-valid artifacts.
  artifacts:
    - crates/vm/src/fast_path.rs
    - crates/vm/src/lib.rs
    - crates/vm/tests/perf_packet_c.rs
    - crates/benchmarks/src/main.rs
    - .planning/phases/11-hot-path-optimization-and-target-closure/11-PACKET-C-EVIDENCE.md
  key_links:
    - `crates/vm/src/lib.rs` must route identifier/property lookup through packet-C guards and canonical fallback paths.
    - `crates/vm/tests/perf_packet_c.rs` must assert parity with packet-C enabled/disabled for scope/prototype/accessor edge cases.
    - `crates/benchmarks/src/main.rs` + `11-PACKET-C-EVIDENCE.md` must publish checker-verifiable packet-c artifacts and closure-gate output.
---

<objective>
Close the unresolved PERF-03 gate with a focused packet-C optimization and authoritative evidence rerun.

Purpose: Phase 11 still fails the aggregate `qjs-rs <= boa-engine` closure check; hotspot data shows identifier resolution dominates remaining cost.
Output: guarded packet-C runtime optimization + parity tests + packet-c benchmark/evidence bundle proving closure status.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-03-SUMMARY.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
@crates/vm/src/lib.rs
@crates/vm/src/fast_path.rs
@crates/benchmarks/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement packet-C guarded identifier/global lookup fast path</name>
  <files>crates/vm/src/fast_path.rs, crates/vm/src/lib.rs</files>
  <action>Use phase11 hotspot attribution evidence (`identifier_resolution` dominates packet-b totals) to add a packet-C fast path for repeated identifier/global resolution in tight loops. Add guard conditions that immediately fall back to canonical lookup when `with` scopes, dynamic `eval`, accessor/prototype mutation risk, or non-canonical property receivers are present. Keep counters/metrics opt-in as in packet-a/packet-b to avoid benchmark-path overhead regressions.</action>
  <verify>
    <automated>cargo test -p vm packet_c_identifier_resolution_guarding -- --exact</automated>
  </verify>
  <done>VM has a guarded packet-C lookup acceleration path that can be switched on/off without changing default semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add packet-C semantic parity and invalidation coverage</name>
  <files>crates/vm/tests/perf_packet_c.rs, crates/vm/src/lib.rs</files>
  <action>Create dedicated packet-C parity tests that run with optimization enabled and disabled, then assert identical externally observable behavior for lexical shadowing, global fallback, `typeof` unknown identifier, `with` scope lookup, prototype/accessor transitions, and mutation-driven invalidation. Include explicit assertions on packet-C guard hit/miss counters where applicable.</action>
  <verify>
    <automated>cargo test -p vm perf_packet_c -- --nocapture</automated>
  </verify>
  <done>Packet-C provides measurable guard activity and preserves script-visible semantics across dynamic lookup edge cases.</done>
</task>

<task type="auto">
  <name>Task 3: Generate packet-C closure artifacts and PERF-03 evidence</name>
  <files>crates/benchmarks/src/main.rs, docs/engine-benchmarks.md, .planning/phases/11-hot-path-optimization-and-target-closure/11-PACKET-C-EVIDENCE.md</files>
  <action>Wire packet-C metadata tagging in benchmark output (mode/tag/attribution consistency), run local-dev and ci-linux packet-c artifacts, validate contract schema, and run perf-target checker against the locked phase11 baseline with `--require-qjs-lte-boa`. Record per-case and aggregate deltas vs baseline and packet-b, plus pass/fail closure verdict and command transcript in `11-PACKET-C-EVIDENCE.md`.</action>
  <verify>
    <automated>cargo run -p benchmarks --release -- --profile local-dev --output target/benchmarks/engine-comparison.local-dev.packet-c.json --allow-missing-comparators && cargo run -p benchmarks --release -- --profile ci-linux --output target/benchmarks/engine-comparison.ci-linux.packet-c.json --allow-missing-comparators && python .github/scripts/check_engine_benchmark_contract.py --input target/benchmarks/engine-comparison.local-dev.packet-c.json && python .github/scripts/check_engine_benchmark_contract.py --input target/benchmarks/engine-comparison.ci-linux.packet-c.json && python .github/scripts/check_perf_target.py --baseline target/benchmarks/engine-comparison.local-dev.phase11-baseline.json --candidate target/benchmarks/engine-comparison.local-dev.packet-c.json --require-qjs-lte-boa</automated>
  </verify>
  <done>Packet-C evidence conclusively shows whether PERF-03 is closed under authoritative policy, with reproducible artifacts and checker output.</done>
</task>

</tasks>

<verification>
Planning-time expectation (revision/gap-closure mode): `11-VERIFICATION.md` may still show `status: gaps_found` with PERF-03 open before execution; that is expected and not a plan-design failure.

Post-execution acceptance requires packet-C parity tests passing and a contract-valid packet-c artifact that satisfies `require-qjs-lte-boa` against the phase11 baseline.
</verification>

<success_criteria>
Planning output is accepted when this plan clearly targets the open PERF-03 blocker without assuming it is already closed.

Execution output is successful when Phase 11 has a new closure candidate targeting the dominant remaining hotspot and an auditable evidence packet for PERF-03 status.
</success_criteria>

<output>
After completion, create `.planning/phases/11-hot-path-optimization-and-target-closure/11-04-SUMMARY.md`.
</output>
