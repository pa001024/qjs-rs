---
phase: 11-hot-path-optimization-and-target-closure
plan: 13
type: execute
wave: 10
depends_on:
  - 11-12
files_modified:
  - crates/bytecode/src/lib.rs
  - crates/vm/src/lib.rs
  - crates/vm/src/fast_path.rs
  - crates/vm/src/perf.rs
  - crates/vm/tests/perf_packet_d.rs
  - crates/vm/tests/perf_hotspot_attribution.rs
  - crates/benchmarks/src/contract.rs
  - crates/benchmarks/src/main.rs
  - crates/benchmarks/tests/hot_path_contract.rs
  - docs/engine-benchmarks.md
autonomous: true
gap_closure: true
requirements:
  - PERF-03
  - PERF-04
  - PERF-05
must_haves:
  truths:
    - Packet-h introduces a higher-impact lexical binding hot-path optimization that keeps canonical fallback semantics intact.
    - Packet-h parity and hotspot attribution clearly separate guard hits, guard misses, and slow-path fallback scans for identifier resolution.
    - Benchmark harness can produce contract-valid packet-h artifacts on locked profile settings without relaxing comparator strictness.
  artifacts:
    - crates/bytecode/src/lib.rs
    - crates/vm/src/lib.rs
    - crates/vm/src/fast_path.rs
    - crates/vm/src/perf.rs
    - crates/vm/tests/perf_packet_d.rs
    - crates/vm/tests/perf_hotspot_attribution.rs
    - crates/benchmarks/src/contract.rs
    - crates/benchmarks/src/main.rs
    - crates/benchmarks/tests/hot_path_contract.rs
  key_links:
    - Bytecode lexical-binding metadata must connect directly to VM fast-path resolution and never bypass canonical fallback on guard miss.
    - Packet-h counters emitted by VM must serialize through benchmark contract payload for machine-readability.
    - Packet-h benchmark toggle inference must be deterministic from candidate output path naming.
---

<objective>
Land the next high-impact gap-closure optimization packet before another authoritative closure verdict run.

Purpose: PERF-03 remains far from closure (`qjs-rs/quickjs-c = 6.236987x > 1.25x`), so incremental guard tuning is insufficient; Phase 11 needs a stronger lexical-binding hot-path cut with hard parity protection.
Output: Packet-h VM/bytecode fast path, parity + hotspot tests, and packet-h benchmark harness support.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-RESEARCH.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-12-SUMMARY.md
@docs/performance-closure-policy.md
@crates/bytecode/src/lib.rs
@crates/vm/src/lib.rs
@crates/vm/src/fast_path.rs
@crates/vm/src/perf.rs
@crates/vm/tests/perf_packet_d.rs
@crates/vm/tests/perf_hotspot_attribution.rs
@crates/benchmarks/src/contract.rs
@crates/benchmarks/src/main.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add packet-h parity and hotspot tests before fast-path implementation</name>
  <files>crates/vm/tests/perf_packet_d.rs, crates/vm/tests/perf_hotspot_attribution.rs</files>
  <behavior>
    - Test 1: packet-h enabled/disabled executions yield identical observable JS results on existing packet-d script families.
    - Test 2: with-scope, scope-mutation, and unknown-identifier flows increment packet-h miss/fallback counters instead of hit counters.
    - Test 3: stable lexical-loop workloads show packet-h fast-hit counters and reduced fallback-scan counters versus packet-g path.
  </behavior>
  <action>Define packet-h guard/parity expectations first, keeping all assertions semantics-first and benchmark-agnostic. Extend existing perf packet suites instead of creating ad-hoc one-off tests, and preserve packet-a..g test coverage unchanged.</action>
  <verify>
    <automated>cargo test -p vm perf_packet_d -- --nocapture && cargo test -p vm perf_hotspot_attribution -- --nocapture</automated>
  </verify>
  <done>Packet-h behavioral and attribution expectations are locked by deterministic tests before optimization code is added.</done>
</task>

<task type="auto">
  <name>Task 2: Implement packet-h lexical-binding slot fast path with guarded fallback</name>
  <files>crates/bytecode/src/lib.rs, crates/vm/src/lib.rs, crates/vm/src/fast_path.rs, crates/vm/src/perf.rs</files>
  <action>Implement a packet-h path that reduces repeated identifier-resolution scans by using lexical binding metadata/slot locality when safety preconditions hold (no with-scope hazards, valid scope generation, stable binding shape). On any guard miss or stale metadata, immediately use canonical identifier resolution and preserve current error/lookup ordering. Keep runtime core pure Rust and avoid C FFI or benchmark-specific shortcuts.</action>
  <verify>
    <automated>cargo test -p vm perf_packet_d -- --nocapture && cargo test -p vm perf_hotspot_attribution -- --nocapture && cargo test -p bytecode -- --nocapture</automated>
  </verify>
  <done>Packet-h fast path is integrated, guarded, parity-safe, and fully covered by packet/perf tests.</done>
</task>

<task type="auto">
  <name>Task 3: Wire packet-h benchmark contract/toggle and produce smoke artifact</name>
  <files>crates/benchmarks/src/contract.rs, crates/benchmarks/src/main.rs, crates/benchmarks/tests/hot_path_contract.rs, docs/engine-benchmarks.md</files>
  <action>Extend benchmark packet inference and hotspot payload mapping to include packet-h counters, keeping contract compatibility for existing packets. Generate a packet-h smoke artifact on locked local-dev profile with strict comparators and verify schema validity to ensure packet-h is ready for authoritative closure rerun.</action>
  <verify>
    <automated>cargo test -p benchmarks hot_path_contract -- --nocapture && cargo run -p benchmarks --bin benchmarks --release -- --profile local-dev --output target/benchmarks/engine-comparison.local-dev.packet-h.smoke.json --quickjs-path scripts/quickjs-wsl.cmd --strict-comparators && python .github/scripts/check_engine_benchmark_contract.py --input target/benchmarks/engine-comparison.local-dev.packet-h.smoke.json</automated>
  </verify>
  <done>Packet-h benchmark pipeline is contract-valid and execution-ready for authoritative gate evaluation.</done>
</task>

</tasks>

<verification>
Execution passes only when packet-h parity tests, hotspot attribution tests, and benchmark contract checks are all green on the same code revision.
</verification>

<success_criteria>
Phase 11 has a new high-impact packet-h optimization candidate with semantics-safe fallback, measurable attribution counters, and runnable benchmark pipeline support.
</success_criteria>

<output>
After completion, create `.planning/phases/11-hot-path-optimization-and-target-closure/11-13-SUMMARY.md`
</output>

