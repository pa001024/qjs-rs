---
phase: 11-hot-path-optimization-and-target-closure
plan: 05
type: execute
wave: 2
depends_on:
  - 11-04
files_modified:
  - crates/benchmarks/src/contract.rs
  - crates/benchmarks/src/main.rs
  - crates/benchmarks/tests/adapter_normalization.rs
  - crates/vm/src/lib.rs
  - crates/vm/tests/perf_packet_b.rs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
  - .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
autonomous: true
gap_closure: true
requirements:
  - PERF-03
  - PERF-04
  - PERF-05
must_haves:
  truths:
    - Governance gates required by the unresolved verification blocker pass in one run: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, and `cargo test`.
    - Packet-B/packet-C perf tests are stable in workspace runs (no hidden builtins/setup assumptions causing intermittent failures).
    - Final Phase 11 closure evidence records both performance gate and governance gate outcomes with exact commands/artifacts, including an explicit `Governance Gate Bundle: GREEN` verdict when all required commands pass.
    - After successful closure checks, traceability/status across `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `11-VERIFICATION.md` is synchronized to the same closed-state narrative for PERF-03/PERF-04/PERF-05.
    - If any governance/perf gate fails, traceability/status across `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `11-VERIFICATION.md` remains synchronized in explicit open/gap language (no premature closure text).
  artifacts:
    - crates/benchmarks/src/contract.rs
    - crates/benchmarks/src/main.rs
    - crates/benchmarks/tests/adapter_normalization.rs
    - crates/vm/src/lib.rs
    - crates/vm/tests/perf_packet_b.rs
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
    - .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
  key_links:
    - Benchmarks crate/test structure must avoid `dead_code`/lint explosions under `clippy --all-targets -D warnings`.
    - VM packet-B/packet-C tests must run through the same runtime bootstrap path used in workspace `cargo test`.
    - `11-TARGET-CLOSURE-EVIDENCE.md` must include governance and perf-target command outputs tied to packet-c artifacts, with explicit gate-bundle verdict text.
    - Traceability docs may be promoted to closed only when the same run that generated closure evidence has all governance + PERF-03 checks green.
---

<objective>
Close the second verification blocker by making governance gates and closure evidence fully green after packet-C integration.

Purpose: Phase 11 still has a gap where governance commands are failing; closure cannot be claimed unless both performance and quality gates pass together.
Output: lint/format/test debt resolution + stable workspace gates + updated target-closure evidence packet.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-04-SUMMARY.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
@crates/benchmarks/src/contract.rs
@crates/benchmarks/src/main.rs
@crates/benchmarks/tests/adapter_normalization.rs
@crates/vm/src/lib.rs
@crates/vm/tests/perf_packet_b.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminate formatting drift and stabilize packet-B workspace test bootstrap</name>
  <files>crates/vm/src/lib.rs, crates/vm/tests/perf_packet_b.rs, crates/benchmarks/src/main.rs, crates/benchmarks/src/contract.rs</files>
  <action>Apply canonical rustfmt formatting to all touched files and fix the current packet-B workspace failure (`UnknownIdentifier(\"Array\")`) by ensuring the test path installs required builtins/realm state before executing array/prototype scripts. Keep packet-B assertions unchanged for semantic parity and guard-counter expectations.</action>
  <verify>
    <automated>cargo test -p vm packet_b_array_dense_index_fast_path_guarding -- --exact && cargo fmt --check</automated>
  </verify>
  <done>Formatting gate is clean and packet-B guard test is deterministic in normal workspace test runs.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve clippy `-D warnings` blockers in benchmarks contract/test harness</name>
  <files>crates/benchmarks/src/contract.rs, crates/benchmarks/src/main.rs, crates/benchmarks/tests/adapter_normalization.rs</files>
  <action>Refactor benchmarks CLI parsing/test wiring to remove current clippy blockers without weakening guardrails: address `large_enum_variant` and `too_many_arguments` in `contract.rs`, and prevent `dead_code` floods in all-target test builds by moving reused benchmark helpers behind a test-support API instead of importing the full binary module. Keep existing behavior and command precedence semantics covered by tests.</action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings && cargo test -p benchmarks</automated>
  </verify>
  <done>Benchmarks crate is clippy-clean under strict settings and retains contract/adapter regression coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run governance + closure gates, then synchronize final traceability status</name>
  <files>.planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md</files>
  <action>After tasks 1-2, first regenerate the packet-c candidate artifact from current code (`cargo run -p benchmarks --release -- --profile local-dev --output target/benchmarks/engine-comparison.local-dev.packet-c.json --allow-missing-comparators`), then execute full governance and closure checks in one sequence: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test`, contract checker on the regenerated packet-c artifact, and `check_perf_target.py --require-qjs-lte-boa` against phase11 baseline. Update `11-TARGET-CLOSURE-EVIDENCE.md` with a final section that includes pass/fail status, full command transcript, blocker summary, and an explicit line `Governance Gate Bundle: GREEN` only when all three governance commands pass in that same run. If (and only if) the full bundle is green, sync status language in `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `11-VERIFICATION.md` so they consistently indicate PERF-03/PERF-04/PERF-05 are closed with packet-c evidence; if any gate fails, keep these traceability statuses in explicit open/gap state and document the failing command in evidence instead of force-closing.</action>
  <verify>
    <automated>python -c "import subprocess, sys; from pathlib import Path; cmds=[['cargo','fmt','--check'],['cargo','clippy','--all-targets','--','-D','warnings'],['cargo','test'],['cargo','run','-p','benchmarks','--release','--','--profile','local-dev','--output','target/benchmarks/engine-comparison.local-dev.packet-c.json','--allow-missing-comparators'],['python','.github/scripts/check_engine_benchmark_contract.py','--input','target/benchmarks/engine-comparison.local-dev.packet-c.json'],['python','.github/scripts/check_perf_target.py','--baseline','target/benchmarks/engine-comparison.local-dev.phase11-baseline.json','--candidate','target/benchmarks/engine-comparison.local-dev.packet-c.json','--require-qjs-lte-boa']]; rc=[subprocess.run(c).returncode for c in cmds]; green=all(code==0 for code in rc); e=Path('.planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md').read_text(encoding='utf-8'); r=Path('.planning/REQUIREMENTS.md').read_text(encoding='utf-8'); m=Path('.planning/ROADMAP.md').read_text(encoding='utf-8'); v=Path('.planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md').read_text(encoding='utf-8');\nif green:\n assert 'Governance Gate Bundle: GREEN' in e\n assert 'PERF-03 | Phase 11 | Completed (closed via packet-c evidence)' in r\n assert 'closure verified via 11-TARGET-CLOSURE-EVIDENCE.md' in m\n assert 'status: passed' in v\nelse:\n assert 'Governance Gate Bundle: GREEN' not in e\n assert ('status: gaps_found' in v) or ('status: failed' in v)\n assert (('open' in r.lower()) or ('gap' in r.lower())) and 'completed (closed via packet-c evidence)' not in r.lower()\n assert (('open' in m.lower()) or ('gap' in m.lower())) and 'closure verified via 11-target-closure-evidence.md' not in m.lower()\nsys.exit(0 if green else 1)"</automated>
  </verify>
  <done>Governance gates and PERF-03 closure gate are evidenced together in a single authoritative packet, and traceability/status docs are synchronized to match the final verified state (closed only on green bundle).</done>
</task>

</tasks>

<verification>
Planning-time expectation (revision/gap-closure mode): existing artifacts may still show open gaps (`status: gaps_found`, PERF-03 open, traceability caveats) until this plan executes; that is expected and not a blocker for approving plan design.

Post-execution acceptance requires all three governance commands and both closure-check commands to pass in one run, with results documented in `11-TARGET-CLOSURE-EVIDENCE.md`, explicit `Governance Gate Bundle: GREEN` evidence, and synchronized status across REQUIREMENTS/ROADMAP/11-VERIFICATION.
</verification>

<success_criteria>
Planning output is accepted when this plan explicitly targets the remaining gaps without pretending they are already closed.

Execution output is successful when the two unresolved Phase 11 verification blockers are closed: aggregate PERF-03 gate passes, governance gates are green with reproducible evidence, and traceability/status documents are aligned to the same closed-state result.
</success_criteria>

<output>
After completion, create `.planning/phases/11-hot-path-optimization-and-target-closure/11-05-SUMMARY.md`.
</output>
