---
phase: 11-hot-path-optimization-and-target-closure
plan: 16
type: execute
wave: 13
depends_on:
  - 11-15
files_modified:
  - docs/engine-benchmarks.md
  - .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
  - .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
gap_closure: true
requirements:
  - PERF-03
  - PERF-04
  - PERF-05
must_haves:
  truths:
    - Authoritative packet-i candidate runs through the locked governance + benchmark + quickjs-ratio checker sequence with machine-checkable provenance.
    - Closure and traceability docs cite one packet-i bundle source with exact aggregate means, ratio, and checker exit status.
    - PERF-05 pure-Rust boundary evidence is refreshed in the same packet-i cycle and remains explicit even when PERF-03 is still open.
  artifacts:
    - target/benchmarks/engine-comparison.local-dev.packet-i.json
    - target/benchmarks/phase11-closure-bundle.packet-i.json
    - .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
    - .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  key_links:
    - PERF-03 checker must compare locked baseline `engine-comparison.local-dev.phase11-baseline.json` against packet-i candidate using `--require-qjs-lte-quickjs-ratio 1.25`.
    - `phase11-closure-bundle.packet-i.json` must bind command outcomes, candidate hash, aggregate means, and ratio from the same run.
    - Verification, roadmap, requirements, and state updates must derive from packet-i bundle values only.
---

<objective>
Run the authoritative packet-i closure verdict workflow and synchronize all Phase 11 traceability outputs to that single verdict source.

Purpose: Phase 11 remains blocked on PERF-03; after packet-i lands, closure status must be decided from one machine-checkable packet-i run with no mixed-run wording.
Output: Authoritative packet-i benchmark candidate, packet-i closure bundle, refreshed boundary evidence, and synchronized verification/roadmap/requirements/state docs.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-RESEARCH.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-15-SUMMARY.md
@docs/performance-closure-policy.md
@docs/engine-benchmarks.md
@crates/benchmarks/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute authoritative packet-i governance and benchmark command bundle</name>
  <files>docs/engine-benchmarks.md</files>
  <action>Run authoritative sequence on locked profile: `cargo fmt --check`, `cargo clippy -p vm -p benchmarks -- -D warnings`, packet perf test suites, strict-comparator benchmark generation for `target/benchmarks/engine-comparison.local-dev.packet-i.json`, benchmark contract validation, and PERF-03 quickjs-ratio checker against phase11 baseline. Capture checker stdout/stderr and exit code into packet-i verdict logs even when threshold fails.</action>
  <verify>
    <automated>cargo fmt --check && cargo clippy -p vm -p benchmarks -- -D warnings && cargo test -p vm perf_packet_d -- --nocapture && cargo test -p vm perf_hotspot_attribution -- --nocapture && cargo run -p benchmarks --bin benchmarks --release -- --profile local-dev --output target/benchmarks/engine-comparison.local-dev.packet-i.json --quickjs-path scripts/quickjs-wsl.cmd --strict-comparators && python .github/scripts/check_engine_benchmark_contract.py --input target/benchmarks/engine-comparison.local-dev.packet-i.json && python -c "import json,pathlib,subprocess,sys; out_path=pathlib.Path('target/benchmarks/perf-target.packet-i.stdout.log'); err_path=pathlib.Path('target/benchmarks/perf-target.packet-i.stderr.log'); verdict_path=pathlib.Path('target/benchmarks/perf-target.packet-i.verdict.json'); out_path.parent.mkdir(parents=True, exist_ok=True); cmd=['python','.github/scripts/check_perf_target.py','--baseline','target/benchmarks/engine-comparison.local-dev.phase11-baseline.json','--candidate','target/benchmarks/engine-comparison.local-dev.packet-i.json','--require-qjs-lte-quickjs-ratio','1.25']; p=subprocess.run(cmd,capture_output=True,text=True); out_path.write_text(p.stdout,encoding='utf-8'); err_path.write_text(p.stderr,encoding='utf-8'); errors=[line[2:] for line in p.stderr.splitlines() if line.startswith('- ')]; threshold_only=bool(errors) and all('require-qjs-lte-quickjs-ratio failed' in line for line in errors); status='passed' if p.returncode==0 else ('threshold_fail_expected' if p.returncode==1 and threshold_only else 'checker_or_runtime_error'); verdict={'status':status,'exit_code':p.returncode,'stdout_log':out_path.as_posix(),'stderr_log':err_path.as_posix()}; verdict_path.write_text(json.dumps(verdict,indent=2),encoding='utf-8'); print(json.dumps(verdict)); sys.exit(0 if status in ('passed','threshold_fail_expected') else (p.returncode or 1))"</automated>
  </verify>
  <done>Authoritative packet-i run outputs are captured with complete governance transcript, contract validation, and quickjs-ratio checker verdict artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Emit packet-i closure bundle and synchronize verification traceability docs</name>
  <files>.planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md, .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Generate `target/benchmarks/phase11-closure-bundle.packet-i.json` including baseline path, candidate path, candidate SHA256 hash, governance command results, checker verdict metadata, aggregate means, and computed `qjs-rs/quickjs-c` ratio. Update evidence/verification/requirements/roadmap/state to quote packet-i bundle values exactly. If packet-i passes ratio gate, move wording to closure-ready; otherwise preserve explicit blocker text with concrete numbers.</action>
  <verify>
    <automated>python -c "import json,sys; p='target/benchmarks/phase11-closure-bundle.packet-i.json'; d=json.load(open(p,'r',encoding='utf-8')); assert d['candidate'].endswith('engine-comparison.local-dev.packet-i.json'); assert 'qjs_rs_to_quickjs_ratio' in d['aggregate']; print('bundle-ok', d['aggregate']['qjs_rs_to_quickjs_ratio'])" && rg --line-number "packet-i|phase11-closure-bundle.packet-i.json|qjs-rs/quickjs-c|PERF-03|PERF-04|PERF-05|status:|gaps_found|passed" .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md</automated>
  </verify>
  <done>All Phase 11 traceability documents reference one packet-i machine-checkable closure verdict source.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh PERF-05 runtime-core boundary scan in packet-i cycle</name>
  <files>.planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md, .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md</files>
  <action>Run runtime-core boundary scan for `extern \"C\"` and `unsafe` over `crates/vm`, `crates/runtime`, `crates/bytecode`, and `crates/builtins`; write the transcript to `target/benchmarks/perf05-boundary-scan.packet-i.log`, and update packet-i evidence/verification sections to keep PERF-05 auditable in the same verdict cycle.</action>
  <verify>
    <automated>python -c "import pathlib,subprocess,sys; out_path=pathlib.Path('target/benchmarks/perf05-boundary-scan.packet-i.log'); out_path.parent.mkdir(parents=True, exist_ok=True); cmd=['rg','--line-number','extern\\\\s+\"C\"|\\\\bunsafe\\\\b','crates/vm','crates/runtime','crates/bytecode','crates/builtins']; p=subprocess.run(cmd,capture_output=True,text=True); text=(p.stdout or '') + (p.stderr or ''); out_path.write_text(text,encoding='utf-8'); print(text,end=''); sys.exit(0 if p.returncode==1 else (1 if p.returncode==0 else p.returncode))" && rg --line-number "runtime-core|PERF-05|boundary|packet-i|extern\\\\s+\\\"C\\\"|unsafe" .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md</automated>
  </verify>
  <done>Packet-i verdict packet includes fresh runtime-core boundary evidence and PERF-05 remains explicitly traceable.</done>
</task>

</tasks>

<verification>
Execution is accepted only if packet-i authoritative bundle, checker verdict logs, and synchronized docs all point to the same candidate artifact and measured ratio.
</verification>

<success_criteria>
Phase 11 has an authoritative packet-i closure verdict path with explicit PASS/FAIL status for PERF-03 and refreshed evidence for PERF-04/PERF-05.
</success_criteria>

<output>
After completion, create `.planning/phases/11-hot-path-optimization-and-target-closure/11-16-SUMMARY.md`
</output>

