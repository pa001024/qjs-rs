---
phase: 11-hot-path-optimization-and-target-closure
plan: 07
type: execute
wave: 4
depends_on:
  - 11-06
files_modified:
  - crates/vm/src/fast_path.rs
  - crates/vm/src/lib.rs
  - crates/vm/src/perf.rs
  - crates/vm/tests/perf_hotspot_attribution.rs
  - crates/vm/tests/perf_packet_b.rs
  - crates/vm/tests/perf_packet_c.rs
  - crates/vm/tests/perf_packet_d.rs
  - target/benchmarks/phase11-closure-bundle.json
  - .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
autonomous: true
gap_closure: true
requirements:
  - PERF-03
  - PERF-04
  - PERF-05
must_haves:
  truths:
    - Governance gates required by latest verification (`cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test`) must run in one authoritative sequence that writes a machine-readable closure bundle artifact.
    - Workspace formatting drift in VM packet/perf files is removed so `cargo fmt --check` is no longer the blocker.
    - Final closure evidence must include packet-d candidate metrics plus explicit governance-bundle verdict text, using closed-state language only when all governance + PERF-03 checks pass in the same run.
    - Task 3 must derive closure state only from the Task 2 machine-readable run artifact (single authoritative provenance), without rerunning governance/perf gate commands.
    - Failure-path synchronization is mandatory: if any gate remains red, REQUIREMENTS/ROADMAP/STATE/11-VERIFICATION must stay in explicit open-gap wording.
  artifacts:
    - crates/vm/src/fast_path.rs
    - crates/vm/src/lib.rs
    - crates/vm/src/perf.rs
    - crates/vm/tests/perf_hotspot_attribution.rs
    - crates/vm/tests/perf_packet_b.rs
    - crates/vm/tests/perf_packet_c.rs
    - crates/vm/tests/perf_packet_d.rs
    - target/benchmarks/phase11-closure-bundle.json
    - .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
  key_links:
    - VM packet/perf source + tests must remain rustfmt-clean so governance runs do not fail on formatting drift.
    - `target/benchmarks/phase11-closure-bundle.json` must capture ordered command provenance with `timestamp_utc`, packet-d artifact hash, and per-command return codes (`rc`).
    - `11-TARGET-CLOSURE-EVIDENCE.md` must capture the exact command transcript and include `Governance Gate Bundle: GREEN` only on a fully green run.
    - Traceability docs may move to closed state only if the Task 2 provenance artifact shows governance + PERF-03 checks jointly green; otherwise they must remain synchronized in open state.
---

<objective>
Close the remaining governance and traceability blockers by running an authoritative final bundle after packet-D and syncing status docs correctly for both success and failure outcomes.

Purpose: Latest verification still reports two blockers (PERF-03 and `cargo fmt --check`); closure claims are invalid until governance + perf gates are jointly green.
Output: rustfmt-clean VM packet files + final packet-d closure transcript + synchronized roadmap/requirements/state/verification status.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-06-SUMMARY.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-PACKET-D-EVIDENCE.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
@crates/vm/src/fast_path.rs
@crates/vm/src/lib.rs
@crates/vm/src/perf.rs
@crates/vm/tests/perf_hotspot_attribution.rs
@crates/vm/tests/perf_packet_b.rs
@crates/vm/tests/perf_packet_c.rs
@crates/vm/tests/perf_packet_d.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminate VM formatting drift and re-validate packet perf test stability</name>
  <files>crates/vm/src/fast_path.rs, crates/vm/src/lib.rs, crates/vm/src/perf.rs, crates/vm/tests/perf_hotspot_attribution.rs, crates/vm/tests/perf_packet_b.rs, crates/vm/tests/perf_packet_c.rs, crates/vm/tests/perf_packet_d.rs</files>
  <action>Apply canonical rustfmt formatting to the currently drifting VM packet/perf files and keep semantic behavior unchanged. Re-run packet-B/packet-C/packet-D VM perf suites to ensure formatting and imports cleanup did not alter guard behavior, parity, or benchmark toggles.</action>
  <verify>
    <automated>cargo fmt --check && cargo test -p vm perf_packet_b -- --nocapture && cargo test -p vm perf_packet_c -- --nocapture && cargo test -p vm perf_packet_d -- --nocapture</automated>
  </verify>
  <done>`cargo fmt --check` passes and packet perf suites remain green after formatting cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: Execute authoritative governance + packet-d closure command bundle</name>
  <files>target/benchmarks/phase11-closure-bundle.json, .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md</files>
  <action>Regenerate `target/benchmarks/engine-comparison.local-dev.packet-d.json`, then run governance + closure commands in one ordered sequence: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test`, contract checker for packet-d artifact, and `check_perf_target.py --require-qjs-lte-boa`. Persist an authoritative machine-readable provenance artifact at `target/benchmarks/phase11-closure-bundle.json` that records `timestamp_utc`, packet-d candidate artifact `hash`/`sha256`, and per-command `rc` (plus ordered command metadata). Append a dated section to `11-TARGET-CLOSURE-EVIDENCE.md` that references this artifact and includes exact command outputs, packet-d aggregate means, and explicit gate verdict lines for governance and PERF-03.</action>
  <verify>
    <automated>python -c "import hashlib, json, subprocess, datetime; from pathlib import Path; bundle_path=Path('target/benchmarks/phase11-closure-bundle.json'); bundle_path.parent.mkdir(parents=True, exist_ok=True); packet_path=Path('target/benchmarks/engine-comparison.local-dev.packet-d.json'); cmds=[('bench_generate',['cargo','run','-p','benchmarks','--release','--','--profile','local-dev','--output',str(packet_path),'--allow-missing-comparators']),('fmt',['cargo','fmt','--check']),('clippy',['cargo','clippy','--all-targets','--','-D','warnings']),('test',['cargo','test']),('contract',['python','.github/scripts/check_engine_benchmark_contract.py','--input',str(packet_path)]),('perf_target',['python','.github/scripts/check_perf_target.py','--baseline','target/benchmarks/engine-comparison.local-dev.phase11-baseline.json','--candidate',str(packet_path),'--require-qjs-lte-boa'])]; results=[{'name':name,'cmd':cmd,'rc':subprocess.run(cmd).returncode} for name,cmd in cmds]; packet_exists=packet_path.exists(); packet_hash=hashlib.sha256(packet_path.read_bytes()).hexdigest() if packet_exists else None; bundle={'timestamp_utc':datetime.datetime.now(datetime.timezone.utc).replace(microsecond=0).isoformat().replace('+00:00','Z'),'packet_d_artifact':{'path':str(packet_path).replace('\\\\','/'),'exists':packet_exists,'hash':packet_hash,'sha256':packet_hash},'commands':results,'overall_rc':0 if all(r['rc']==0 for r in results) else 1}; bundle_path.write_text(json.dumps(bundle, indent=2)+'\n', encoding='utf-8')"</automated>
  </verify>
  <done>Evidence doc contains a single authoritative transcript for governance + PERF-03 outcomes against packet-d artifact.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize traceability docs with strict success/failure-path rules</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md, .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md</files>
  <action>Update REQUIREMENTS/ROADMAP/STATE/11-VERIFICATION to match the final bundle outcome recorded in `target/benchmarks/phase11-closure-bundle.json` from Task 2 (single authoritative source). Closed-state wording is allowed only if that artifact shows governance (`fmt+clippy+test`) and PERF-03 checker all green in the same run; otherwise retain explicit open-gap wording and blocker details. Keep wording synchronized across all traceability docs (no mixed closed/open narrative). Do not rerun governance/perf commands in this task.</action>
  <verify>
    <automated>python -c "import json, re, sys; from pathlib import Path; bundle_path=Path('target/benchmarks/phase11-closure-bundle.json'); assert bundle_path.exists(), 'missing Task 2 run artifact'; bundle=json.loads(bundle_path.read_text(encoding='utf-8')); assert bundle.get('timestamp_utc'), 'bundle missing timestamp_utc'; packet=bundle.get('packet_d_artifact') or {}; assert packet.get('hash') and packet.get('sha256'), 'bundle missing packet_d_artifact hash fields'; commands=bundle.get('commands') or []; assert commands, 'bundle missing commands'; rc_by_name={c.get('name'): c.get('rc') for c in commands}; required=('fmt','clippy','test','contract','perf_target'); missing=[name for name in required if name not in rc_by_name]; assert not missing, f'bundle missing command results: {missing}'; green=all(rc_by_name[name]==0 for name in required); evidence=Path('.planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md').read_text(encoding='utf-8'); req_raw=Path('.planning/REQUIREMENTS.md').read_text(encoding='utf-8'); road_raw=Path('.planning/ROADMAP.md').read_text(encoding='utf-8'); state_raw=Path('.planning/STATE.md').read_text(encoding='utf-8'); ver_raw=Path('.planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md').read_text(encoding='utf-8'); req=req_raw.lower(); road=road_raw.lower(); state=state_raw.lower(); ver=ver_raw.lower();
def must(pattern, text, label):
 assert re.search(pattern, text, re.IGNORECASE | re.MULTILINE), f'{label} missing pattern: {pattern}'
if green:
 must(r'Governance Gate Bundle:\\s*GREEN', evidence, 'evidence governance verdict')
 must(r'PERF-03[^\\n]*(GREEN|PASS)', evidence, 'evidence perf-03 verdict')
 for rid in ('03','04','05'):
  must(rf'- \\[x\\]\\s+\\*\\*PERF-{rid}\\*\\*', req_raw, f'requirement PERF-{rid} checkbox closed')
  must(rf'\\|\\s*PERF-{rid}\\s*\\|\\s*Phase\\s*11\\s*\\|\\s*Completed\\b', req_raw, f'requirement PERF-{rid} traceability closed')
 must(r'- \\[x\\]\\s+\\*\\*Phase 11: Hot-Path Optimization and Target Closure\\*\\*', road_raw, 'roadmap phase-11 closed')
 must(r'status:\\s*passed', ver, 'verification status passed')
 must(r'phase 11 closure:\\s*\\*\\*closed\\*\\*', ver, 'verification phase-11 closed')
 must(r'- \\[x\\]\\s*Phase 11: Hot-Path Optimization and Target Closure', state_raw, 'state phase-11 closed checklist')
 must(r'^status:\\s*.*phase\\s*11.*(closed|completed)', state_raw, 'state metadata phase-11 closure')
else:
 assert 'Governance Gate Bundle: GREEN' not in evidence
 for rid in ('03','04','05'):
  must(rf'- \\[ \\]\\s+\\*\\*PERF-{rid}\\*\\*', req_raw, f'requirement PERF-{rid} remains open')
 assert ('open' in req or 'gap' in req)
 assert ('open' in road or 'gap' in road)
 assert ('status: gaps_found' in ver) or ('status: failed' in ver)
 assert ('open' in state or 'gap' in state)
sys.exit(0)"</automated>
  </verify>
  <done>All traceability docs consistently reflect the same final closure state, and failure-path sync is preserved when any required gate remains red.</done>
</task>

</tasks>

<verification>
Planning-time expectation (gap-closure mode): docs may remain open-gap before execution; this is expected.

Post-execution acceptance requires one authoritative run containing governance + PERF-03 checks, explicit verdict text in `11-TARGET-CLOSURE-EVIDENCE.md`, and synchronized traceability documents that follow strict success/failure-path state rules.
</verification>

<success_criteria>
Planning output is accepted when this plan explicitly closes the known governance blocker (`cargo fmt --check`) and codifies strict state-sync behavior for both green and red outcomes.

Execution output is successful when governance and PERF-03 are jointly green and docs are promoted consistently; otherwise execution must still leave docs in a synchronized open-gap state with explicit blocker evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/11-hot-path-optimization-and-target-closure/11-07-SUMMARY.md`.
</output>
