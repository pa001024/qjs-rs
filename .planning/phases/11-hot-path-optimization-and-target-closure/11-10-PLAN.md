---
phase: 11-hot-path-optimization-and-target-closure
plan: 10
type: execute
wave: 7
depends_on:
  - 11-09
files_modified:
  - crates/vm/src/fast_path.rs
  - crates/vm/src/lib.rs
  - crates/vm/src/perf.rs
  - crates/vm/tests/perf_packet_d.rs
  - crates/vm/tests/perf_hotspot_attribution.rs
  - crates/benchmarks/src/main.rs
  - docs/engine-benchmarks.md
  - .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
  - .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
autonomous: true
gap_closure: true
requirements:
  - PERF-03
  - PERF-04
  - PERF-05
must_haves:
  truths:
    - Governance gates (cargo fmt --check, clippy, tests) are green to enable joint closure evidence.
    - A new packet-level optimization attempt targets different/remaining hotspot families with guarded fallback semantics.
    - Authoritative benchmark artifacts include quickjs comparator and are validated by contract + quickjs-ratio PERF-03 checker.
  artifacts:
    - crates/vm/src/fast_path.rs
    - crates/vm/src/lib.rs
    - crates/vm/src/perf.rs
    - crates/vm/tests/perf_packet_d.rs
    - crates/vm/tests/perf_hotspot_attribution.rs
    - crates/benchmarks/src/main.rs
    - .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
    - .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
  key_links:
    - VM packet fast-path guard logic must stay parity-safe under with/mutation/accessor/prototype transitions.
    - Benchmark harness packet metadata must align with checker policy from 11-08.
    - Evidence + verification docs must report measured ratio and explicit gate verdict from same candidate artifact.
---

<objective>
Fix governance failures and execute another optimization loop to close the remaining PERF-03 gap.

Purpose: Phase 11 remains open because cargo fmt check fails AND performance is far from target; this plan fixes governance first, then attempts another optimization.
Output: fmt-clean codebase + packet candidate code changes + parity tests + contract-valid benchmark evidence + updated verification verdict.
</objective>

<execution_context>
@C:/Users/Administrator/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-09-SUMMARY.md
@.planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md
@crates/vm/src/fast_path.rs
@crates/vm/src/lib.rs
@crates/vm/src/perf.rs
@crates/vm/tests/perf_packet_d.rs
@crates/vm/tests/perf_hotspot_attribution.rs
@crates/benchmarks/src/main.rs
@docs/engine-benchmarks.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix cargo fmt failures and verify governance is green</name>
  <files>crates/vm/src/fast_path.rs, crates/vm/src/lib.rs, crates/vm/src/perf.rs</files>
  <action>Run cargo fmt on VM and benchmark crates to fix formatting drift. Verify cargo fmt --check passes, clippy passes, and targeted vm tests pass before proceeding to optimization tasks.</action>
  <verify>
    <automated>cargo fmt --check && cargo clippy -p vm -p benchmarks -- -D warnings && cargo test -p vm perf_packet_d -- --nocapture</automated>
  </verify>
  <done>Governance gates (fmt, clippy, tests) are green and ready for authoritative closure evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement another guarded hotspot optimization candidate</name>
  <files>crates/vm/src/fast_path.rs, crates/vm/src/lib.rs, crates/vm/src/perf.rs</files>
  <behavior>
    - Test 1: optimization-enabled and optimization-disabled runs produce identical observable JS behavior on existing packet parity suites.
    - Test 2: hotspot counters expose meaningful deltas for the targeted path.
    - Test 3: fallback path remains active whenever guard predicates fail.
  </behavior>
  <action>Choose a different or complementary hotspot from current telemetry (not the same as packet-e identifier-call dispatch) and implement a guarded optimization in packet path logic. Focus on reducing allocations, branch overhead, or metadata lookups. Keep all behavior behind existing packet guard rules.</action>
  <verify>
    <automated>cargo test -p vm perf_packet_d -- --nocapture && cargo test -p vm perf_hotspot_attribution -- --nocapture</automated>
  </verify>
  <done>Optimization candidate is implemented with guard-safe fallback and passes VM parity/hotspot tests.</done>
</task>

<task type="auto">
  <name>Task 3: Generate authoritative candidate artifact and PERF-03 quickjs-ratio verdict</name>
  <files>crates/benchmarks/src/main.rs, docs/engine-benchmarks.md, .planning/phases/11-hot-path-optimization-and-target-closure/11-TARGET-CLOSURE-EVIDENCE.md, .planning/phases/11-hot-path-optimization-and-target-closure/11-VERIFICATION.md</files>
  <action>Run the locked benchmark workflow for the new candidate using explicit quickjs comparator path. Validate JSON contract, then evaluate perf target against Phase 11 baseline using quickjs-ratio checker mode. Append evidence with candidate hash, aggregate means, ratio, and explicit PASS/FAIL gate verdict.</action>
  <verify>
    <automated>cargo run -p benchmarks --bin benchmarks --release -- --profile local-dev --output target/benchmarks/engine-comparison.local-dev.packet-f.json --quickjs-path scripts/quickjs-wsl.cmd --strict-comparators && python .github/scripts/check_engine_benchmark_contract.py --input target/benchmarks/engine-comparison.local-dev.packet-f.json && python .github/scripts/check_perf_target.py --baseline target/benchmarks/engine-comparison.local-dev.phase11-baseline.json --candidate target/benchmarks/engine-comparison.local-dev.packet-f.json --require-qjs-lte-quickjs-ratio 1.25</automated>
  </verify>
  <done>New candidate has reproducible, contract-valid benchmark evidence and a machine-checked PERF-03 verdict.</done>
</task>

</tasks>

<verification>
Execution passes only if governance is green and candidate evidence includes contract + quickjs-ratio checker outputs from the same run.
</verification>

<success_criteria>
Phase 11 obtains another evidence-backed optimization attempt with explicit measured impact and authoritative PERF-03 decision under the active target policy.
</success_criteria>

<output>
After completion, create `.planning/phases/11-hot-path-optimization-and-target-closure/11-10-SUMMARY.md`
</output>
