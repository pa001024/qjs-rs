---
phase: 04-es-module-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - crates/parser/src/lib.rs
  - crates/bytecode/src/lib.rs
  - crates/vm/src/lib.rs
autonomous: true
requirements:
  - MOD-01
  - MOD-02
must_haves:
  truths:
    - Static import/export graphs complete parse -> instantiate -> evaluate through a deterministic VM pipeline.
    - Cyclic graphs execute with deterministic traversal and stable failure propagation.
    - Re-import of evaluated modules uses cache without re-evaluating side effects.
  artifacts:
    - crates/parser/src/lib.rs
    - crates/bytecode/src/lib.rs
    - crates/vm/src/lib.rs
  key_links:
    - 04-01 module state/cache APIs are the only module record mutation path.
    - Instantiate and evaluate phases use explicit visitation markers to avoid duplicate work in cycles.
    - Cycle and failure propagation semantics are asserted with deterministic ordering/error tests.
---

<objective>
Implement MOD-01 graph lifecycle execution and MOD-02 cycle/cache behavior on top of the module cache foundation.

Purpose: Deliver full static ESM lifecycle flow with deterministic semantics and error propagation.
Output: Parser/bytecode module entry points, VM instantiate/evaluate graph execution, and deterministic cycle/cache behavior tests.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-es-module-lifecycle/04-CONTEXT.md
@.planning/phases/04-es-module-lifecycle/04-RESEARCH.md
@.planning/phases/04-es-module-lifecycle/04-01-SUMMARY.md
@crates/parser/src/lib.rs
@crates/bytecode/src/lib.rs
@crates/vm/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add module parse/compile entry points for static import-export flow</name>
  <files>crates/parser/src/lib.rs, crates/bytecode/src/lib.rs</files>
  <action>Introduce parser/bytecode entry points for module sources (separate from script path), including static import/export declaration handling needed for instantiate/evaluate flow. Keep script path behavior unchanged and avoid scope/hoist regressions.</action>
  <verify>
    <automated>cargo test -p parser module_parse_baseline -- --exact</automated>
  </verify>
  <done>Module parse/compile path exists and can represent static import/export graphs without regressing script path tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic instantiate and evaluate graph traversal</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Add VM module graph pipeline: resolve/load -> parse -> instantiate dependencies -> evaluate in deterministic order. Reuse module cache records from 04-01 and enforce single execution per evaluated module.</action>
  <verify>
    <automated>cargo test -p vm module_graph_instantiate_evaluate -- --exact</automated>
  </verify>
  <done>Static module graphs execute through deterministic instantiate/evaluate phases with cache-aware single execution.</done>
</task>

<task type="auto">
  <name>Task 3: Handle cycles and failure propagation with deterministic replay</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Implement cycle-aware traversal guards and failure propagation rules so cyclic graphs either evaluate deterministically or fail deterministically. Ensure failed cache entries replay stable error behavior on re-import.</action>
  <verify>
    <automated>cargo test -p vm module_cycle_and_failure_replay -- --exact</automated>
  </verify>
  <done>Cycle behavior and failure replay are deterministic and covered by focused regression tests.</done>
</task>

</tasks>

<verification>
Run parser/bytecode/vm targeted tests and a VM full pass to confirm module lifecycle behavior, cycle handling, and cache replay semantics are stable.
</verification>

<success_criteria>
Static ESM graphs are supported end-to-end with deterministic instantiate/evaluate order, cycle behavior, and cache reuse/failure replay.
</success_criteria>

<output>
After completion, create `.planning/phases/04-es-module-lifecycle/04-02-SUMMARY.md`.
</output>
