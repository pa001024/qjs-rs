---
phase: 04-es-module-lifecycle
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - crates/vm/src/lib.rs
  - crates/test-harness/tests/module_lifecycle.rs
  - crates/test-harness/tests/test262_lite.rs
autonomous: true
requirements:
  - MOD-01
  - MOD-02
must_haves:
  truths:
    - Harness-level tests validate parse/instantiate/evaluate lifecycle behavior for static module graphs.
    - Cache reuse and cycle/failure replay behavior is observable and deterministic in integration tests.
    - Module lifecycle references remain GC-safe through cache-root candidate management.
  artifacts:
    - crates/vm/src/lib.rs
    - crates/test-harness/tests/module_lifecycle.rs
    - crates/test-harness/tests/test262_lite.rs
  key_links:
    - 04-01 cache/root-candidate model is validated under module lifecycle stress.
    - 04-02 graph execution semantics are exercised from harness-level observable outcomes.
    - Deterministic typed error behavior for parse/link/evaluate failures is locked with regression assertions.
---

<objective>
Close Phase 4 by proving module lifecycle correctness and determinism through VM + harness verification.

Purpose: Convert MOD-01/MOD-02 from implementation claims to stable integration guarantees.
Output: Dedicated module lifecycle integration suite and VM hardening tests for cache/cycle/error/GC behavior.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-es-module-lifecycle/04-CONTEXT.md
@.planning/phases/04-es-module-lifecycle/04-RESEARCH.md
@.planning/phases/04-es-module-lifecycle/04-01-SUMMARY.md
@.planning/phases/04-es-module-lifecycle/04-02-SUMMARY.md
@crates/vm/src/lib.rs
@crates/test-harness/tests/test262_lite.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add module lifecycle integration suite in test-harness</name>
  <files>crates/test-harness/tests/module_lifecycle.rs</files>
  <action>Create a dedicated integration test file for static module lifecycle scenarios: baseline import/export graph execution, cache reuse (no duplicate side effects), and cycle execution order/failure behavior. Keep assertions user-observable via results/order tokens and typed errors.</action>
  <verify>
    <automated>cargo test -p test-harness --test module_lifecycle</automated>
  </verify>
  <done>Harness tests provide deterministic integration coverage for core module lifecycle behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Harden VM deterministic error mapping and failure replay paths</name>
  <files>crates/vm/src/lib.rs, crates/test-harness/tests/test262_lite.rs</files>
  <action>Add/adjust VM and harness assertions for deterministic parse/link/evaluate failure categories and cache replay behavior on repeated imports of failed modules. Ensure message-shape/type assertions are stable and reproducible.</action>
  <verify>
    <automated>cargo test -p vm module_error_replay_determinism -- --exact</automated>
  </verify>
  <done>Module failure and replay semantics are deterministic and regression-tested across VM and harness layers.</done>
</task>

<task type="auto">
  <name>Task 3: Validate GC root integrity for cached module records</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Add GC-focused VM tests that keep module records reachable through cache root candidate lifecycle during load/evaluate, run collections before/after transitions, and assert release/reclaim behavior after deterministic eviction points.</action>
  <verify>
    <automated>cargo test -p vm module_cache_gc_root_integrity -- --exact</automated>
  </verify>
  <done>Cached module references are GC-safe while active and reclaimable after deterministic release points.</done>
</task>

</tasks>

<verification>
Run `cargo test -p vm` and `cargo test -p test-harness --test module_lifecycle`. Confirm deterministic parse/instantiate/evaluate behavior, cache reuse, cycle handling, failure replay, and module cache GC root safety.
</verification>

<success_criteria>
Phase 4 closes with end-to-end deterministic module lifecycle guarantees for MOD-01 and MOD-02.
</success_criteria>

<output>
After completion, create `.planning/phases/04-es-module-lifecycle/04-03-SUMMARY.md`.
</output>
