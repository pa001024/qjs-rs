---
phase: 02-runtime-safety-and-root-integrity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/test-harness/tests/test262_lite.rs
  - crates/test-harness/src/bin/test262-run.rs
  - crates/test-harness/fixtures/test262-lite/gc-guard.baseline
autonomous: true
requirements:
  - MEM-01
must_haves:
  truths:
    - Default profile runs keep GC counters at zero when runtime and auto GC are disabled.
    - Stress profile runs keep GC accounting balanced and satisfy runtime-collection and reclaimed-object thresholds.
    - GC guard failures are deterministic and actionable when baseline expectations are violated.
  artifacts:
    - crates/test-harness/tests/test262_lite.rs
    - crates/test-harness/src/bin/test262-run.rs
    - crates/test-harness/fixtures/test262-lite/gc-guard.baseline
  key_links:
    - test262 integration tests execute both default and stress SuiteOptions profiles.
    - test262-run guard logic checks the same GC fields produced by SuiteSummary.
    - baseline fixture values map directly to CLI expectation checks.
---

<objective>
Stabilize the MEM-01 verification gate by hardening dual-profile GC checks in the harness and CLI guard path.

Purpose: Keep root-integrity regressions observable in CI-style runs before Phase 3 async/module work lands.
Output: Test-harness profile tests plus deterministic GC guard baseline checks.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-runtime-safety-and-root-integrity/02-CONTEXT.md
@.planning/phases/02-runtime-safety-and-root-integrity/02-RESEARCH.md
@docs/gc-test-plan.md
@crates/test-harness/tests/test262_lite.rs
@crates/test-harness/src/bin/test262-run.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand test262-lite integration tests to cover default and stress GC profiles</name>
  <files>crates/test-harness/tests/test262_lite.rs</files>
  <action>Split coverage into explicit default-profile and stress-profile assertions. Keep existing stress invariants (runtime collections, accounting balance, reclaimed objects, runtime ratio), and add a default-profile assertion set that enforces zero collections when GC toggles are disabled.</action>
  <verify>
    <automated>cargo test -p test-harness --test test262_lite</automated>
  </verify>
  <done>Integration tests fail on profile drift and pass with deterministic expectations in both profiles.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten GC guard parsing and expectation checks in test262-run</name>
  <files>crates/test-harness/src/bin/test262-run.rs</files>
  <action>Add or extend unit tests around baseline parsing and guard evaluation so accounting imbalance and threshold misses produce deterministic failure messages. Preserve existing CLI flag semantics and baseline-override precedence.</action>
  <verify>
    <automated>cargo test -p test-harness --bin test262-run</automated>
  </verify>
  <done>Guard logic and parser behavior are regression-locked with deterministic pass/fail outcomes.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh GC guard baseline fixture to match profile gate expectations</name>
  <files>crates/test-harness/fixtures/test262-lite/gc-guard.baseline</files>
  <action>Update baseline threshold values to align with current stress-profile guard policy and keep comments concise about each threshold intent. Ensure values are strict enough to catch regressions but stable for repeatable CI runs.</action>
  <verify>
    <automated>cargo run -p test-harness --bin test262-run -- --root crates/test-harness/fixtures/test262-lite --auto-gc --auto-gc-threshold 1 --runtime-gc --runtime-gc-interval 1 --expect-gc-baseline crates/test-harness/fixtures/test262-lite/gc-guard.baseline --show-gc --allow-failures</automated>
  </verify>
  <done>Baseline file is consumable by CLI guard and matches the expected stress-profile invariant contract.</done>
</task>

</tasks>

<verification>
Run `cargo test -p test-harness --test test262_lite` and `cargo test -p test-harness --bin test262-run`. Then run the `test262-run` guard command with the baseline fixture to confirm deterministic guard evaluation.
</verification>

<success_criteria>
Harness and CLI gates make MEM-01 regressions observable: default/stress profile behavior is explicitly asserted and guard failures are deterministic.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-safety-and-root-integrity/02-02-SUMMARY.md`.
</output>
