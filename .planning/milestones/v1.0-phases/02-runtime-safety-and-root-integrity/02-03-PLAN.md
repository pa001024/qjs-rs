---
phase: 02-runtime-safety-and-root-integrity
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - crates/vm/src/lib.rs
autonomous: true
requirements:
  - MEM-02
must_haves:
  truths:
    - Invalid and stale object-handle accesses are deterministically classified into distinct categories.
    - Runtime-exposed failure paths convert handle-classification failures into deterministic typed errors.
    - GC/restore exceptional paths reject integrity violations with typed failures instead of panic paths.
  artifacts:
    - crates/vm/src/lib.rs
  key_links:
    - UnknownObject resolution paths feed a single handle-classification helper for InvalidHandle vs StaleHandle.
    - runtime_error_exception_value maps classified handle failures to stable TypeError payloads.
    - restore_caller_state uses a typed VmError path rather than expect-panic on shadow-root mismatch.
---

<objective>
Close MEM-02 by classifying handle failures deterministically and eliminating panic-based failure behavior on runtime-integrity paths.

Purpose: Ensure stale/invalid-handle access always produces stable typed errors and never undefined behavior.
Output: VM handle-classification/error-routing hardening plus regression tests in `crates/vm/src/lib.rs`.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-runtime-safety-and-root-integrity/02-CONTEXT.md
@.planning/phases/02-runtime-safety-and-root-integrity/02-RESEARCH.md
@crates/vm/src/lib.rs

<interfaces>
From crates/vm/src/lib.rs:
```rust
pub enum VmError {
    UnknownObject(u64),
    TypeError(&'static str),
    // ...
}

fn runtime_error_exception_value(&mut self, err: &VmError) -> Option<JsValue>;
fn restore_caller_state(&mut self, ...);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic handle classification helpers and error kinds</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Introduce a slot/generation-aware classifier that distinguishes `InvalidHandle` from `StaleHandle` for missing object lookups. Keep one classification helper as the canonical branch point and use stable message tokens suitable for regression assertions.</action>
  <verify>
    <automated>cargo test -p vm</automated>
  </verify>
  <done>Handle-classification logic exists in one VM helper and produces deterministic categories for malformed/out-of-range vs reclaimed-generation handles.</done>
</task>

<task type="auto">
  <name>Task 2: Route classified handle failures and shadow-root mismatches to typed runtime errors</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Extend runtime error conversion to map classified handle failures into deterministic TypeError payloads, and replace `restore_caller_state` panic-on-missing-shadow-root with a typed `VmError` integrity failure path. Keep normal-path behavior unchanged.</action>
  <verify>
    <automated>cargo test -p vm</automated>
  </verify>
  <done>Script-visible and VM-level failure paths use typed deterministic errors and no longer rely on panic for reachable runtime-integrity violations.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression matrix for stale/invalid handle and exceptional restore paths</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Add VM tests that assert category-stable behavior for stale slot reuse, invalid generation/slot handles, and caller-state restore mismatch paths. Keep assertions specific to error kind and message contract, not generic failure existence.</action>
  <verify>
    <automated>cargo test -p vm</automated>
  </verify>
  <done>Regression matrix proves deterministic MEM-02 behavior across both normal and exceptional handle-integrity paths.</done>
</task>

</tasks>

<verification>
Run `cargo test -p vm` and confirm stale/invalid handle cases and restore-path integrity failures produce deterministic typed errors with no panic behavior in reachable paths.
</verification>

<success_criteria>
MEM-02 is met: invalid/stale handles are rejected with stable typed errors and runtime integrity failures avoid panic-based behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-safety-and-root-integrity/02-03-SUMMARY.md`.
</output>
