---
phase: 04-es-module-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/runtime/src/lib.rs
  - crates/vm/src/lib.rs
autonomous: true
requirements:
  - MOD-01
  - MOD-02
must_haves:
  truths:
    - VM owns canonical-key module records with explicit lifecycle state transitions.
    - Module cache reuse returns existing records without duplicate instantiate/evaluate side effects.
    - Invalid module lifecycle transitions fail with deterministic typed errors.
  artifacts:
    - crates/runtime/src/lib.rs
    - crates/vm/src/lib.rs
  key_links:
    - Module cache record path integrates existing module-cache root candidate safety introduced in Phase 2.
    - Lifecycle state transitions are centralized in one VM transition path with guard assertions.
    - Host-facing resolve/load boundary does not expose direct mutable module graph internals.
---

<objective>
Establish the core module record/cache state machine and deterministic host boundary needed by MOD-01 and MOD-02.

Purpose: Lock deterministic module identity and lifecycle transitions before graph execution logic.
Output: Canonical-key module cache, lifecycle transition guardrails, and baseline state/cache tests.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-es-module-lifecycle/04-CONTEXT.md
@.planning/phases/04-es-module-lifecycle/04-RESEARCH.md
@crates/runtime/src/lib.rs
@crates/vm/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce module record model with explicit lifecycle states</name>
  <files>crates/runtime/src/lib.rs, crates/vm/src/lib.rs</files>
  <action>Add module-record data structures and explicit lifecycle status enum (`unlinked/linking/linked/evaluating/evaluated/errored`) in VM/runtime boundary types. Add transition helpers that reject invalid state jumps with deterministic typed errors.</action>
  <verify>
    <automated>cargo test -p vm module_state_transition_guards -- --exact</automated>
  </verify>
  <done>Lifecycle states and guarded transition helpers exist and deterministically reject illegal transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Add canonical-key module cache and deterministic reuse semantics</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Implement VM-owned module cache keyed by canonical resolved specifier, with get-or-create semantics for records. Ensure completed modules are reused without duplicate instantiate/evaluate side effects, and failed records retain deterministic failure replay behavior.</action>
  <verify>
    <automated>cargo test -p vm module_cache_reuse_semantics -- --exact</automated>
  </verify>
  <done>Module cache behavior is deterministic for first load, repeated load, and failed-record replay paths.</done>
</task>

<task type="auto">
  <name>Task 3: Expose narrow host resolve/load contract without mutable graph access</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Add host-facing resolve/load integration points that return canonical key + source and funnel through VM-owned record creation/transition paths. Guard invalid host interactions and map to fixed typed errors; avoid exposing direct mutable module graph internals.</action>
  <verify>
    <automated>cargo test -p vm module_host_contract -- --exact</automated>
  </verify>
  <done>Host boundary supports deterministic resolve/load entry without violating module cache invariants.</done>
</task>

</tasks>

<verification>
Run targeted VM tests plus `cargo test -p vm` sanity pass to confirm transition guardrails, cache reuse behavior, and host boundary errors remain deterministic.
</verification>

<success_criteria>
Phase 4 foundation is ready: module records, lifecycle states, and canonical cache semantics are implemented and regression-tested before graph execution work.
</success_criteria>

<output>
After completion, create `.planning/phases/04-es-module-lifecycle/04-01-SUMMARY.md`.
</output>
