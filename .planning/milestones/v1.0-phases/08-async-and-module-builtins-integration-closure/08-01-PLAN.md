---
phase: 08-async-and-module-builtins-integration-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/vm/Cargo.toml
  - crates/vm/src/lib.rs
  - crates/vm/tests/module_lifecycle.rs
  - crates/test-harness/src/lib.rs
  - crates/test-harness/tests/module_lifecycle.rs
autonomous: true
gap_closure: true
requirements:
  - ASY-01
  - ASY-02
must_haves:
  truths:
    - Module evaluation no longer fails for baseline `Promise` access due to missing realm builtins.
    - Script and module entry paths expose the same baseline builtin availability contract for Promise-dependent flows.
    - True module evaluation failures still map to deterministic `ModuleLifecycle:*` typed errors after builtin wiring changes.
  artifacts:
    - crates/vm/Cargo.toml
    - crates/vm/src/lib.rs
    - crates/vm/tests/module_lifecycle.rs
    - crates/test-harness/src/lib.rs
    - crates/test-harness/tests/module_lifecycle.rs
  key_links:
    - Module realm initialization in `Vm::evaluate_module_entry`/module execution path installs baseline globals before compiled module chunk execution.
    - VM exact-name regression `module_promise_builtin_parity` proves Promise availability in module path.
    - Harness module-lifecycle regression exercises `run_module_entry` and confirms module Promise behavior stays deterministic.
---

<objective>
Reproduce and close the module-realm baseline builtin gap that triggers Promise-related `ModuleLifecycle:EvaluateFailed` failures.

Purpose: Restore end-to-end module execution parity so Phase 8 async/module closure starts from a stable builtin baseline.
Output: Deterministic reproduction tests, VM module-realm builtin wiring fix, and harness-level parity regression coverage.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-async-and-module-builtins-integration-closure/08-RESEARCH.md
@.planning/v1.0-MILESTONE-AUDIT.md
@crates/vm/src/lib.rs
@crates/vm/tests/module_lifecycle.rs
@crates/test-harness/src/lib.rs
@crates/test-harness/tests/module_lifecycle.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing module-path Promise baseline reproductions with exact-name tests</name>
  <files>crates/vm/tests/module_lifecycle.rs, crates/test-harness/tests/module_lifecycle.rs</files>
  <action>Add deterministic reproductions for the reported module Promise failure path by introducing exact-name tests `module_promise_builtin_parity` (VM) and `module_entry_promise_builtin_parity` (harness). Each test must evaluate a module that references `Promise` directly and through a basic `then` chain, and assert successful evaluation/export values instead of `ModuleLifecycle:EvaluateFailed`.</action>
  <verify>
    <automated>cargo test -p vm module_promise_builtin_parity -- --exact && cargo test -p test-harness --test module_lifecycle module_entry_promise_builtin_parity -- --exact</automated>
  </verify>
  <done>Reproduction tests exist with exact names and fail before the VM builtin wiring fix.</done>
</task>

<task type="auto">
  <name>Task 2: Install baseline builtins in module evaluation realm without weakening lifecycle error typing</name>
  <files>crates/vm/Cargo.toml, crates/vm/src/lib.rs</files>
  <action>Add the required builtin wiring dependency and update module execution setup so module realm initialization installs the same baseline globals used by script execution paths before evaluating compiled module chunks. Preserve existing lifecycle token mapping (`ParseFailed`, `EvaluateFailed`, host contract errors) so only missing-builtin behavior changes.</action>
  <verify>
    <automated>cargo test -p vm module_promise_builtin_parity -- --exact && cargo test -p vm module_error_replay_determinism -- --exact</automated>
  </verify>
  <done>Module Promise baseline tests pass and lifecycle failure tokens remain deterministic for non-builtin failure cases.</done>
</task>

<task type="auto">
  <name>Task 3: Lock harness-level module baseline parity through `run_module_entry`</name>
  <files>crates/test-harness/src/lib.rs, crates/test-harness/tests/module_lifecycle.rs</files>
  <action>Keep `run_module_entry` routed through `Vm::evaluate_module_entry` and extend harness module-lifecycle assertions to prove Promise availability and module cache reuse still hold together. Ensure the test validates both successful Promise module export evaluation and deterministic replay behavior on intentionally failing modules.</action>
  <verify>
    <automated>cargo test -p test-harness --test module_lifecycle</automated>
  </verify>
  <done>Harness module lifecycle suite proves Promise baseline parity on module entry while preserving deterministic failure replay contracts.</done>
</task>

</tasks>

<verification>
Run the two exact-name reproduction commands first, then execute the full harness `module_lifecycle` suite; acceptance requires Promise module cases to pass without regressing existing `ModuleLifecycle:*` deterministic failure tests.
</verification>

<success_criteria>
The previously reported module Promise evaluate failure is reproducibly closed: module path exposes baseline Promise builtin availability with deterministic parity to script path and without lifecycle error-token drift.
</success_criteria>

<output>
After completion, create `.planning/phases/08-async-and-module-builtins-integration-closure/08-01-SUMMARY.md`.
</output>
