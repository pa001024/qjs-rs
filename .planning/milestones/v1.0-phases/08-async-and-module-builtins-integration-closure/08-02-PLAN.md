---
phase: 08-async-and-module-builtins-integration-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - crates/vm/src/lib.rs
  - crates/vm/tests/module_lifecycle.rs
  - crates/test-harness/src/lib.rs
  - crates/test-harness/tests/promise_job_queue.rs
  - crates/test-harness/tests/module_async_integration.rs
autonomous: true
gap_closure: true
requirements:
  - ASY-01
  - ASY-02
must_haves:
  truths:
    - Module-triggered `then/catch/finally` reactions execute in deterministic microtask order equivalent to script-path Promise jobs.
    - Host enqueue/drain callbacks observe module-originated Promise jobs through the same VM hook contract surface.
    - Callback misuse and callback failure paths keep deterministic typed Promise queue errors in module-driven scenarios.
  artifacts:
    - crates/vm/src/lib.rs
    - crates/vm/tests/module_lifecycle.rs
    - crates/test-harness/src/lib.rs
    - crates/test-harness/tests/promise_job_queue.rs
    - crates/test-harness/tests/module_async_integration.rs
  key_links:
    - Module evaluation schedules Promise reactions into the VM’s shared `promise_job_queue` instead of an unobservable side path.
    - `module_promise_queue_semantics` and `module_host_hook_drain_through_module_jobs` prove ASY-01 and ASY-02 through module execution.
    - Harness regression matrix drains queue via host hooks and asserts deterministic ordering plus typed callback failures.
---

<objective>
Add a module-path Promise queue regression matrix that directly proves ASY-01 and ASY-02 semantics through module execution.

Purpose: Close the requirement-evidence gap where Promise queue behavior was only validated in script-driven scenarios.
Output: VM + harness module-async regressions covering queue ordering, nested enqueue, and host hook callback contracts.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-async-and-module-builtins-integration-closure/08-RESEARCH.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/08-async-and-module-builtins-integration-closure/08-01-SUMMARY.md
@crates/vm/src/lib.rs
@crates/vm/tests/module_lifecycle.rs
@crates/test-harness/src/lib.rs
@crates/test-harness/tests/promise_job_queue.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure module-evaluated Promise reactions flow through shared VM queue and host hooks</name>
  <files>crates/vm/src/lib.rs, crates/vm/tests/module_lifecycle.rs</files>
  <action>Update module execution internals so Promise jobs created during module evaluation are observable via the same `enqueue_host_promise_job` and `drain_promise_jobs_with_host_hooks` infrastructure used for script path. Add VM exact-name tests `module_promise_queue_semantics` and `module_host_hook_drain_through_module_jobs` that assert deterministic FIFO ordering, nested enqueue during drain, and expected drain stop reasons.</action>
  <verify>
    <automated>cargo test -p vm module_promise_queue_semantics -- --exact && cargo test -p vm module_host_hook_drain_through_module_jobs -- --exact</automated>
  </verify>
  <done>VM queue semantics and host hook observability are explicitly proven for module-triggered Promise jobs.</done>
</task>

<task type="auto">
  <name>Task 2: Add harness module-async integration matrix for `then/catch/finally` ordering and callback contracts</name>
  <files>crates/test-harness/tests/module_async_integration.rs, crates/test-harness/tests/promise_job_queue.rs</files>
  <action>Create `module_async_integration.rs` with deterministic module scenarios that produce ordered traces across `then`, `catch`, and `finally` chains (including nested reaction enqueue). Extend `promise_job_queue.rs` with exact-name regression `module_path_promise_queue_matrix` that validates host callback event sequence and deterministic typed errors for enqueue/drain callback failures under module-originated jobs.</action>
  <verify>
    <automated>cargo test -p test-harness --test module_async_integration && cargo test -p test-harness --test promise_job_queue module_path_promise_queue_matrix -- --exact</automated>
  </verify>
  <done>Harness regression matrix provides module-path evidence for queue ordering and host callback behavior, including boundary/error paths.</done>
</task>

<task type="auto">
  <name>Task 3: Add reusable harness helper flow for module evaluation + host-driven drain assertions</name>
  <files>crates/test-harness/src/lib.rs, crates/test-harness/tests/module_async_integration.rs</files>
  <action>Add a focused helper in test-harness that evaluates module entry, preserves returned VM state needed for draining Promise jobs with hooks, and avoids duplicate setup logic across module async tests. Use the helper in new module async tests so assertions lock a single host-driven drain contract and avoid implicit auto-drain behavior.</action>
  <verify>
    <automated>cargo test -p test-harness --test module_async_integration module_then_catch_finally_ordering -- --exact && cargo test -p test-harness --test module_async_integration module_host_hook_visibility -- --exact</automated>
  </verify>
  <done>Module async tests consistently execute through one reusable host-driven drain path, locking ASY-01/ASY-02 evidence to deterministic harness behavior.</done>
</task>

</tasks>

<verification>
Run VM exact-name queue tests first, then harness module-async matrix and promise job queue exact-name regression; acceptance requires deterministic ordering assertions and typed callback failure assertions to pass in module-driven flows.
</verification>

<success_criteria>
ASY-01 and ASY-02 are directly evidenced through module execution: queue ordering, nested enqueue, and host callback semantics are no longer inferred from script-only tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-async-and-module-builtins-integration-closure/08-02-SUMMARY.md`.
</output>
