---
phase: 08-async-and-module-builtins-integration-closure
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - .github/workflows/ci.yml
  - docs/test262-baseline.md
  - .planning/phases/08-async-and-module-builtins-integration-closure/08-VERIFICATION.md
autonomous: true
gap_closure: true
requirements:
  - ASY-01
  - ASY-02
must_haves:
  truths:
    - CI runs a deterministic Phase 8 module+async gate set that fails on queue or module-path regressions.
    - Baseline documentation records exact Phase 8 command contracts and expected evidence output locations.
    - Phase verification artifact explicitly maps ASY-01 and ASY-02 to concrete commands, artifacts, and key links (no orphaned requirement IDs).
  artifacts:
    - .github/workflows/ci.yml
    - docs/test262-baseline.md
    - .planning/phases/08-async-and-module-builtins-integration-closure/08-VERIFICATION.md
  key_links:
    - CI workflow step executes the same exact-name VM and harness tests introduced in 08-01/08-02.
    - `docs/test262-baseline.md` and `08-VERIFICATION.md` reference one shared command contract to avoid evidence drift.
    - Verification frontmatter requirement mapping includes both `ASY-01` and `ASY-02` with machine-parseable evidence fields.
---

<objective>
Wire Phase 8 module+async end-to-end gates into CI and verification artifacts with deterministic, auditable evidence output.

Purpose: Prevent regression and close audit traceability by making ASY requirement evidence machine-verifiable.
Output: CI gate step, baseline contract documentation, and schema-aligned Phase 8 verification report mapping ASY-01/ASY-02.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-async-and-module-builtins-integration-closure/08-RESEARCH.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/08-async-and-module-builtins-integration-closure/08-01-SUMMARY.md
@.planning/phases/08-async-and-module-builtins-integration-closure/08-02-SUMMARY.md
@.github/workflows/ci.yml
@docs/test262-baseline.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 8 module+async deterministic gate block to CI</name>
  <files>.github/workflows/ci.yml</files>
  <action>Append a dedicated `Phase 8 Async and Module Integration Gates` CI step after existing workspace tests and prior additive gates. Use exact commands from 08-01/08-02: `cargo test -p vm module_promise_builtin_parity -- --exact`, `cargo test -p vm module_promise_queue_semantics -- --exact`, `cargo test -p vm module_host_hook_drain_through_module_jobs -- --exact`, `cargo test -p test-harness --test module_lifecycle`, `cargo test -p test-harness --test module_async_integration`, and `cargo test -p test-harness --test promise_job_queue module_path_promise_queue_matrix -- --exact`. Keep this additive; do not remove Phase 5/6/7 gates.</action>
  <verify>
    <automated>cargo test -p vm module_promise_builtin_parity -- --exact && cargo test -p vm module_promise_queue_semantics -- --exact && cargo test -p vm module_host_hook_drain_through_module_jobs -- --exact && cargo test -p test-harness --test module_lifecycle && cargo test -p test-harness --test module_async_integration && cargo test -p test-harness --test promise_job_queue module_path_promise_queue_matrix -- --exact</automated>
  </verify>
  <done>CI contains an additive Phase 8 gate block executing deterministic module+async command chain.</done>
</task>

<task type="auto">
  <name>Task 2: Document Phase 8 baseline command contract and expected evidence outputs</name>
  <files>docs/test262-baseline.md</files>
  <action>Add a new Phase 8 section that records the exact CI command contract, expected passing signal format (exact-name tests execute non-zero case counts), and required evidence artifacts referenced by verification. Explicitly state this section closes module-path Promise and host-hook parity gaps for ASY-01/ASY-02 rather than broad test262 module-flag enablement.</action>
  <verify>
    <automated>rg --line-number "Phase 8 Async and Module Integration|module_promise_builtin_parity|module_promise_queue_semantics|module_host_hook_drain_through_module_jobs|module_path_promise_queue_matrix" docs/test262-baseline.md</automated>
  </verify>
  <done>Baseline docs provide a stable, copyable Phase 8 contract aligned to CI commands and audit evidence expectations.</done>
</task>

<task type="auto">
  <name>Task 3: Produce schema-aligned `08-VERIFICATION.md` with explicit ASY evidence mapping</name>
  <files>.planning/phases/08-async-and-module-builtins-integration-closure/08-VERIFICATION.md</files>
  <action>Create/update Phase 8 verification report using the current normalized schema (requirements evidence, command outputs, artifact links, key links, and final status). Map both `ASY-01` and `ASY-02` explicitly to command evidence from Task 1 and regression artifacts from 08-01/08-02 summaries so milestone audit tooling no longer marks them orphaned.</action>
  <verify>
    <automated>rg --line-number "ASY-01|ASY-02|module_promise_builtin_parity|module_promise_queue_semantics|module_host_hook_drain_through_module_jobs|module_path_promise_queue_matrix" .planning/phases/08-async-and-module-builtins-integration-closure/08-VERIFICATION.md</automated>
  </verify>
  <done>Phase 8 verification artifact is machine-parseable and directly binds ASY requirements to deterministic module+async evidence.</done>
</task>

</tasks>

<verification>
Run the full Phase 8 command chain, then validate docs and verification artifact string anchors with `rg`; acceptance requires CI commands, baseline docs, and verification mapping to be synchronized around the same deterministic gate set.
</verification>

<success_criteria>
Phase 8 integration evidence is enforceable: CI, documentation, and verification artifacts all reference one deterministic module+async gate contract that explicitly satisfies ASY-01 and ASY-02.
</success_criteria>

<output>
After completion, create `.planning/phases/08-async-and-module-builtins-integration-closure/08-03-SUMMARY.md`.
</output>
