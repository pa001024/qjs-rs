---
phase: 03-promise-job-queue-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - crates/vm/src/lib.rs
autonomous: true
requirements:
  - ASY-01
must_haves:
  truths:
    - `then/catch/finally` reactions are always enqueued and execute in deterministic FIFO microtask order.
    - Already-settled promises still schedule reactions through the queue, not synchronously.
    - `finally` stays transparent unless it throws/rejects, in which case it deterministically overrides with rejection.
  artifacts:
    - crates/vm/src/lib.rs
  key_links:
    - Promise settlement path appends reaction jobs to the shared queue from plan `03-01`.
    - Promise prototype methods resolve to the same queue-backed reaction registration helpers.
    - Promise state/result storage and reaction execution share one deterministic transition model.
---

<objective>
Implement ASY-01 Promise chain behavior by routing all `then/catch/finally` execution through the deterministic microtask queue.

Purpose: Deliver spec-aligned observable Promise ordering and `finally` semantics without synchronous shortcuts.
Output: Queue-backed Promise state/reaction wiring and ordering regression tests in `crates/vm/src/lib.rs`.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-promise-job-queue-semantics/03-CONTEXT.md
@.planning/phases/03-promise-job-queue-semantics/03-RESEARCH.md
@.planning/phases/03-promise-job-queue-semantics/03-01-SUMMARY.md
@crates/vm/src/lib.rs

<interfaces>
From crates/vm/src/lib.rs (from plan 03-01 output):
```rust
// queue primitives introduced in 03-01 and reused here
fn enqueue_promise_job(&mut self, job: PromiseJob) -> Result<(), VmError>;
pub fn drain_promise_jobs(&mut self, realm: &Realm, budget: usize) -> Result<PromiseDrainReport, VmError>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Promise internal state and reaction registration model</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Replace placeholder Promise tagging with deterministic Promise internals (state/result/reaction lists and settled-once guard). Ensure constructor resolve/reject closures settle once and enqueue registered reactions rather than executing handlers inline.</action>
  <verify>
    <automated>cargo test -p vm promise_constructor_settles_once -- --exact</automated>
  </verify>
  <done>Promise instances track state and reactions with deterministic single-settlement behavior and queue-based reaction scheduling.</done>
</task>

<task type="auto">
  <name>Task 2: Implement queue-backed then/catch/finally semantics</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Implement `Promise.prototype.then`, `Promise.prototype.catch`, and `Promise.prototype.finally` so handlers always execute via queued jobs. Preserve chaining semantics for fulfill/reject paths, ensure `catch` delegates to rejection branch handling, and enforce `finally` pass-through unless callback throws/rejects.</action>
  <verify>
    <automated>cargo test -p vm promise_then_catch_finally_queue_semantics -- --exact</automated>
  </verify>
  <done>Promise chaining behavior is queue-driven and spec-aligned for fulfillment, rejection, and `finally` transparency/override cases.</done>
</task>

<task type="auto">
  <name>Task 3: Lock deterministic microtask ordering with regression matrix tests</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Add focused VM tests for ordering-sensitive scenarios: already-settled `then`, nested handler enqueues, interleaved fulfill/reject chains, and `finally` override behavior. Assert exact execution order tokens and stable observable outputs.</action>
  <verify>
    <automated>cargo test -p vm promise_microtask_ordering_matrix -- --exact</automated>
  </verify>
  <done>Ordering matrix consistently proves FIFO and queue-tail append semantics across nested chain cases.</done>
</task>

</tasks>

<verification>
Run targeted VM Promise semantics tests plus a full `cargo test -p vm` pass. Confirm all Promise reaction paths execute through queue drain points with deterministic order.
</verification>

<success_criteria>
ASY-01 is satisfied in engine behavior: Promise reaction ordering, chaining, and `finally` semantics are deterministic and regression-locked.
</success_criteria>

<output>
After completion, create `.planning/phases/03-promise-job-queue-semantics/03-02-SUMMARY.md`.
</output>

