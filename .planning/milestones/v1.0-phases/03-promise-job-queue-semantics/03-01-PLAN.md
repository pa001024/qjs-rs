---
phase: 03-promise-job-queue-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/runtime/src/lib.rs
  - crates/vm/src/lib.rs
autonomous: true
requirements:
  - ASY-02
must_haves:
  truths:
    - VM owns a single FIFO Promise job queue with deterministic append/drain behavior.
    - Embedding hosts enqueue + drain Promise jobs via required host callbacks (`on_enqueue`, `on_drain_start`, `on_drain_end`) without mutating queue internals directly.
    - Reentrant or invalid host callback interactions fail with fixed deterministic typed error behavior.
  artifacts:
    - crates/runtime/src/lib.rs
    - crates/vm/src/lib.rs
  key_links:
    - Promise prototype method dispatch routes through new runtime function variants wired in VM native dispatch.
    - VM queue drain path uses pending-job root candidate lifecycle so queued captures remain GC-safe until consumed.
    - Host-facing enqueue/drain APIs and required callbacks share one queue-state transition path guarded by reentrancy checks and fixed error mapping.
---

<objective>
Establish ASY-02 host contract foundations by introducing a VM-owned Promise job queue and deterministic enqueue + drain via host callbacks boundaries.

Purpose: Lock the queue contract and safety rails before wiring Promise chain semantics.
Output: Queue data model, host callback enqueue + drain API, and deterministic contract tests in `crates/runtime/src/lib.rs` and `crates/vm/src/lib.rs`.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-promise-job-queue-semantics/03-CONTEXT.md
@.planning/phases/03-promise-job-queue-semantics/03-RESEARCH.md
@crates/runtime/src/lib.rs
@crates/vm/src/lib.rs

<interfaces>
From crates/runtime/src/lib.rs:
```rust
pub enum NativeFunction {
    PromiseConstructor,
    // Promise prototype methods are added in this plan.
}
```

From crates/vm/src/lib.rs:
```rust
pub(crate) fn register_pending_job_root_candidate(&mut self, value: JsValue) -> u64;
pub(crate) fn release_pending_job_root_candidate(&mut self, handle: u64) -> Option<JsValue>;
fn collect_roots(&self, realm: &Realm) -> Vec<JsValue>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Promise-native dispatch surface for queue-backed prototype methods</name>
  <files>crates/runtime/src/lib.rs, crates/vm/src/lib.rs</files>
  <action>Add runtime `NativeFunction` variants and VM native dispatch entries for Promise prototype behavior (`then`, `catch`, `finally`) that will route to queued execution paths. Keep method availability deterministic on `Promise.prototype`, and avoid any synchronous settlement fast path in dispatch wiring.</action>
  <verify>
    <automated>cargo test -p runtime</automated>
  </verify>
  <done>Runtime/VM dispatch tables expose Promise prototype method call targets required by queued semantics with no compile regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce VM Promise job queue core with FIFO and bounded drain</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Implement a VM-owned queue record type (`PromiseJob`) and queue container using strict FIFO ordering. Add enqueue and bounded drain helpers that append nested jobs to queue tail, track `draining` state, and emit deterministic drain reports (processed count, remaining count, stop reason). Ensure queue lifecycle integrates with existing pending-job root candidate registration/release so captures remain rooted until processed.</action>
  <verify>
    <automated>cargo test -p vm promise_job_queue_fifo_ordering -- --exact</automated>
  </verify>
  <done>Queue core behavior is deterministic for FIFO, nested append, and drain budget boundaries, and queue captures remain GC-safe during drain.</done>
</task>

<task type="auto">
  <name>Task 3: Expose required host callback enqueue + drain contract and lock misuse failures</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Add host-facing VM APIs that support enqueue + bounded drain flows via required callback hooks (`on_enqueue`, `on_drain_start`, `on_drain_end`). Treat callback invocation order and transition state as part of the contract, guard invalid transitions (especially reentrant drain/callback misuse), and map failures to fixed deterministic typed errors with stable message tokens. Do not expose direct mutable access to queue internals.</action>
  <verify>
    <automated>cargo test -p vm promise_job_host_contract -- --exact</automated>
  </verify>
  <done>Embedding-facing callback contract for enqueue + drain is mandatory, preserves queue invariants, and rejects invalid callback interactions with fixed deterministic errors.</done>
</task>

</tasks>

<verification>
Run `cargo test -p runtime` and targeted VM queue/host-contract tests. Confirm callback-backed enqueue + drain transitions, bounded drain behavior, callback invocation sequencing, and host misuse failures are deterministic and reproducible.
</verification>

<success_criteria>
ASY-02 contract groundwork is complete: hosts can enqueue + drain Promise jobs via required host callbacks while queue invariants and fixed error behavior are enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/03-promise-job-queue-semantics/03-01-SUMMARY.md`.
</output>
