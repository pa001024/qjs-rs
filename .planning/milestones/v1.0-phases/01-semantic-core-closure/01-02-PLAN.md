---
phase: 01-semantic-core-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/bytecode/src/lib.rs
  - crates/test-harness/tests/semantics_completion.rs
autonomous: true
requirements:
  - SEM-03
must_haves:
  truths:
    - Completion values across if/switch/label/try-finally/loop combinations are deterministic and match expected observable script results.
    - Abrupt completion interactions (break/continue/return/throw with finally) do not create panic/stack-underflow paths.
    - Empty statements and non-value branches do not overwrite prior completion values incorrectly.
  artifacts:
    - crates/bytecode/src/lib.rs
    - crates/test-harness/tests/semantics_completion.rs
  key_links:
    - Bytecode lowering emits and updates completion temporary bindings for control-flow forms that can carry values.
    - VM completion/unwind instructions consume emitted completion ops without requiring ad-hoc runtime value reconstruction.
---

<objective>
Close SEM-03 by hardening completion-value lowering and validating nested abrupt-completion behavior through a focused regression grid.

Purpose: Lock deterministic control-flow completion behavior before async/module phases depend on this runtime baseline.
Output: A completion-focused regression suite and bytecode-level fixes in centralized lowering paths.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-semantic-core-closure/01-CONTEXT.md
@.planning/phases/01-semantic-core-closure/01-RESEARCH.md
@crates/bytecode/src/lib.rs
@crates/test-harness/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nested completion regression grid</name>
  <files>crates/test-harness/tests/semantics_completion.rs</files>
  <action>Create a new integration suite that covers completion values for nested if/switch/label/loop/try-finally flows, including combinations of break/continue/return/throw that previously risk value loss or inconsistent outcomes. Include explicit assertions for expected final script value and expected typed runtime error behavior where applicable.</action>
  <verify>
    <automated>cargo test -p test-harness --test semantics_completion</automated>
  </verify>
  <done>Regression suite exists and validates completion behavior across all SEM-03 control-flow categories listed in this task.</done>
</task>

<task type="auto">
  <name>Task 2: Stabilize completion lowering in bytecode compiler</name>
  <files>crates/bytecode/src/lib.rs</files>
  <action>Adjust completion lowering in existing control-flow compilation paths so completion temporary creation/reset/propagation stays consistent for loop/switch/label/try-finally constructs. Keep the static-empty filtering behavior coherent and avoid VM-only workaround logic unless a compiler-side fix is impossible. Preserve typed runtime errors instead of panic paths.</action>
  <verify>
    <automated>cargo test -p bytecode</automated>
    <automated>cargo test -p test-harness --test semantics_completion</automated>
  </verify>
  <done>All new completion regression cases pass with deterministic results and no panic regressions in targeted compiler/runtime paths.</done>
</task>

</tasks>

<verification>
Run `cargo test -p bytecode` and `cargo test -p test-harness --test semantics_completion`. If a failing case requires runtime unwind alignment, limit follow-up edits to existing completion/unwind choke points and keep behavior test-driven.
</verification>

<success_criteria>
Control-flow completion values are deterministic across nested abrupt/non-abrupt paths, and SEM-03 tests pass without panic behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-semantic-core-closure/01-02-SUMMARY.md`.
</output>
