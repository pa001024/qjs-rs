---
phase: 01-semantic-core-closure
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - crates/vm/src/lib.rs
  - crates/test-harness/tests/semantics_descriptors.rs
autonomous: true
requirements:
  - SEM-04
must_haves:
  truths:
    - Invalid descriptor transitions fail deterministically with typed semantic errors (no silent fallback).
    - Data/accessor exclusivity and configurable/enumerable/writable invariants are enforced consistently.
    - Descriptor readback APIs reflect post-definition attributes consistently across supported object targets.
    - Array length/index descriptor invariants remain enforced during defineProperty/defineProperties edge flows.
  artifacts:
    - crates/vm/src/lib.rs
    - crates/test-harness/tests/semantics_descriptors.rs
  key_links:
    - Object.defineProperty/defineProperties semantic entrypoints funnel through the centralized descriptor transition validator.
    - getOwnPropertyDescriptor/getOwnPropertyDescriptors synthesis mirrors stored descriptor attributes after writes.
---

<objective>
Close SEM-04 by enforcing descriptor invariants through centralized VM property-definition logic and parity-focused descriptor readback tests.

Purpose: Prevent subtle object-model divergence that would invalidate later builtin and module behavior.
Output: Descriptor edge-case regression suite plus deterministic invariant enforcement fixes in VM descriptor paths.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-semantic-core-closure/01-CONTEXT.md
@.planning/phases/01-semantic-core-closure/01-RESEARCH.md
@crates/vm/src/lib.rs
@crates/test-harness/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add descriptor invariant regression suite</name>
  <files>crates/test-harness/tests/semantics_descriptors.rs</files>
  <action>Create integration tests covering non-configurable redefinition failures, accessor/data mutual exclusivity, defineProperties mixed-validity transitions, and descriptor readback parity via getOwnPropertyDescriptor/getOwnPropertyDescriptors. Include array length/index edge assertions where transition rules are strict.</action>
  <verify>
    <automated>cargo test -p test-harness --test semantics_descriptors</automated>
  </verify>
  <done>Regression suite exists and captures SEM-04 edge classes with deterministic success/failure assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce centralized descriptor transition invariants</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Patch descriptor transition logic only in centralized defineProperty/defineProperties/getOwnPropertyDescriptor paths so illegal transitions raise deterministic typed errors, legal transitions preserve attribute consistency, and descriptor synthesis reflects stored state. Avoid feature-local property mutation shortcuts outside these centralized paths.</action>
  <verify>
    <automated>cargo test -p vm</automated>
    <automated>cargo test -p test-harness --test semantics_descriptors</automated>
  </verify>
  <done>All descriptor regression cases pass and VM tests remain green without introducing non-deterministic descriptor behavior.</done>
</task>

</tasks>

<verification>
Run `cargo test -p vm` and `cargo test -p test-harness --test semantics_descriptors`. Confirm failures are expressed as typed semantic errors, not panic paths or silent acceptance.
</verification>

<success_criteria>
Descriptor invariant behavior is deterministic and SEM-04 edge cases are covered by automated regression tests passing in CI-equivalent commands.
</success_criteria>

<output>
After completion, create `.planning/phases/01-semantic-core-closure/01-03-SUMMARY.md`.
</output>
