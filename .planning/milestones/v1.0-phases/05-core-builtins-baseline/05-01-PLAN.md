---
phase: 05-core-builtins-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/vm/src/lib.rs
  - crates/test-harness/tests/native_errors.rs
  - crates/test-harness/tests/test262_lite.rs
autonomous: true
requirements:
  - BUI-02
must_haves:
  truths:
    - Native error subclasses expose dedicated constructor/prototype chains rather than aliasing `Error.prototype`.
    - `name` and `message` defaults/overrides, plus `Error.prototype.toString`, are deterministic across `Error` and standard subclasses.
    - `instanceof` checks pass for both subclass and `Error` ancestors in targeted runtime and harness regressions.
  artifacts:
    - crates/vm/src/lib.rs
    - crates/test-harness/tests/native_errors.rs
    - crates/test-harness/tests/test262_lite.rs
  key_links:
    - Native error constructor `.prototype` wiring in VM points to per-subclass prototype objects.
    - Each subclass prototype chain links to `Error.prototype` through a single shared initialization path.
    - Harness regressions call into runtime constructors and assert prototype-chain plus stringification behavior.
---

<objective>
Close BUI-02 by making Error hierarchy behavior externally correct and regression-locked.

Purpose: Remove prototype alias shortcuts and lock deterministic observable behavior for native errors.
Output: Dedicated native-error constructor/prototype wiring plus focused VM/harness regression coverage.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-core-builtins-baseline/05-CONTEXT.md
@.planning/phases/05-core-builtins-baseline/05-RESEARCH.md
@crates/vm/src/lib.rs
@crates/test-harness/tests/test262_lite.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace native-error prototype aliasing with per-subclass prototype factories</name>
  <files>crates/vm/src/lib.rs</files>
  <action>Refactor native error initialization so `TypeError`, `ReferenceError`, `SyntaxError`, `RangeError`, `EvalError`, and `URIError` each have dedicated prototype objects whose `__proto__` links to `Error.prototype`. Ensure constructor `.prototype` properties point to the matching subclass prototype and remove any fallback paths that return `Error.prototype` for subclasses.</action>
  <verify>
    <automated>cargo test -p vm native_error_constructor_prototype_chain -- --exact</automated>
  </verify>
  <done>Subclass constructors and prototypes are distinct, linked correctly, and no longer depend on shared alias shortcuts.</done>
</task>

<task type="auto">
  <name>Task 2: Lock deterministic name/message defaults and Error#toString behavior</name>
  <files>crates/vm/src/lib.rs, crates/test-harness/tests/native_errors.rs</files>
  <action>Implement/adjust deterministic constructor behavior for omitted/custom `message` values and enforce `Error.prototype.toString` combination semantics for all phase-5 error classes. Add focused runtime-visible tests covering default names, overridden names/messages, and subclass/object coercion boundaries.</action>
  <verify>
    <automated>cargo test -p test-harness --test native_errors</automated>
  </verify>
  <done>`name`/`message` and `toString` semantics are stable and asserted for baseline and boundary/error cases.</done>
</task>

<task type="auto">
  <name>Task 3: Add NativeErrors conformance subset gate to test262 lite coverage</name>
  <files>crates/test-harness/tests/test262_lite.rs</files>
  <action>Extend phase-local lite conformance coverage with a deterministic NativeErrors/Error sample set targeting prototype-chain and constructor behavior (`built-ins/NativeErrors` and `built-ins/Error` smoke subset). Keep command shape stable for CI reuse and avoid broadening scope to non-Phase-5 builtin families.</action>
  <verify>
    <automated>cargo test -p test-harness --test test262_lite native_errors_subset -- --exact</automated>
  </verify>
  <done>Phase-5 native error expectations are enforced by repeatable lite-harness gates.</done>
</task>

</tasks>

<verification>
Run `cargo test -p vm native_error_constructor_prototype_chain -- --exact`, `cargo test -p test-harness --test native_errors`, and `cargo test -p test-harness --test test262_lite native_errors_subset -- --exact` to confirm deterministic Error hierarchy behavior.
</verification>

<success_criteria>
BUI-02 is closed for baseline scope: native error subclass chains, constructor semantics, and stringification are deterministic and regression-protected.
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-builtins-baseline/05-01-SUMMARY.md`.
</output>
