---
phase: 05-core-builtins-baseline
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - crates/vm/src/lib.rs
  - crates/test-harness/tests/core_builtins_baseline.rs
  - crates/test-harness/tests/test262_lite.rs
  - docs/test262-baseline.md
autonomous: true
requirements:
  - BUI-01
must_haves:
  truths:
    - Core builtin subset (`Object`, `Function`, `Array`, `String`, `Number`, `Boolean`, `Math`, `Date`) executes deterministically in targeted phase CI scenarios.
    - Baseline green areas (`Object`, `Array`, `Boolean`) remain regression-locked while remaining high-failure clusters are reduced and stabilized.
    - Function/String/Number/Math/Date behavior is validated with both positive and boundary/error assertions.
  artifacts:
    - crates/vm/src/lib.rs
    - crates/test-harness/tests/core_builtins_baseline.rs
    - crates/test-harness/tests/test262_lite.rs
    - docs/test262-baseline.md
  key_links:
    - Builtin constructor/prototype behavior changes in VM are exercised by phase-specific harness assertions.
    - test262-lite subset profiles reflect the Phase 5 CI contract for the targeted builtin roots.
    - Baseline documentation links measured subset commands/results to required BUI-01 acceptance gates.
---

<objective>
Close BUI-01 by hardening and gating Phase-5 core builtins coverage without expanding beyond defined builtin families.

Purpose: Turn current mixed builtin status into deterministic, regression-checked baseline conformance for the phase target set.
Output: VM semantic closures for remaining core builtin gaps plus dedicated harness + test262-lite phase subset contract updates.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-core-builtins-baseline/05-CONTEXT.md
@.planning/phases/05-core-builtins-baseline/05-RESEARCH.md
@.planning/phases/05-core-builtins-baseline/05-01-SUMMARY.md
@.planning/phases/05-core-builtins-baseline/05-02-SUMMARY.md
@crates/vm/src/lib.rs
@crates/test-harness/tests/test262_lite.rs
@docs/test262-baseline.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze Object/Array/Boolean non-regression and close Function constructor edges</name>
  <files>crates/vm/src/lib.rs, crates/test-harness/tests/core_builtins_baseline.rs</files>
  <action>First lock non-regression expectations for already-strong `Object`/`Array` and `Boolean` behavior, then close remaining `Function` constructor/prototype edge cases that still fail targeted subsets. Preserve existing passing behavior and add both positive and boundary/error assertions for constructor call/new distinctions and metadata invariants.</action>
  <verify>
    <automated>cargo test -p vm core_builtins_object_array_boolean_function -- --exact</automated>
  </verify>
  <done>Object/Array/Boolean stays green and Function edge behavior is deterministically covered in runtime/harness tests.</done>
</task>

<task type="auto">
  <name>Task 2: Close String then Number/Math deterministic gaps in locked priority order</name>
  <files>crates/vm/src/lib.rs, crates/test-harness/tests/core_builtins_baseline.rs</files>
  <action>Implement remaining `String` constructor/prototype semantics first, then close `Number/Math` gaps (including `NaN`, `Infinity`, `-0`, rounding/format edges, and missing Math callable surface). Keep changes scoped to Phase-5 builtins only and add explicit boundary/error assertions alongside positive cases.</action>
  <verify>
    <automated>cargo test -p vm core_builtins_string_number_math -- --exact</automated>
  </verify>
  <done>String and Number/Math targeted subset behavior is deterministic with locked positive and boundary/error regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Close Date deterministic baseline and wire Phase-5 subset CI contract</name>
  <files>crates/vm/src/lib.rs, crates/test-harness/tests/test262_lite.rs, docs/test262-baseline.md</files>
  <action>Finish Date baseline semantics for constructor, `Date.parse`, `Date.UTC`, `getTime`, `toString`, and `toUTCString` with UTC/timestamp-biased assertions that avoid locale-fragile expectations. Update phase-local test262-lite subset profile and baseline docs so CI uses stable, explicit commands/results for `Function/String/Number/Math/Date` plus frozen `Object/Array/Boolean` checks.</action>
  <verify>
    <automated>cargo test -p test-harness --test core_builtins_baseline</automated>
  </verify>
  <done>Date baseline behavior and Phase-5 core builtin subset gates are deterministic and documented for CI execution.</done>
</task>

</tasks>

<verification>
Run `cargo test -p vm core_builtins_object_array_boolean_function -- --exact`, `cargo test -p vm core_builtins_string_number_math -- --exact`, and `cargo test -p test-harness --test core_builtins_baseline` to confirm BUI-01 phase closure.
</verification>

<success_criteria>
Phase-5 core builtin baseline is executable and measurable: target builtins are covered by deterministic runtime behavior plus repeatable harness/CI subset gates.
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-builtins-baseline/05-03-SUMMARY.md`.
</output>
