---
phase: 09-verification-traceability-normalization
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - .github/scripts/check_verification_traceability.py
  - .github/scripts/verification_traceability/fixtures/requirements_traceability_sample.md
  - .github/scripts/verification_traceability/fixtures/phase01-verification-valid.md
  - .github/scripts/verification_traceability/fixtures/phase03-verification-missing-reqs.md
  - .github/scripts/verification_traceability/fixtures/phase08-verification-valid.md
  - .github/workflows/ci.yml
  - .planning/v1.0-MILESTONE-AUDIT.md
  - .planning/phases/09-verification-traceability-normalization/09-TRACEABILITY-RERUN.md
autonomous: true
requirements:
  - None (audit integration debt closure)
must_haves:
  truths:
    - A repo-local traceability checker validates verification schema and computes requirement coverage from frontmatter-only fields.
    - CI blocks merges when verification schema drift, orphaned requirements, duplicate mappings, or invalid requirement IDs are detected.
    - Milestone audit rerun evidence is generated from standardized checker outputs rather than manual fallback interpretation.
  artifacts:
    - .github/scripts/check_verification_traceability.py
    - .github/workflows/ci.yml
    - .planning/v1.0-MILESTONE-AUDIT.md
    - .planning/phases/09-verification-traceability-normalization/09-TRACEABILITY-RERUN.md
  key_links:
    - `check_verification_traceability.py` reads `.planning/REQUIREMENTS.md` and all `.planning/phases/*/*-VERIFICATION.md` frontmatter to compute coverage.
    - `.github/workflows/ci.yml` runs the checker as a blocking gate and persists deterministic output artifacts under `target/`.
    - Milestone audit rerun references checker JSON/Markdown outputs to prove requirement coverage can be computed without narrative parsing.
---

<objective>
Replace manual verification traceability fallback with deterministic repository-native automation and CI enforcement.

Purpose: close audit integration debt by making schema conformance + requirement coverage computation reproducible in local and CI runs.
Output: checker script with fixtures/self-test behavior, CI gate wiring, and milestone audit rerun evidence sourced from checker outputs.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/verification-schema.md
@.planning/phases/09-verification-traceability-normalization/09-VERIFICATION-SCHEMA-MIGRATION.md
@.planning/phases/09-verification-traceability-normalization/09-RESEARCH.md
@.github/scripts/validate_governance.py
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement repo-local verification traceability checker with deterministic outputs</name>
  <files>.github/scripts/check_verification_traceability.py</files>
  <action>Create a Python checker script that: (1) parses all phase verification frontmatter, (2) enforces canonical required fields and requirement ID format (`^[A-Z]{3}-\\d{2}$`), (3) loads canonical requirement IDs from `.planning/REQUIREMENTS.md` traceability table, (4) computes missing/orphaned/duplicate mappings from `requirements_checked` only, and (5) writes deterministic machine + human outputs to `target/verification-traceability.json` and `target/verification-traceability.md`. Return non-zero on any contract violation.</action>
  <verify>
    <automated>python .github/scripts/check_verification_traceability.py --requirements .planning/REQUIREMENTS.md --phases-dir .planning/phases --out-json target/verification-traceability.json --out-md target/verification-traceability.md</automated>
  </verify>
  <done>Traceability checker exists, runs on repository artifacts, emits stable outputs, and fails deterministically on schema/coverage drift.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic fixtures and checker self-test mode</name>
  <files>.github/scripts/check_verification_traceability.py, .github/scripts/verification_traceability/fixtures/requirements_traceability_sample.md, .github/scripts/verification_traceability/fixtures/phase01-verification-valid.md, .github/scripts/verification_traceability/fixtures/phase03-verification-missing-reqs.md, .github/scripts/verification_traceability/fixtures/phase08-verification-valid.md</files>
  <action>Following existing governance script patterns, add `--self-test` support to the new checker and include lightweight fixture files that prove both positive and failure paths (valid mapping, missing `requirements_checked`, missing canonical requirement coverage). Self-test must run without touching live phase files and must fail if negative fixtures stop being detected.</action>
  <verify>
    <automated>python .github/scripts/check_verification_traceability.py --requirements .planning/REQUIREMENTS.md --phases-dir .planning/phases --self-test</automated>
  </verify>
  <done>Self-test mode executes deterministic fixture coverage for both pass and intentional-failure scenarios, preventing silent checker regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Wire blocking CI gate and publish milestone audit rerun evidence</name>
  <files>.github/workflows/ci.yml, .planning/v1.0-MILESTONE-AUDIT.md, .planning/phases/09-verification-traceability-normalization/09-TRACEABILITY-RERUN.md</files>
  <action>Add a dedicated `Verification Traceability Gate` step in CI invoking the repo-local checker with explicit output paths under `target/`. Re-run milestone traceability audit using checker outputs and record the rerun result in both `.planning/v1.0-MILESTONE-AUDIT.md` (status/evidence update) and phase-local rerun evidence document `09-TRACEABILITY-RERUN.md` with command transcript + computed coverage summary. Preserve prior audit history sections; append/update instead of destructive rewrite.</action>
  <verify>
    <automated>python .github/scripts/check_verification_traceability.py --requirements .planning/REQUIREMENTS.md --phases-dir .planning/phases --out-json target/verification-traceability.json --out-md target/verification-traceability.md && rg --line-number "Verification Traceability Gate|check_verification_traceability.py" .github/workflows/ci.yml && rg --line-number "traceability rerun|verification-traceability.json|requirements coverage" .planning/v1.0-MILESTONE-AUDIT.md .planning/phases/09-verification-traceability-normalization/09-TRACEABILITY-RERUN.md</automated>
  </verify>
  <done>CI enforces the new checker and audit artifacts show requirement coverage computed directly from standardized verification frontmatter outputs.</done>
</task>

</tasks>

<verification>
Run checker (normal + self-test) and confirm CI wiring references exactly that command path; acceptance requires reproducible JSON/Markdown outputs and audit rerun evidence tied to generated artifacts.
</verification>

<success_criteria>
Verification traceability becomes a deterministic, machine-enforced contract: schema drift is blocked in CI and milestone coverage can be recomputed from standardized artifacts without manual fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/09-verification-traceability-normalization/09-02-SUMMARY.md`.
</output>

